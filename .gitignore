# Env — NEVER commit secrets
.env
*.env
*.env.local
backend/.env
ai-service/.env

# Node
node_modules/
backend/logs/
npm-debug.log*

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
ai-service/saved_models/*.joblib

# ML artifacts
*.h5
*.pt
*.onnx

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Flutter
mobile/.dart_tool/
mobile/build/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/android/local.properties
mobile/android/app/google-services.json
mobile/pubspec.lock

# Build outputs / APKs
*.apk
*.aab
AiTrading-Railway.apk

# Logs
*.log
backend.log
backend-err.log
aiservice.log
aiservice-err.log

# Test coverage output
backend/coverage/

# Claude Code local session settings (machine-specific, not shared)
.claude/settings.local.json

# Ad-hoc debug/scratch dumps
mobile/_positions_check.json

# Files this session couldn't delete outright (filesystem restriction) — moved
# here instead of deleted; safe to actually delete from disk, never commit
_to_delete/

# Sync bundles Claude generates to move commits between its cloud
# workspace and this device — never project content
*.bundle
