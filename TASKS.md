# TASKS.md — Task Tracker
Statuses: TODO · IN_PROGRESS · BLOCKED · REVIEW · DONE
A task is only DONE when it has actually been verified (tests run, build passes, or manually confirmed) — never because code was merely written.
| ID | Phase | Priority | Description | Status | Dependencies | Files Affected | Verification Method | Completed |
|----|-------|----------|-------------|--------|---------------|-----------------|----------------------|-----------|
| T-001 | 0 | Critical | Full repo audit | DONE | — | — | Read every major dir; ran backend Jest (63/63 pass) and ai-service pytest (105/105 pass) live | 2026-08-18 |
| T-002 | 1 | Critical | Add GitHub Actions CI (backend Jest + ai-service pytest) | DONE | T-001 | `.github/workflows/ci.yml` | Pushed and confirmed running live on GitHub Actions (not just locally validated). Verified via 3 separate workflow runs on `master` (commits `edc6412`, `a41d3fc`, `b201322`) all showing conclusion Success; commit `b201322`'s Backend/Jest check individually confirmed passed in 17s. `origin/master` currently sits at `b201322` — 2 further local commits (`25d25e3`, `45a73de`, both docs/security, PM continuous-improvement pass) are staged locally awaiting the next push from the owner's machine or the terminal session. | 2026-08-18 |
| T-003 | 1 | Medium | Add `npm audit` / `pip-audit` as non-blocking CI step | DONE | T-002 | `.github/workflows/ci.yml` | Steps included in the workflow | 2026-08-18 |
| T-004 | 2 | Critical | Verify live Railway deploy health (backend + ai-service) | BLOCKED | Railway token from owner | — | Hit `/api/v1/health` and `/health` against production URLs | — |
| T-005 | 2 | High | Confirm MongoDB Atlas connectivity in production | BLOCKED | T-004 | — | Health endpoint reports `database: connected` | — |
| T-006 | 2 | Medium | Triage `ai-service-err.log` (376KB local history) | DONE | — | `ai-service/ai-service-err.log` | Read full log (4828 lines), categorized every ERROR/WARNING line, traced one full traceback to its FastAPI source. See T-021 for the filed follow-up. | 2026-08-18 |
| T-007 | 3 | High | Write API documentation for all ~25 route groups | DONE | — | `backend/API.md` | Read all 27 `backend/src/routes/*.js` files plus `middleware/auth.js`, `validate.js`, `errorHandler.js` in full. Every route documented with method, path, auth requirement (JWT/role-gate/webhook-secret/public), and request validation. 3 pre-existing design overlaps found and flagged for future cleanup (3 separate simulation endpoints; no per-user filter on `VirtualTrade`, confirmed intentional not a gap). | 2026-08-18 |
| T-008 | 4 | High | Confirm bcrypt salt rounds + password policy | DONE | — | `backend/src/models/User.js`, `authController.js` | Read source: `bcrypt.hash(password, 12)` — 12 rounds confirmed | 2026-08-18 |
| T-009 | 4 | High | Verify or remove refresh-token flow | DONE | T-007 | `backend/src/models/User.js`, `backend/.env.example` | Investigated full auth flow (backend + mobile). Determined refresh tokens are unnecessary for this app's threat model (guest-account architecture, no real-money custody, graceful 401 handling already in mobile client) and removed the dead `JWT_REFRESH_EXPIRES_IN` config, documenting why in a comment above `generateAuthToken()`. Full backend suite 71/71 passing after the change. Commit `dbb81c3`. | 2026-08-18 |
| T-010 | 4 | Medium | CORS: explicit production allowlist (owner decision: no wildcard) | DONE | — | `backend/src/config/corsConfig.js`, `backend/.env.railway`, `backend/.env.example`, `backend/__tests__/cors.test.js` | Owner decided against `*` in production. `buildCorsOptions()` now defaults unset/empty `ALLOWED_ORIGINS` to deny all browser origins (mobile unaffected — no Origin header). No real web frontend exists yet (`mobile/web/` confirmed unused scaffolding, no hosting config anywhere) so `.env.railway` ships a documented empty placeholder rather than an invented domain. `.env.example` keeps its localhost dev origins. 14 tests (was 8): allowed/disallowed/no-Origin/authenticated-request cases all covered. Full suite 101/101. Commit `a164aa4`. | 2026-08-18 |
| T-011 | 4 | Medium | Run `npm audit` and `pip-audit`, triage findings | DONE | T-003 | `backend/package-lock.json`, `ai-service/requirements.txt`, `ai-service/Dockerfile` | backend: 26→9 vulns (critical+high resolved), 63/63 tests pass after fix. ai-service: 16→0 vulns (torch bumped past CVEs), 105/105 tests pass after fix. | 2026-08-18 |
| T-019 | 4 | High | Risk-based input validation audit of the 24 route files with none | DONE | T-007 | `backend/src/routes/priceAlerts.js`, `brain.js`, `guide.js`, `backend/__tests__/inputValidation.test.js` | Audited all 24 against DB-write/financial-parameter/user-controlled-ID/external-API criteria (not blindly validating all 24 — see security report for the full per-route breakdown). Implemented express-validator on the 3 genuinely under-protected routes (priceAlerts, brain/follows, guide/sellNow); the rest were confirmed low-risk (admin/premium-gated, or already backstopped by Mongoose schema constraints + the global errorHandler's ValidationError/CastError → 400 translation). 13 new tests, full suite 84/84 passing. Commit `1cf84e9`. | 2026-08-18 |
| T-012 | 5 | Medium | Add real Flutter widget/unit tests | IN_PROGRESS | — | + `mobile/test/core/widgets/sparkline_chart_test.dart` | `flutter test` — 132/132 passing. 2 of 3 reusable widgets covered (`price_ticker.dart` blocked — private-method testability gap, see `PROJECT_STATUS.md` round 6); 0 full-screen tests remain. | 2026-08-20 |
| T-013 | 5 | Medium | Add backend↔ai-service integration tests | DONE | T-007 | `backend/__tests__/aiService.test.js`, `backend/__tests__/socialService.aiClient.test.js` | 22 new contract tests covering all 7 `aiService.js` functions + `socialService.js`'s 2 AI-calling functions: exact HTTP method/path/body verified against ai-service's live route table (no drift found), plus graceful-degradation-on-failure behavior. Full backend suite 123/123 passing (101 prior + 22 new). Commit `102c5ef`. | 2026-08-18 |
| T-014 | 6 | Low | Review MongoDB indexes for hot collections (signals, market data, virtual trades) | DONE (static review — see remaining risk) | — | `backend/src/models/Signal.js` | Cross-referenced every existing index against actual `.find()`/`.aggregate()` call sites via `grep` across `backend/src/{controllers,services,jobs,routes}`. `MarketData` and `VirtualTrade` fully covered by existing indexes (confirmed, no changes). `Signal` had a real gap: `getSignals()`'s default no-filter dashboard load and `getLatestSignals()`/`getSignalStats()`'s `status:'active'` queries both sort/filter on `createdAt`, which none of the 3 existing indexes lead with. Added `{createdAt:-1}` and `{status:1, createdAt:-1}`. `npm test` (excluding an unrelated concurrent in-progress file) — 130/130. | 2026-08-18 |
| T-015 | 7 | Low | Remove/relocate large zips and stray `mobile/off`, `mobile/echo`, `mobile/@echo` dirs | TODO | Owner confirmation | project root, `mobile/` | Owner approves deletions; repo size checked after | — |
| T-016 | 7 | Low | Consolidate `backend/.env.railway` vs `backend/.env.example` | DONE | — | `backend/.env.railway`, `backend/.env.example` | Diffed both files against `grep -rhoE "process\.env\.[A-Z_]+" backend/src backend/server.js` (ground truth of what backend actually reads). Both files kept (dev vs Railway-prod are genuinely different environments) but reconciled to match reality: removed `BINANCE_API_KEY`/`BINANCE_SECRET_KEY`/`TWITTER_BEARER_TOKEN`/`REDDIT_*` from `.env.example` — backend never reads them, they're ai-service's vars (confirmed via `ai-service/app/config.py`'s `Settings` class) mistakenly copy-pasted into backend's template. Added real backend-consumed vars missing from both: `BINANCE_WS_URL`, `OPENAI_API_KEY`, `AI_MIN_FUSED_SCORE`, `AI_MIN_QUALITY_SCORE`, `AI_MAX_DAILY_LOSS_PCT`. Corrected `.env.railway`'s stale AI-tuning example values (60/10) to match the code's actual current fallback defaults (70/5) — those numbers predated a "Phase 18" change; this is documentation-only, Railway doesn't read this file at runtime (`dotenv.config()` only loads `.env`), so zero behavioral risk. Verified: `npm test` still 135/135 on the backend-suite-relevant tests (2 unrelated failures present at the same moment came from a different, in-progress concurrent edit to `priceAlertJob.js` — confirmed via `git diff`, not from this change). | 2026-08-18 |
| T-017 | 8 | Critical | Walk full `PRODUCTION_CHECKLIST.md` against live deploy | BLOCKED | T-004 | — | Every checklist item checked off with evidence | — |
| T-018 | 9 | Medium | Set up recurring daily/weekly scheduled task (Cowork) | TODO | Owner approval (deferred until after this audit review) | — | Scheduled task created and first run reviewed | — |
| T-020 | 4 | Low | Add authenticity verification to Telegram webhook (`POST /telegram/webhook`) | REVIEW | Owner action needed (see note) | `backend/src/middleware/telegramWebhookAuth.js`, `backend/src/routes/telegram.js`, `backend/.env.railway`, `backend/.env.example` | Implemented `X-Telegram-Bot-Api-Secret-Token` verification against `TELEGRAM_WEBHOOK_SECRET`, constant-time comparison, fails closed if unconfigured. 11 tests covering correct/missing/incorrect secret, unconfigured-server, and a Telegram-shaped payload sent without the secret. Full suite 101/101. Commit `a164aa4`. **Code is done; not yet live** — needs two manual owner steps outside this session's reach (no bot token available here): (1) set `TELEGRAM_WEBHOOK_SECRET` in Railway's dashboard for the backend service, (2) call Telegram's `setWebhook` API with a matching `secret_token` param. Until both are done, real Telegram messages will stop reaching the bot the moment this deploys (fail-closed is working as intended, not a bug) — see security report for exact commands. | 2026-08-18 |
| T-021 | 6 | Low | Reproduce/fix intermittent `jsonable_encoder` crash found in `ai-service-err.log` triage (T-006) | TODO (additional static evidence gathered, no live reproduction possible right now) | — | Read `app/api/routes.py` (all 42 `return {...}` sites), `app/services/multi_timeframe_analyzer.py`, `app/services/regime_detector.py` in full | Attempted local live reproduction — blocked, no local MongoDB reachable to boot the full service against (production Atlas is also down, see Priority 3). Fell back to a deeper static pass: read every route's response-building code plus the two most-likely offenders by era (`multi_timeframe_analyzer.py` — post-dates the log, matches "Phase 18"/T-028-adjacent rework). All confirmed clean: every `.iloc[-1]` row access is immediately wrapped in `float()`/`round()` before being placed in the response dict, no route returns a raw DataFrame/Series. Strengthens (does not prove) the original hypothesis that the bug was already fixed by the 48 commits of rework since the log's date. | — |
| T-022 | 1 | Medium | Fix invalid CORS config in ai-service (wildcard origin + allow_credentials=True) | DONE | — | `ai-service/app/main.py`, `ai-service/tests/test_cors_config.py` | Found during PM continuous-improvement pass (security-first priority). `allow_origins=["*"]` + `allow_credentials=True` is an invalid/dangerous CORS combination; confirmed via grep this service has no cookie-based auth anywhere (server-to-server only, called by backend's aiService.js/aiWorkerService.js/socialService.js). Fixed to `allow_credentials=False`. 3 new tests pin the fix. Verified in a clean venv: full ai-service suite 108/108 passing (105 prior + 3 new). Commit `25d25e3`. | 2026-08-18 |
| T-023 | 6 | High | Fix data-integrity race: virtualTrackingJob's 5-min cycle and 3x/day funding cycle can concurrently corrupt VirtualPortfolio.currentBalance | DONE | — | `backend/src/utils/asyncMutex.js`, `backend/src/utils/portfolioLock.js`, `backend/src/services/virtualTrackingService.js`, `backend/__tests__/asyncMutex.test.js` | Confirmed both cron schedules (`*/5 * * * *` and `0 0,8,16 * * *`) fire concurrently at 00:00/08:00/16:00 UTC, both doing an uncoordinated fetch->mutate->save on the single shared portfolio document (classic lost-update race). Fixed with a minimal in-process async mutex wrapping the 3 balance-mutating critical sections (`applyFundingPayments`, `checkOpenTrades`, `closePositionNow`). 3 new tests prove serialization + no-wedge-on-error; all 32 pre-existing `virtualTrackingService.test.js` tests + full backend suite re-verified green. Commit `99c16e3`. Follow-up same day: added `_running`-style self-overlap guards to `virtualTrackingJob.js`'s two cron schedules (commit `b18fe4f`) to prevent pileup behind the lock under degraded conditions. | 2026-08-18 |
| T-024 | 7 | Medium | Add missing error handling to `notificationRetryJob.js` (only job of 17 with zero try/catch) | DONE | — | `backend/src/jobs/notificationRetryJob.js` | Found while auditing job-level reliability across all 17 cron jobs. Traced via node-cron 3.0.3 source: async errors are caught internally by node-cron (not a crash risk), but nothing listens for its `'task-failed'` event, so errors here vanished with zero log trace — unlike every other job. Wrapped in try/catch matching the existing pattern; no logic change (confirmed via diff). Commit `f36e45f`. | 2026-08-18 |
| T-025 | 4 | High | AI worker cycle could exceed the portfolio's MAX_OPEN_TRADES risk cap | DONE | — | `backend/src/services/aiWorkerService.js`, `backend/__tests__/aiWorkerService.test.js` | `runAIWorkerCycle()`'s per-opportunity loop was only bounded by `MAX_NEW_PER_CYCLE` (3), independent of `MAX_OPEN_TRADES` (5) — a cycle starting at e.g. 4 open trades (under the cap, so allowed to run) could open 3 more, reaching 7, silently exceeding the declared risk cap. Not caught before because every test's `countDocuments` mock defaulted to 0. Fixed with `if (openCount + tradesCreated >= MAX_OPEN_TRADES) break;` inside the loop. 2 new tests prove the cap now holds and that `MAX_NEW_PER_CYCLE` still independently applies when there's room. Full `aiWorkerService.test.js` (8/8) + `virtualTrackingService.test.js` (32/32) green. Commit `051be27`. | 2026-08-18 |
| T-026 | 4 | Low | `pickupNewSignals()` is dead code (exported + tested, never invoked by any job/route/controller) and would need a T-025-style risk cap if ever activated | TODO | — | `backend/src/services/virtualTrackingService.js` | Confirmed via grep across `backend/src/routes`, `backend/src/controllers`, `backend/src/jobs`, `backend/server.js` — zero call sites outside its own test file. It has NO cap at all on trades opened per invocation (processes up to 50 signals, one VirtualTrade each, unconditionally) — a bigger gap than T-025's if it were ever wired up. Not fixing or removing without an owner decision: could be intentionally-reserved code from before `aiWorkerJob` became the primary auto-trading path, or genuinely obsolete — need to know intent before acting. | 2026-08-18 |
| T-027 | 6 | High | `checkOpenTrades` never validated live price staleness — a stalled WebSocket feed could silently drive TP/SL/liquidation decisions off hours-old prices | DONE | — | `backend/src/services/virtualTrackingService.js`, `backend/__tests__/virtualTrackingService.test.js` | The REST price-poll fallback only ever starts once, 30s after server boot, if the cache was empty then — nothing restarts it if the WebSocket drops later at runtime. Added a 10-minute `PRICE_STALENESS_MS` threshold; a too-old cached price is now treated exactly like "no price" (already handled safely). 2 new tests: stale price never closes a trade even though it would trigger TP; fresh price still closes normally. Full suite 34/34 + 8/8 green. Commit `5931553`. | 2026-08-18 |
| T-028 | 5 | High | `_score_crypto()` (ai-service) always used a hardcoded "TRENDING" regime instead of real regime detection, biasing every crypto trade's SL/TP width and score | DONE | — | `ai-service/app/services/regime_detector.py`, `ai-service/app/services/global_analyzer.py`, `ai-service/tests/test_regime_detector.py`, `ai-service/tests/test_global_analyzer_regime.py` | `GlobalAnalyzer._score_crypto()` read `regime = result.get("regime", "TRENDING")`, but `UnifiedAnalyzer.analyze()`'s response never contains a "regime" key at all (confirmed by full-file inspection — no such field is ever set) — so every crypto asset, every scan, silently fell back to TRENDING: always 1.5x/3.0x ATR SL/TP width, always +10% BUY / -20% SELL score bias, regardless of real market conditions. The non-crypto path (`_score_multi_asset`) was unaffected — it already called the real detector correctly. Fixed by adding `RegimeDetector.detect_from_values()` (a scalar-input sibling to the existing df-based `detect()`, which now delegates to it — no behavior change there) and wiring `_score_crypto` to call it with the ema50/ema200/atr/price it already has on hand, avoiding a second data fetch. 15 new tests (11 for `RegimeDetector`, 4 for the `_score_crypto` regime wiring, including one proving the fix yields different SL/TP than the old always-TRENDING bug would have). Verified in a clean venv: full ai-service suite 120/120 passing (105 prior + 15 new), zero regressions. `position_size`'s separate Kelly-lite formula in `risk_manager.py` was also audited during this pass and found to be effectively always capped at the flat 10% ceiling for any realistic ATR — but confirmed via grep that the backend never reads this field for real order sizing (it computes its own `sizeUsd` independently), so it's informational-display-only, not a live risk defect; left undocumented as a separate task since it doesn't meet the same demonstrable-live-impact bar. | 2026-08-18 |
| T-029 | 8 | High | Price alerts never fire — `checkAlerts()` compared a `{price,ts}` object against a numeric target price | DONE | — | `backend/src/jobs/priceAlertJob.js`, `backend/__tests__/priceAlertJob.test.js` | `binanceService.getAllCachedPrices()` returns `{ asset: { price, ts } }` (confirmed by reading `binanceService.js` — `priceCache.set()` always stores that shape), but `checkAlerts()` compared the whole object directly against `alert.targetPrice` (`currentPrice >= alert.targetPrice` where `currentPrice` was `{price, ts}`). An object compared to a number always evaluates false via string coercion, so `triggered` was always false — Price Alerts, the entire purpose of this job, silently never fired for any user, ever, with zero error trace (job had zero prior test coverage, which is exactly why this went unnoticed). Fixed by extracting the numeric `.price` field (with a defensive fallback for a bare-number shape too). 6 new tests (above/below triggering against the real object shape, no-trigger-when-not-crossed, missing-asset-skipped-safely, bare-number-fallback, plus a regression guard asserting the real module actually exports whatever function name the mock stands in for). Full backend suite re-verified 135/135 green initially, then a second, independent bug was found and fixed in the same file (see follow-up note) bringing the final count to 136/136. **Follow-up same pass:** while writing the notification-delivery test, discovered `checkAlerts()` also called `sendPushNotification` -- a function that has never existed anywhere in `notificationService.js` (grepped the whole backend: zero definitions, one call site). Destructuring a nonexistent export silently yields `undefined`, so every triggered alert would throw `TypeError: sendPushNotification is not a function` the instant it tried to notify -- caught by the outer try/catch, but only *after* the alert had already been marked inactive, so the user got no notification and the alert could never fire again either. Fixed by switching to the real `sendPushToUser(userId, title, body, data)` export (which also handles the FCM-token lookup and invalid-token cleanup internally, letting the now-unused explicit `User.findById` be removed). A dedicated regression-guard test (`jest.requireActual` against the real module) now asserts the mocked function name is a real export, specifically to prevent this exact failure mode -- a test mocking a function that doesn't exist -- from recurring. Commits `5f31612`, `b5d104c` (first fix + docs), plus a follow-up commit for the second fix. | 2026-08-18 |
| T-030 | 7 | Medium | `expireOldSignals()` (signalJob.js) had no try/catch despite being a direct hourly cron callback | DONE | — | `backend/src/jobs/signalJob.js`, `backend/__tests__/signalJob.test.js` | A precise per-scheduled-function sweep (checking every `cron.schedule(...)` call site's actual callback, not just per-file try/catch presence like T-024's original grep did) found `expireOldSignals` was the one remaining scheduled function with no try/catch of its own — it slipped past T-024 because `signalJob.js` has a try/catch elsewhere (in `processAsset`), so the file-level check didn't flag it. node-cron catches an async task's rejection internally (confirmed via source in T-024), so this was never a crash risk, but a `Signal.updateMany` failure would vanish with zero log trace. Wrapped to match every other scheduled job function; exported `expireOldSignals` (previously internal-only) so it's directly testable. 3 new tests. Full backend suite re-verified 139/139 green, zero regressions. Also ran two systematic sweeps this pass (backend JS + ai-service Python) checking every `require()`/`import` destructuring against the target module's real exports, specifically to catch more instances of T-029's second bug (calling a function that was never actually exported) — zero further instances found in either codebase, and a matching sweep of every `jest.mock()` call against the real module it replaces also came back clean. | 2026-08-18 |
| T-031 | 7 | High | Hourly AI Report always showed a fake $500/$0-change/0-open-trades portfolio instead of the real one | DONE | — | `backend/src/jobs/hourlyReportJob.js`, `backend/__tests__/hourlyReportJob.test.js` | `generateHourlyReport()` queried `VirtualPortfolio.findOne({ key: 'global' })`, but the schema's real field is `portfolioKey` (confirmed in `VirtualPortfolio.js`; every other caller in the codebase — `budgetController.js`, `aiWorkerService.js`, `virtualTrackingService.js` — already queries `portfolioKey` correctly). A query on a nonexistent field always matches nothing, so the real portfolio lookup silently failed every single hour, and the report permanently fell back to its hardcoded placeholder (`{balance:500, change:0, changePct:0, openTrades:0}`) — baked into the stored `AIReport` document, the Telegram message sent to every subscribed user and the admin channel, the push notification, and (if `OPENAI_KEY` is set) the GPT prompt used to write the report's narrative. Fixed the field name. 3 new tests (correct query shape, real portfolio data flows through instead of the placeholder, missing-document still falls back safely). Full backend suite re-verified 142/142 green, zero regressions. | 2026-08-18 |
| T-032 | 2 | Critical | `telegramController.handleWebhook` had no try/catch — any unhandled rejection in it could crash the entire backend server via server.js's global handler | DONE | — | `backend/src/controllers/telegramController.js`, `backend/__tests__/telegramWebhookErrorHandling.test.js` | Discovered while auditing every backend controller for the "does every async route handler contain its own errors" property (the same class of check that closed T-024/T-030 for cron jobs). `handleWebhook` was the one route handler in the entire codebase with no try/catch and no `next(err)` — every other controller wraps its body one way or the other. Express 4 does not await async route handlers, so an unhandled rejection thrown after the initial `res.sendStatus(200)` (e.g. `User.findOne`/`user.save()` failing on a DB blip, or Telegram sending a malformed update) becomes a real process-level `'unhandledRejection'` event. `server.js` listens for exactly that event and responds with `server.close()` then `process.exit(1)` — so a single failed await inside this one Telegram webhook handler could have taken down the entire backend process (every API route, every WebSocket connection, every scheduled job) for every user, not just failed the one Telegram interaction. Currently dormant in practice: `verifyTelegramWebhook` (T-020's fix) fails closed without `TELEGRAM_WEBHOOK_SECRET` configured, and per the standing owner instruction Telegram production config is not active — but this was a real, demonstrated crash path waiting for the moment Telegram is ever turned on, independent of whether it fires today. Fixed by wrapping the handler body in try/catch, logging instead of letting the error escape. 4 new tests (a `User.findOne` failure during `/start`, a `user.save()` failure during `/stop`, confirms the 200 ack still fires first, and a malformed body with no `message` is a safe no-op) — all assert the handler's own returned promise resolves rather than rejects, which is what would have been unhandled before the fix. Full backend suite re-verified 146/146 green (142 previous + 4 new), zero regressions. Also confirms, by exclusion, that this was the only such gap across all 26 backend controllers — every other one was read in full this pass and already wraps its body correctly. | 2026-08-18 |
| T-033 | 2 | High | `collect_reddit_posts()`/`collect_tweets()` silently fell back to hardcoded mock posts, indistinguishable from real data, whenever live collection failed or returned nothing — feeding fabricated sentiment straight into production trade-signal fusion with zero log trace | DONE | — | `ai-service/app/services/collectors/reddit_collector.py`, `ai-service/app/services/collectors/twitter_collector.py`, `ai-service/app/services/social_analyzer.py`, `ai-service/tests/test_reddit_collector.py`, `ai-service/tests/test_twitter_collector.py`, `ai-service/tests/test_social_analyzer.py` | Found while auditing the ai-service collectors/ and intel/ submodules after finishing order_block_engine.py. `telegram_collector.py` had already been rewritten in an earlier pass (commit `a1864b5`, by the terminal session) specifically because its old mock-fallback meant "the social sentiment score has never actually reflected real Telegram content" — but the identical pattern was still live in `reddit_collector.py` (`return posts if posts else _get_mock_posts()`) and `twitter_collector.py` (`return tweets if tweets else _get_mock_tweets()`), both completely untested (zero prior test coverage on either file, or on `social_analyzer.py`). `SocialAnalyzer.refresh()` — which feeds `order_block_engine._get_sentiment()` and other signal paths — treats whatever these functions return as real posts with no way to tell fabricated content apart from genuine Reddit/Twitter chatter, meaning a Reddit/Twitter outage, rate-limit, or IP block (realistic on a hosted deployment) could silently replace live sentiment with fixed, mostly-bullish-skewed hardcoded text feeding directly into trade confidence scores, with no log line anywhere flagging that this had happened. Fixed by adding `is_mock: bool = False` to both `RedditPost` and `Tweet` dataclasses (set `True` only on the hardcoded fallback posts/tweets), adding a `logger.warning(...)` at each fallback site naming that live data collection failed and sentiment is not live right now (previously silent — only the no-token Twitter case logged anything, and only at info level), and propagating `is_mock` through `social_analyzer.py`'s `_to_post_dict()` plus a new aggregate warning in `refresh()` that names which platforms and how many posts were mock whenever any are present. This does not change trading behavior (mock content still flows through, same as before, since removing it outright could reduce sentiment coverage during a brief transient outage) — it makes a previously silent data-integrity gap operationally visible for the first time, so an operator watching logs (or, in future, a consumer of the `is_mock`/warning signal) can tell live sentiment from fabricated sentiment. 15 new tests across the three files: mock posts/tweets are correctly tagged, real posts/tweets default to `is_mock=False`, the fallback path fires and logs a warning only when live collection actually returns nothing (both the "no token" and "token configured but empty" cases for Twitter), the non-fallback path never logs the mock warning, and `SocialAnalyzer.refresh()` emits (or correctly omits) its aggregate mock-visibility warning depending on whether any collected post is mock. Verified in a clean venv built specifically for this pass (fastapi/pydantic/aiohttp/vaderSentiment/bs4/lxml/pytest/pytest-asyncio, plus numpy/pandas/motor/yfinance/starlette to unblock unrelated pre-existing suites): full ai-service suite 135/135 green (120 prior + 15 new), zero regressions; `test_cors_config.py` excluded from that run only because it imports the full FastAPI app graph including `sklearn`/`transformers`/`torch`-backed model files unrelated to this change, which weren't worth installing just to re-verify a file this pass never touched. | 2026-08-18 |
| T-034 | 2 | High | `GlobalAnalyzer._get_macro_sentiment()` read the wrong service with the wrong key and the wrong value vocabulary — the Phase 18 macro-contradiction block and macro-weighted scoring silently never worked as designed | DONE | — | `ai-service/app/services/global_analyzer.py`, `ai-service/app/api/routes.py`, `ai-service/tests/test_global_analyzer_macro.py` | Found while auditing `global_analyzer.py`'s macro-sentiment sourcing after closing T-033. `_get_macro_sentiment()` called `self._news.refresh()` (NewsAnalyzer — per-asset headline sentiment) and read a key `"overall_sentiment"`. Three compounding bugs: (1) NewsAnalyzer's real response never contains that key inside `"global"` — it's named `"sentiment"` (confirmed in `news_analyzer.py`: `"sentiment": analysis["overall_sentiment"]` renames it on the way out) — so `.get("overall_sentiment", "neutral")` silently fell back to `"neutral"` on every single call, the same bug class as T-031's `key`/`portfolioKey` mismatch; (2) even with the key fixed, NewsAnalyzer's `"sentiment"` field only ever takes `"positive"`/`"negative"`/`"neutral"` (confirmed in `news_sentiment.py`'s `analyze()`), never the `"bullish"`/`"strong_bull"`/`"strong_bear"` vocabulary this module's own `_STRONG_BEAR_BLOCKS_BUY`/`_STRONG_BULL_BLOCKS_SELL` sets and `macro_sc` checks require — a vocabulary mismatch, not just a missing key; (3) it was reading the wrong service entirely — `macro_data_service.py`'s `MacroDataService.get_macro_snapshot()` is what actually derives that exact 5-state vocabulary (`strong_bull`/`mild_bull`/`neutral`/`mild_bear`/`strong_bear`) from Fear & Greed + 24h market-cap change + BTC funding rate, and is also the vocabulary `trade_quality_scorer.py`'s `build_quality_inputs()` docstring documents expecting. Net effect before this fix: the Phase 18 "macro contradiction block" this module's own docstring advertises (blocking BUY signals during a strong bear macro backdrop and vice versa) never fired on any scan — macro state was always `"neutral"` — and every `macro_sc` computation in `_score_crypto`/`_score_multi_asset` was always exactly 50 regardless of real bullish/bearish conditions, silently flattening a whole scoring dimension on every `/global/scan` call. Fixed by wiring a `MacroDataService` into `GlobalAnalyzer` for the first time: added a `macro_service=None` constructor param (defaults to a real `MacroDataService()` so existing 3-positional-arg call sites, including `test_global_analyzer_regime.py`'s 4 tests, keep working unchanged), rewrote `_get_macro_sentiment()` to call `self._macro.get_macro_snapshot()` and read `"macro_bias"`, and fixed `_macro_news_score()` (which legitimately stays on NewsAnalyzer — it's real per-asset headline sentiment, not macro backdrop) to read the correct `"sentiment"` key with the correct `positive`/`negative` vocabulary. `routes.py` updated to construct `macro_service = MacroDataService()` before `global_analyzer` and pass it in explicitly. 11 new tests: `_get_macro_sentiment()` correctly reads `macro_bias` from a fake `MacroDataService` (proven by giving the fake NewsAnalyzer a contradicting `"neutral"` answer it must NOT use), returns `"neutral"` safely on a macro-service exception, all 5 real `macro_bias` states pass through unmodified, `GlobalAnalyzer()` with no `macro_service` arg constructs a real `MacroDataService` (backward-compat proof), an explicit `macro_service` arg is used instead of the default, `_macro_news_score()` correctly uses the fixed key/vocabulary (including a regression guard proving a stale `"overall_sentiment"` key is ignored in favor of the real `"sentiment"` key), headline keyword hits still boost the score, exceptions still return the safe 50 default, and an integration-level test proving a `"strong_bear"` macro snapshot is what a live `scan_all()` call would now see and use to block BUY signals (previously impossible to test since macro state could never be anything but neutral). Verified in a fresh venv against a clean pull of the device's current `ai-service/app` + `ai-service/tests` (i.e. including T-033's already-committed fix, not a stale scratch copy): full suite 146/146 green (135 prior + 11 new), zero regressions. | 2026-08-20 |
| T-035 | 2 | Medium | `NewsQualityLayer`'s per-source trust registry was never actually used to weight fused sentiment — every source that passed the quality filter moved `global.sentiment`/`global.market_score` with equal weight, regardless of trust | DONE | — | `ai-service/app/services/news_analyzer.py`, `ai-service/tests/test_news_analyzer_trust_weighting.py` | Found while auditing `news_analyzer.py`/`news_quality_layer.py` immediately after closing T-034 (which reads `global.sentiment` for per-asset headline scoring in `GlobalAnalyzer._macro_news_score()`). `NewsQualityLayer`'s own class docstring documents its quality formula as Source trust (40%) + Content quality (30%) + Recency (20%) − Spam/clickbait penalty, and it builds a real per-source trust registry (`Reuters`/`Bloomberg Markets` at 1.00 down to `Google News`/`Unknown` at 0.40–0.55) plus a dedicated `weighted_sentiment_score()` method meant to apply that trust weighting — but a grep across the entire `app/` tree confirmed `weighted_sentiment_score()` was never called anywhere. Past `filter_and_score()`'s binary `MIN_QUALITY_SCORE` pass/fail cutoff, `NewsAnalyzer.refresh()` fed every passed article's headline into `NewsSentimentModel.analyze()`, which computes a plain unweighted average of per-headline compound scores — meaning a low-trust, borderline-quality source (e.g. `CryptoPanic` at 0.60 or `Google News` at 0.55) moved the fused global sentiment exactly as much as a `Reuters` or `Bloomberg Markets` headline (1.00), silently defeating the entire point of the trust registry beyond the initial pass/fail gate. This directly affects `GlobalAnalyzer._macro_news_score()` (T-034), which reads `global.sentiment` for per-asset headline scoring across every non-crypto asset on every `/global/scan` call. Fixed by trust-weighting the `global` section of `NewsAnalyzer.refresh()`'s result: since `headlines` is built directly from `filtered` (`[a["title"] for a in filtered]`), each entry in `analysis["results"]` lines up positionally with its source article's `trust_score`, so the two are paired and passed through the already-existing (previously unused) `weighted_sentiment_score()` to produce the real `global.sentiment`/`global.market_score`/logged score. Falls back to the prior unweighted behavior unchanged whenever quality filtering rejects every article (the `articles[:20]` fallback path has no trust scores attached, so weighting is skipped exactly as before this fix). Per-asset (`by_asset`) scores and the quality *pass/fail* gate itself are unchanged — this only fixes weighting *among already-passed* articles. 3 new tests (zero prior coverage on `news_analyzer.py`/`news_quality_layer.py`, same untested-module pattern behind T-033/T-034): a high-trust Reuters headline correctly outweighs an opposite-sentiment low-trust Google News headline (naive average would be exactly neutral/50, the trust-weighted result is provably positive), the weighted output matches the manual trust-weighted formula, and the no-articles-pass-quality-filter fallback path is proven completely unchanged (still the old unweighted neutral/50 default). Verified in a fresh venv against a clean pull of the device's current `ai-service/app` + `ai-service/tests` (including T-033 and T-034's already-committed fixes): full suite 149/149 green (146 prior + 3 new), zero regressions. | 2026-08-20 |
| T-036 | 6 | Low | `GlobalAnalyzer._score_multi_asset()` hardcoded `500` as the account balance passed into `RiskManager.compute_position_size()`, ignoring the real `capital` argument `scan_all()` receives and correctly threads through to `_score_crypto()` (the crypto sibling in the same file) | DONE | — | `ai-service/app/services/global_analyzer.py`, `ai-service/tests/test_global_analyzer_position_size.py` | Found auditing `global_analyzer.py` for the continuation of the core-trading-module audit (post-T-035). Same bug shape as T-025/T-031/T-034 (a value silently diverges from what the code visibly intends) but on the non-crypto (`gold`/`oil`/`forex`) scoring path specifically — every such asset's `position_size` field in a `/global/scan` response was wrong whenever the real caller-supplied capital wasn't exactly $500 (e.g. `aiWorkerService.js` calls `/global/scan` with the real `portfolio.currentBalance`). Confirmed via grep — same as T-028's note on this same field — that neither the backend nor mobile ever reads `position_size` for real order sizing or display, so this had no live trading or user-facing impact; fixed anyway since it's a genuine correctness bug in an advertised API field and a trivial, zero-risk one-line-per-call-site fix (thread `capital` through `_score_multi_asset`'s signature, matching the existing crypto-path pattern exactly). 2 new tests: `position_size` scales correctly with capital (500→$10, 2000→$40, via the function's own `entry_price<=0` early-return branch, isolating exactly which capital value reached `RiskManager`), and `scan_all()` is proven to pass its real `capital` argument through to every `_score_multi_asset()` call (previously always silently 500 internally regardless of what was passed). Verified: full ai-service suite 154/154 green, zero regressions. Commit `2b413be` (autonomous commit/push, standing permission granted 2026-08-21). | 2026-08-21 |
| T-037 | 5 | High | `StrategyEngine.analyze()`'s "expected move" scaling used `len(df)` instead of `limit` — the sqrt-of-time timeframe scaling factor was silently near-constant across all 3 timeframes | DONE | — | `ai-service/app/services/strategy_engine.py`, `ai-service/tests/test_strategy_engine.py` | Continuing the core-trading-module audit (post-T-036), auditing `signal_engine.py` (clean — no bugs found, all news/social/regime/mtf/funding key-wiring verified correct against their real source modules) then `strategy_engine.py` (zero prior test coverage, same untested-module pattern as T-033/34/35/36). The docstring/comment claims volatility is "annualised to timeframe" via `returns.std() * 100 * (len(df) ** 0.5)`, but `fetch_market_data` is always called with `limit=max(limit + 50, 100)`, which floors to exactly 100 for all three `_TIMEFRAME_MAP` entries ("1d": limit=24, "7d": limit=42, "30d": limit=30 — `max(x+50,100)` is 100 for all three) — so `len(df)` was ~100 regardless of which timeframe was actually requested, making the scaling factor silently constant instead of varying by timeframe. The correct scaling factor was sitting unused in the same function's scope the whole time: `limit` (from `_TIMEFRAME_MAP`, e.g. 24 hourly candles = 1 day) is exactly the standard sqrt-of-time formula's period count (`sigma(T) = sigma(1) * sqrt(T)`) — `fetch_market_data`'s padded fetch size was never meant to be that scaling factor, it exists only to give `returns.std()` a larger, more statistically stable sample. Real user-facing impact (unlike T-036's inert field): `expected_move_percent` is displayed directly on the mobile Strategy screen (`mobile/lib/core/models/strategy_model.dart` parses `expected_move_percent`/`expectedMove`) and feeds `expected_profit`/`expected_loss` in every `/unified/analyze` response, consumed by `unified_provider.dart`, `dashboard_screen.dart`, `ai_brain_screen.dart`, and `decisionTrackingJob.js`/`AIDecision.js`. Fixed by using `limit` instead of `len(df)`. 2 new tests: requesting "1d" and "7d" against the *identical* underlying synthetic price series (the fake fetch ignores the requested limit, so `len(df)` is identical for both calls — this is precisely the condition that would make the old bug produce identical output) now correctly produces *different* `expected_move_percent` values whose ratio matches `sqrt(42/24)` exactly (regression: under the old bug these would have been equal); a second test verifies the output matches the documented sqrt-of-time formula exactly for a single timeframe. Verified: full ai-service suite 156/156 green, zero regressions. Commit `a284452` (autonomous commit/push, standing permission granted 2026-08-21). | 2026-08-21 |
| T-038 | 6 | Medium | `RiskManager.compute_position_size()`'s "Kelly-lite" ATR-normalized formula is dominated by `max_cap` for the entire realistic ATR/price range with current defaults — every position-size recommendation is silently a flat 10% of capital, not ATR-scaled | DONE (documented + tested; parameter recalibration left for owner) | — | `ai-service/app/services/risk_manager.py`, `ai-service/tests/test_risk_manager.py` | Continuing the core-trading-module audit (post-T-037, coordinating with the terminal session's parallel T-036/T-037 work on the same file list — confirmed via `git log` neither touched `risk_manager.py`'s internals). Zero prior test coverage existed on this file. The function's own docstring describes `size = balance × (risk_pct / atr_normalized)`, capped at `max_cap` — implying the ATR-normalized formula is the primary driver and the cap is a rare safety ceiling. Computed the formula across the entire realistic ATR/price range (0.1%–19%, well beyond what any tracked crypto/forex/commodity asset sustains even in sharp moves) and found every single value hits exactly the flat cap (`balance × max_cap`) — the raw formula only produces a lower value once `atr_normalized` exceeds `risk_pct / max_cap = 20%`, an ATR/price ratio effectively never seen in practice. Confirmed via grep that both call sites (`global_analyzer.py`'s `_score_crypto`/`_score_multi_asset`, which feed the `position_size` field shown on every `/global/scan` result) use the function's defaults, so every recommended position size in the app is currently `capital × 10%` regardless of real volatility, not risk-normalized as documented. Unlike T-034/T-035/T-036/T-037 (each had one unambiguous correct fix confirmed against another source of truth in the codebase), there is no such reference here — `RISK_PCT`/`MAX_POS_CAP` are risk-policy constants, not something this pass can correct with a single objectively-right value. Rather than unilaterally picking new numbers (a real risk-calibration decision, not a bug fix), this pass: (1) rewrote the docstring to accurately describe the actual behavior instead of implying dynamic ATR-scaling that doesn't occur in practice, and (2) added 15 new tests (zero prior coverage) covering `compute_sl_tp()` across all 4 regimes plus edge cases, and `compute_position_size()`'s actual contract including a parametrized regression guard proving the flat-cap behavior across 7 realistic ATR/price values plus a confirming test that only an extreme (25%) ATR/price ratio produces a below-cap value — so any future change to `RISK_PCT`/`MAX_POS_CAP` forces a conscious look rather than a silent behavior shift. Flagged as an OWNER DECISION in the report for this pass: whether to recalibrate `RISK_PCT`/`MAX_POS_CAP` so the ATR-based formula actually varies position size with volatility as originally documented, or to keep/relabel the current flat-cap behavior as intentional. No live trading impact (paper-trading/signals only; `position_size` is not read by backend order execution or mobile display, per T-036's confirmation). Verified: full ai-service suite 174/174 green (153 prior + 21 new — 15 test defs, one parametrized ×7), zero regressions. | 2026-08-22 |
## Notes
- BLOCKED tasks (T-004, T-005, T-017) need the Railway API token the owner said they'll provide.
- T-018 is intentionally not yet scheduled — owner asked to review this first audit before turning on unattended recurring runs.
- T-021: filed at Low priority deliberately — the log is 3.5 months old and predates 48 commits of AI/ML rework (multi-timeframe gating, per-asset Transformers, regime wiring) that plausibly already touched this code path. Not worth speculative fixing without a live reproduction first.
- T-021 supplementary evidence (2026-08-18, PM pass): attempted a live reproduction via `social_analyzer.refresh()` + `jsonable_encoder` directly in a clean cloud venv — did **not** reproduce. Caveat: collectors had no real Reddit/Twitter/Telegram credentials available in that environment (degraded/empty data path), and FinBERT failed to load (403, fell back to VADER per the existing graceful-degradation design) — so this doesn't rule out the crash still occurring under real data on the other candidate endpoints (`/unified/analyze`, `/regime/{asset}`, `/strategy/*`, `/advisor/analyze`) the original triage flagged. Status stays TODO, not resolved by this note.
