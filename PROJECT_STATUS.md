# PROJECT_STATUS.md

Last updated: 2026-08-18, by Claude (technical lead session — Priority 2 complete, incl. CORS allowlist + Telegram webhook follow-up)

## CHECKLIST DISCIPLINE (per owner instruction)
Every item below carries STATUS / EVIDENCE / DATE — nothing is marked PASS without evidence.

---

### PRIORITY 1 — GitHub / CI

**Local commits (audited, tested, correctly authored)**
STATUS: PASS
EVIDENCE: 6 commits on local `master`, all authored `Claude <noreply@anthropic.com>` (re-authored after a repo hook flagged the first 3 as carrying the owner's identity): `97a7a5e` (audit docs), `f53d967` (CI workflow), `f42178a` (status update), `e95bec9` (tracks previously-uncommitted tests/intel/guide/mobile-web — see finding below), `a1864b5` (companion implementation for those tests — also previously uncommitted), `67d9018` (dependency security patch).
DEPLOYMENT: not deployed — local commits only.
DATE: 2026-08-18

**Push to GitHub**
STATUS: RESOLVED — pushed successfully
EVIDENCE: This session's own sandbox still cannot push (no network / repo not authorized for this session's git proxy — confirmed, unchanged, not fixable from here). The owner pushed from their own machine as planned. `origin/master` (last confirmed via this session's local git remote-tracking ref, this session still cannot `git fetch`) is at `b201322`, matching the local repo's history exactly through that point.
RESOLUTION: no longer blocking. Future pushes still require the owner's machine or the terminal ("Claude Code") session — this session remains push-incapable by design, not by bug.
DATE: 2026-08-18

**IMPORTANT FINDING — uncommitted work discovered during CI setup**
STATUS: FIXED and pushed
EVIDENCE: When first wiring the CI workflow, `backend/__tests__/` and `ai-service/tests/` — the very test suites this audit verified as 63/63 and 105/105 passing — turned out to have **never been committed to git**. Neither had the `ai-service/app/services/intel/` subsystem, `translation_service.py`, the `guide` feature (backend controller/route + mobile screen/providers), or `mobile/web/`. Additionally, ~20 tracked files (job schedulers, `virtualTrackingService.js`, `aiWorkerService.js`, ai-service routes/config, mobile screens) had local uncommitted edits that the new tests depend on (`capToMaxRisk`, `getEdgeMultiplier`, `approveSuggestion` didn't exist in the last real commit — confirmed by first committing tests alone and watching 27 of them fail, then finding and committing the paired implementation, then re-verifying 63/63 green). All of this is now committed and pushed — no longer machine-local-only.
DATE: 2026-08-18

**GitHub Actions actually running / green**
STATUS: CONFIRMED LIVE AND GREEN (verified via GitHub's own Actions page, not assumed from local test runs)
EVIDENCE: Checked GitHub Actions directly (`github.com/RenasTalabani/ai-trading-system/actions`). The CI workflow (`ci.yml`) has run automatically on every push to `master` and passed every time, including the 3 most recent runs as of this check — commits `edc6412`, `a41d3fc`, `b201322`, each conclusion "Success". Drilled into `b201322` specifically: its Backend/Jest check individually shows passed, completed in 17s. This closes T-002 (was REVIEW pending exactly this confirmation, now DONE).
DATE: 2026-08-18

**Dependency audits**
STATUS: PASS (backend + ai-service both improved and verified)
EVIDENCE:
- Backend `npm audit`: 26 vulnerabilities (1 critical, 8 high, 14 moderate, 3 low) → 9 (all moderate) after `npm audit fix`. Critical (`websocket-driver`) and all high (`ws`, the library backing this app's own WebSocket server) are resolved. Remaining 9 moderate are all transitively behind `firebase-admin`'s pinned `uuid` version — fixing requires a breaking `firebase-admin` major bump, deferred since there's no test coverage for push notifications in this repo to safely verify against. Verified: 63/63 backend tests pass after the fix.
- ai-service `pip-audit`: 16 known vulnerabilities in 3 packages → 0 in app dependencies. `torch` was pinned `<2.9.0` (8 CVEs, several deserialization/RCE-class); bumped to `>=2.9.1,<2.14.0` (installs 2.13.0, 0 known vulnerabilities). Dockerfile now also upgrades `pip`/`setuptools` at build time to clear their advisories. Verified: 105/105 ai-service tests pass on the new torch version.
DATE: 2026-08-18

---

### PRIORITY 2 — Security — COMPLETE (2026-08-18)

**bcrypt password hashing**
STATUS: PASS
EVIDENCE: `backend/src/models/User.js` — `bcrypt.hash(this.password, 12)`. 12 salt rounds is solid (industry-typical range is 10–12).
DATE: 2026-08-18
COMMIT: — (no change needed)
REMAINING RISK: none.

**Refresh-token flow**
STATUS: FIXED (removed dead config, decision documented)
EVIDENCE: Investigated the full auth flow end to end: `User.js`/`authController.js` (single JWT, no refresh logic anywhere), mobile `auth_provider.dart` (auto-creates a local guest account on first launch; real login only reached if a session is explicitly cleared), `api_service.dart` (401 → immediate logout, no refresh attempt), `router.dart`. Conclusion: refresh-token rotation would add real complexity (secure storage, a revocation store, new endpoints) with no corresponding security need — no real-money/exchange-custody data sits behind the token, and expiry is already handled gracefully. Removed the dead `JWT_REFRESH_EXPIRES_IN` from `backend/.env.example` and documented the reasoning in a comment above `generateAuthToken()` in `User.js`, so this isn't mistaken for an oversight later.
DATE: 2026-08-18
COMMIT: `dbb81c3`
REMAINING RISK: low. Revisit only if the product ever adds real user accounts holding real financial data (noted in the code comment).

**CORS — explicit production allowlist (owner decision: no wildcard)**
STATUS: DONE
EVIDENCE: Owner reviewed the mechanism-verification pass and decided against `*` in production even given the lower risk (Bearer-token auth, no cookies). `buildCorsOptions()` (`backend/src/config/corsConfig.js`) no longer defaults unset/empty `ALLOWED_ORIGINS` to `*` — it now defaults to an empty allowlist, which denies all browser-based cross-origin requests. `*` is still supported but must be explicitly set (kept only for local dev flexibility). Investigated the actual project for real web origins before writing the production config, per owner instruction not to invent domains: confirmed there is no deployed web frontend today — `mobile/web/` is unused, never-customized `flutter create` scaffolding (generic "A new Flutter project" title, default icons, no hosting config anywhere: no `firebase.json`/`vercel.json`/`netlify.toml`, no custom domain referenced in `DEPLOYMENT.md`). `backend/.env.railway`'s `ALLOWED_ORIGINS` is therefore a documented empty placeholder with an inline example of the expected format, ready to fill in once a real origin exists. `backend/.env.example` (local dev) keeps its existing `http://localhost:3000,http://localhost:8080`. Flutter mobile is unaffected either way — it sends no `Origin` header. `cors.test.js` expanded from 8 to 14 tests, covering all four owner-required cases: allowed origin, disallowed origin, no-Origin-header, and authenticated (Bearer token) request behavior from both an allowed and a disallowed origin — plus the new empty-default-denies case and wildcard now exercised as an explicit opt-in.
DATE: 2026-08-18
COMMIT: `a164aa4`
REMAINING RISK: none for the code path (verified, fails closed by default). Operationally: if a real web frontend is ever deployed, its origin must be added to `ALLOWED_ORIGINS` in the Railway dashboard or it will be silently blocked by CORS — noted here so that's not a surprise later.

**Input validation coverage**
STATUS: RISK-BASED FIXES APPLIED (not all 24 files — by design, see rationale)
EVIDENCE: Audited all 24 previously-unvalidated route files (of 27 total; `auth.js`/`notifications.js`/`virtual.js` already had `express-validator`) against the owner's explicit risk criteria: DB writes, account/permission changes, trading/portfolio data, financial parameters, user-controlled identifiers, external API interaction. Findings:
  - **Fixed** — `priceAlerts.js` (create/delete/toggle): old handler did `parseFloat(targetPrice)` with no bound, so non-numeric/negative/zero values could be silently stored and would then never (or incorrectly) fire in the alert-checking job. Now validates `targetPrice` as a positive float, `direction` as an enum, string field lengths, and `:id` as a Mongo ObjectId.
  - **Fixed** — `brain.js` (`POST /follows`, `PATCH /follows/:id/close`, `DELETE /follows/:id`): validates `action`/`outcome` enums, `confidence` range (0–100), price fields as positive numbers, `:id` as a Mongo ObjectId.
  - **Fixed** — `guide.js` (`POST /positions/:tradeId/sell`): validates `:tradeId` as a Mongo ObjectId (previously relied entirely on Mongoose's `CastError` bubbling to the global error handler — functionally correct, since `errorHandler.js` does translate `CastError`/`ValidationError` to 400, but with no route-level guard). Also checked `VirtualTrade` for a missing per-user ownership filter (a real IDOR pattern in other apps) — confirmed the model has no `userId` field anywhere; this is a single shared paper portfolio by design, not a multi-tenant resource, consistent with `CLAUDE.md`'s "single-user personal app" framing. Not a bug, no fix needed.
  - **Reviewed, no fix needed** — `users.js` preferences: already schema-backstopped with explicit `min`/`max`/enum on every field (`User.js` preferences sub-schema), plus `runValidators: true` on the update call. `market.js` batch tickers: already whitelist-filters against `TRACKED_ASSETS` before forwarding to Binance, so unvalidated input can't reach the external call. `ai.js`/`signals.js`/`news.js` trigger endpoints: admin/premium-role-gated already, lower exposure. The ~9 remaining files (`budget`, `global`, `reports`, `simulator`, `strategy`, `tracker`, `unified`, `advisor`, `orderBlocks`) already validate in their controllers (confirmed via `grep -l express-validator` on the controllers directory), even though the route files themselves don't import it directly.
  - **Fixed in a follow-up pass (owner approved)** — `telegram.js` `POST /webhook`'s missing authenticity check is now closed; see the dedicated Telegram webhook entry below. Originally documented here as T-020 for owner decision; owner approved implementing it on 2026-08-18.
DATE: 2026-08-18
COMMIT: `1cf84e9`
REMAINING RISK: low for the fixed routes (verified by 13 new tests). Low-medium for the telegram webhook until T-020 is actioned — realistic impact is bot-abuse-as-spam-relay, not account takeover (linking a Telegram account still requires a correct, short-lived, random UUID token).

**Rate limiting / security headers / error exposure**
STATUS: PASS
EVIDENCE: `helmet()`, global `express-rate-limit` (100 req/15min default), and `errorHandler.js` only includes stack traces when `NODE_ENV=development` — confirmed by reading the actual code, not assumed. `errorHandler.js` also correctly translates Mongoose `ValidationError` → 400 (per-field messages) and `CastError` → 400, which several of the routes above rely on as a backstop.
DATE: 2026-08-18
COMMIT: — (no change needed)
REMAINING RISK: none identified.

**Telegram webhook authenticity (T-020)**
STATUS: CODE DONE — NOT YET LIVE (needs 2 manual owner steps outside this session's reach)
EVIDENCE: Implemented `X-Telegram-Bot-Api-Secret-Token` verification per Telegram's documented `setWebhook` `secret_token` mechanism. New middleware `backend/src/middleware/telegramWebhookAuth.js` compares the incoming header against `TELEGRAM_WEBHOOK_SECRET` using `crypto.timingSafeEqual` (constant-time, avoids leaking the secret via response-timing), and **fails closed**: if the server has no secret configured, every webhook call is rejected (403) rather than silently accepted, because there's nothing safe to verify against. Wired ahead of the existing `handleWebhook` controller in `routes/telegram.js`, which is otherwise unchanged. Added `TELEGRAM_WEBHOOK_SECRET` to both `.env.railway` and `.env.example` as a placeholder with generation guidance (`openssl rand -hex 32`) — no real value written, committed, or logged anywhere. 11 new tests cover all four owner-specified cases (correct secret → accepted; missing secret → rejected; incorrect secret → rejected; a well-formed Telegram-shaped payload sent without the secret → rejected) plus the fail-closed unconfigured-server case and a check that the configured secret never appears in a rejection response body.
DATE: 2026-08-18
COMMIT: `a164aa4`
REMAINING RISK: **the fix is not active in production until the owner does two things I cannot do from here** (no bot token available in this session, and I was instructed not to touch Telegram's live webhook config without it): (1) set `TELEGRAM_WEBHOOK_SECRET` to a strong random value in Railway's dashboard for the backend service — e.g. generate with `openssl rand -hex 32`; (2) call Telegram's `setWebhook` API with a matching `secret_token` param, e.g. `curl "https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/setWebhook?url=<your-backend-url>/api/v1/telegram/webhook&secret_token=<same-value-as-step-1>"`. Until both are done, real Telegram messages will stop reaching the bot the moment this code deploys — that's the fail-closed design working as intended, not a bug, but it will look like "Telegram broke" if the two steps above aren't done in the same deploy window.

**Security testing**
STATUS: PASS
EVIDENCE: No lint/type-check/build script exists for the backend (`package.json` scripts are `start`/`dev`/`test` only) — it's plain CommonJS Node with no TypeScript or ESLint configured, so there's nothing applicable to run beyond the test suite; noting this so it doesn't read as a skipped step. Full backend test suite run after all Priority 2 changes (including this follow-up pass): **101/101 passing** (84 prior + 6 new CORS tests + 11 new Telegram webhook tests). ai-service was not touched this pass (all changes are backend-only), so its 105/105 baseline stands unchanged from Priority 1.
DATE: 2026-08-18
COMMIT: `dbb81c3`, `1cf84e9`, `bac9af8`, `a164aa4`
REMAINING RISK: none — all four commits are covered by passing tests, not just written.

---

### PRIORITY 3 — Railway infrastructure audit
STATUS: AUDITED, READ-ONLY — production found DOWN on both target services, not merely unverified
EVIDENCE: Logged into Railway CLI as `renastalabani@gmail.com`. Project `distinguished-empathy` (id `bdaec7e5-29ad-4504-91aa-d58fe103c73f`) hosts this app's two services alongside two unrelated ones (`smt-catalog-api`, `distinguished-empathy` itself) and a `Postgres` service that belongs to `smt-catalog-api` (Prisma-based, not this app — this app uses MongoDB Atlas per `MONGODB_URI`). None of the unrelated services were modified.
- **Backend (`ai-trading-system` service)**: latest deployment `33bbbbef` created 2026-05-04, `status: FAILED`, `deploymentStopped: true`, 0 active instances. Deployed commit was `fa202b5` ("notification tap nav, brain countdown, exit price dialog") — **48 commits behind current HEAD** (`d12679e`), i.e. none of Priority 1 or Priority 2's work has ever reached production. `GET https://ai-trading-system-production-24d7.up.railway.app/api/v1/health` → `HTTP 404 {"message":"Application not found"}` (Railway's edge, confirming no live app behind the domain — verified directly with curl, not inferred from dashboard state).
- **AI service**: latest deployment `fcc98d20` created 2026-04-26, `status: FAILED`, reason `"Waiting for build slot"` — it appears to have never successfully built at all. `GET https://ai-service-production-0626.up.railway.app/health` → same `404 Application not found`.
- **Build/deploy logs**: unavailable for both (`railway logs --build`/`--deployment` returned empty) — deployments are 3+ months old and past Railway's log retention window. No production evidence of cron execution, DB connectivity, or model loading exists or can exist until a fresh deploy runs.
- **Environment variables** (names only, no values read into this session beyond confirming presence): backend has `MONGODB_URI`, `JWT_SECRET`, `AI_SERVICE_URL`, `ALLOWED_ORIGINS` configured — but **no `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHANNEL_ID`, `TELEGRAM_BOT_USERNAME`, Firebase, Twitter, or Reddit vars exist at all**. ai-service has `MONGODB_URI`, `MODEL_PATH`, `BINANCE_BASE_URL`, `CONFIDENCE_THRESHOLD` etc. configured, same absence of collector/notification keys. This matches "degrades gracefully" for optional features but confirms Telegram is not just "not activated," it's not configured at all yet.
- **Model artifacts**: `ai-service/saved_models/` exists locally (gitignored, ~13MB of `.joblib`/`.pt` files, newest dated 2026-08-15) but is **not in git and not copied by the Dockerfile** (`Dockerfile` only does `mkdir -p saved_models` — an empty directory). No Railway volume is attached to the ai-service service (only `Postgres`, for the unrelated `smt-catalog-api`, has one). Conclusion: **if ai-service were redeployed as-is today, it would boot with zero trained models** — `trainer.py`'s output has no path from this laptop into production. This needs a decision (bake models into the image, attach a volume and upload them, or add a fetch-on-boot step) before Priority 3 can call AI predictions "verified," not just "code deployed."
- **Cron jobs**: 17 jobs registered in `backend/server.js` (`marketDataJob`, `signalJob`, `newsJob`, `socialJob`, `notificationRetryJob`, `virtualTrackingJob`, `dailyReportJob`, `weeklyReportJob`, `globalScanJob`, `aiWorkerJob`, `hourlyReportJob`, `trackerEvalJob`, `keepAliveJob`, `priceAlertJob`, `decisionTrackingJob`, `aiDecisionJob`, `performanceAnalysisJob`, `dcaJob` — confirmed from source, matches the owner's "~16"). **Zero production evidence possible** for any of them — nothing has run since May.
DATE: 2026-08-18
COMMIT: n/a (read-only infrastructure audit, no code changed)
REMAINING RISK: Production is fully down, not degraded — this is a bigger gap than "unverified," and redeploying is a production-infrastructure action requiring explicit owner sign-off per this file's own Deployment Rules (not taken in this pass). The model-artifact delivery path is undefined and must be decided before a redeploy is assumed to make AI predictions actually work. Do not deploy, and do not treat CI green as production-ready — they are unrelated facts right now.

### PRIORITY 6 (partial) — `ai-service-err.log` triage (T-006)
STATUS: DONE
EVIDENCE: Read the full 4828-line log. It's a single local dev session (`uvicorn --reload`, machine path `C:\Users\Karwan Store\...` — a different machine/user than this one, not a Railway log), spanning 2026-04-27 21:07 to 2026-04-28 23:45 (~26h). Breakdown: 2 `[WARNING]` lines (both transient Reddit subreddit timeouts — normal network blips, not a code issue), and every error-level line reduces to **one single recurring bug**, repeated 70 times with an identical signature: FastAPI's `jsonable_encoder` fails while serializing an endpoint's response — `dict(obj)` raises `ValueError: dictionary update sequence element #0 has length 13; 2 is required`, its fallback `vars(obj)` then raises `TypeError: vars() argument must have __dict__ attribute`, and FastAPI surfaces both as a combined `ValueError`. This pattern (`dict()` on an iterable of length-13 items) is the classic signature of a pandas `Series`/row or similar tabular object being nested in a response instead of converted to a plain dict — but the traceback frames available stop at Starlette/FastAPI internals and don't show which endpoint's handler produced the object, and the log predates 48 commits of substantial AI/ML rework (multi-timeframe gating, per-asset Transformers, regime wiring) that plausibly already touched the responsible code path. Checked current `app/api/routes.py`: every route handler I found builds an explicit plain `dict` for its response (including the two `candles.iloc[-1]` usages in `/indicators/{asset}` and `/regime/{asset}`, which already convert each field via `round(float(...))` before returning — not the cause). No route currently returns a raw DataFrame/Series. Filed as **T-021** (Low priority) rather than guessing at a fix — needs live reproduction first.
DATE: 2026-08-18
COMMIT: `[pending]` (docs only)
REMAINING RISK: low — this is a 3.5-month-old single-session dev log, not a current production signal (production has no logs at all right now — see Priority 3). If it recurs during local dev or after a future Railway redeploy, T-021 has enough evidence to start from immediately (exact error signature, both suspect-but-cleared endpoints, dict/vars() fallback mechanism explained).

### PRIORITY 6 (partial 2) — API documentation (T-007)
STATUS: DONE
EVIDENCE: Read all 27 `backend/src/routes/*.js` files in full (not skimmed), plus `middleware/auth.js`, `middleware/validate.js`, `middleware/errorHandler.js` to document the auth/validation/error conventions precisely rather than guessing. Wrote `backend/API.md` — every one of the ~110 endpoints across the 27 route groups has method, full path, auth requirement (🔒 JWT / 🔐 role-gate / webhook-secret / public), and request validation rules where they exist (many routes, especially `virtual.js` and `brain.js`, have explicit inline `express-validator` chains that give exact field constraints). Response body schemas are intentionally *not* fully enumerated field-by-field for every route (would require reading every controller + Mongoose model to matching depth) — documented as a known scope limit with a path to close it incrementally. Three pre-existing design overlaps surfaced during the read-through and documented rather than silently changed: (1) three separate simulation endpoints (`/virtual/*`, `/strategy/simulate`, `/simulator/run` + `/core/simulator`) that could be consolidated later; (2) `VirtualTrade` has no per-user ownership filter — confirmed intentional (single shared paper portfolio, not multi-tenant) via the model, not assumed.
DATE: 2026-08-18
COMMIT: `[pending]`
REMAINING RISK: low. This is now a maintained reference, not a generated spec — the biggest risk is docs drifting from code on future route changes; `API.md`'s own header says to update it in the same commit as any route change. Full response-schema coverage remains a nice-to-have follow-up, not filed as a separate ticket since it's low-value without a concrete consumer (no public API clients besides the first-party Flutter app) — revisit if that changes.

### PRIORITY 5 (started) — Flutter test coverage (T-012)
STATUS: IN_PROGRESS — first real model-layer coverage added, 0% → meaningful, not yet comprehensive
EVIDENCE: `mobile/test/` previously had exactly one test: a literal `expect(true, isTrue)` placeholder. Added 46 real unit tests across 3 of the ~10 files in `mobile/lib/core/models/`: `signal_model_test.dart` (18 tests — `SignalPrice`/`SignalSources`/`SignalModel`, covering `fromJson` parsing, null/missing-field defaults, and derived getters `riskRewardRatio`/`confidenceBar`/`baseAsset`/`isBuy`/`isSell`), `virtual_portfolio_model_test.dart` (18 tests — `BalancePoint`/`TradeSnapshot`/`VirtualPerformanceModel`/`VirtualTradeModel`, covering nested-object parsing, `isProfitable`/`returnPct`/`durationLabel`/`isLiquidated`/`isFutures`), `user_model_test.dart` (10 tests — `UserPreferences`/`UserModel`, covering `fromJson`/`toJson` round-trip, `copyWith`, `isAdmin`/`isPremium`). `flutter test` — 47/47 passing (verified, not assumed).
**Found and fixed one real bug via this work**: `UserPreferences.fromJson({})` returned `assets: []`, silently contradicting the constructor's own documented default of `['BTCUSDT', 'ETHUSDT']` shown three lines above it in the same file — `fromJson` built an explicit named-parameter value that bypassed the constructor default entirely. A user with no saved `preferences.assets` would have landed on an empty watchlist instead of the intended BTC+ETH starter set. Fixed in `mobile/lib/core/models/user_model.dart` (one added conditional), confirmed no other call site in `mobile/lib/` reads `preferences.assets` in a way the fix could break (`grep` confirmed zero other references). Test added specifically to catch this regressing again.
Investigated but deliberately did **not** change: `SignalModel.baseAsset`'s slash-branch is effectively dead for symbols that also end in `USDT`/`USD` (e.g. `'BTC/USDT'` hits the earlier `endsWith` check first and returns `'BTC/'`, not `'BTC'`) — no evidence this format is ever actually produced by the backend (`TRACKED_ASSETS` uses concatenated Binance-style symbols, no slash), so this is speculative dead-code behavior, not a confirmed bug; documented in the test comment rather than "fixed" on a guess. Also checked whether `durationMinutes`/`avgDurationMinutes`'s `as int` casts in `virtual_portfolio_model.dart` were a latent crash risk (JSON numbers with a fractional part would throw on that cast) — confirmed safe by reading `backend/src/services/virtualTrackingService.js:417,557,644`, which always `Math.round()`s these before sending, so left unchanged (not broken, no fix needed).
DATE: 2026-08-18
COMMIT: `b201322`
REMAINING RISK: medium-low. Coverage is real but partial — see round 2 below for the update.

**Round 2 (same session, later pass)**: added 4 more model test files — `pnl_model_test.dart` (4 tests), `exposure_model_test.dart` (6 tests), `dca_model_test.dart` (6 tests), `order_block_model_test.dart` (13 tests) — bringing coverage to 7 of ~10 files in `mobile/lib/core/models/`. `flutter test test/core/models/` — 74/74 passing. Noticed and documented (not "fixed" — it's an intentional, consistent pattern once traced) that `PnLModel` and `OrderBlockResult` use strict, non-defaulting `as num`/`as String` casts unlike the more defensive `?? 0` style in `SignalModel`/`VirtualTradeModel` — confirmed both are consumed via Riverpod `AsyncNotifier`s (`pnl_provider.dart`, `order_block_provider.dart`), so a thrown parse error becomes a normal `AsyncValue.error` UI state, not a crash. This is a real, deliberate two-tier convention in the codebase (strict-parse-plus-framework-error-handling for single-item detail views vs. defensive-defaulting for bulk list views) — documented via test comments so a future pass doesn't "fix" the strict models into silently swallowing bad data.
DATE: 2026-08-18 (round 2)
COMMIT: `6d18b6e`
REMAINING RISK: medium-low. See round 3 below.

**Round 3 (same session)**: added the last 3 model files — `backtest_model_test.dart` (4 tests), `notification_model_test.dart` (6 tests), `strategy_model_test.dart` (13 tests). **All 10 of `mobile/lib/core/models/` now have real test coverage.** `flutter test test/core/models/` — 97/97 passing. Found (via `strategy_model.dart`'s `AssetRecommendation.fromJson`) and tested a real, deliberate wire-format compatibility shim: `expected_move_percent`/`current_price` fall back to `expectedMove`/`currentPrice` if the snake_case key is absent, with snake_case taking precedence when both exist — this dual-format tolerance is now regression-tested, not just implicit.
DATE: 2026-08-18 (round 3)
COMMIT: `f138326`
REMAINING RISK: medium-low. Model layer fully covered as of this round — see round 4 below for the provider-layer follow-up.

**Round 4 (2026-08-19/20, after an overnight session gap)**: started the provider slice flagged as the next step in round 3. `mobile/lib/core/providers/` has no dependency-injection point for its HTTP client (`ApiService.dio` is a `static final Dio` with interceptors baked in at construction) — properly mocking full network round-trips per-provider would mean either hand-writing a custom `HttpClientAdapter` or adding a new dev dependency (e.g. `http_mock_adapter`), neither undertaken yet as a deliberate scoping choice for this pass. Instead covered the substantial pure logic that doesn't need network mocking: `signals_provider_test.dart` (7 tests — `SignalsState.copyWith`, including a documented-not-fixed quirk where `copyWith()` without an explicit `error:` argument always clears any existing error, since the field uses `error: error` rather than the `?? this.error` pattern every other field uses — confirmed intentional for both of the provider's actual call sites, not changed; and `assetSignalProvider`, the family-provider derived-selector logic, tested via `ProviderContainer` overrides with a `SignalsNotifier` subclass whose `fetch()` is overridden to a no-op so construction doesn't trigger a real network call), `virtual_portfolio_provider_test.dart` (5 tests — `performanceRangeProvider` default/update, `VirtualTradesState` defaults/`copyWith`), and `api_service_test.dart` (8 tests — the `DioErrorMessage.userMessage` extension's full branch coverage: Map-body-with-message, Map-body-without-message, timeout/connection-error-specific copy, non-Map response body doesn't crash). `flutter test` — **118/118 passing** (117 real tests + 1 pre-existing placeholder).
DATE: 2026-08-19/20 (round 4)
COMMIT: `4994aff`
REMAINING RISK: medium. Provider coverage is non-zero but still thin relative to ~28 provider files, network-mocking scoping decision still open. See round 5 below.

**Round 5 (2026-08-20)**: first widget test. `mobile/test/features/dashboard/widgets/signal_card_test.dart` (8 tests) covers `SignalCard` — a self-contained `StatelessWidget` with no Riverpod dependency (takes a `SignalModel` directly), making it the natural first target. Covers rendering of asset/direction/confidence, `compact` mode correctly hiding the price/sources rows, conditional stop-loss/take-profit tile rendering when either is null, and — the part worth calling out — real `go_router` navigation: tapping the card is verified to actually push `/signals/:id` using a real `GoRouter` with both routes registered, not just a tap-doesn't-crash smoke test. This establishes the pattern (`MaterialApp`/`MaterialApp.router` wrapping, no golden-image comparisons, no network/font dependencies to cause flakiness) for testing the other ~2 reusable widgets and eventually full screens. `flutter test` — **126/126 passing** (125 real + 1 pre-existing placeholder).
DATE: 2026-08-20 (round 5)
COMMIT: `d920a42`
REMAINING RISK: medium. See round 6 below.

**Round 6 (2026-08-20)**: `mobile/test/core/widgets/sparkline_chart_test.dart` (6 tests) covers `SparklineChart` (`fl_chart`-based). All four render-nothing branches tested explicitly (loading, provider error, <2 points, zero-range/flat data — the last one guards against a degenerate chart with `minY == maxY`), plus a positive case confirming a `LineChart` renders for varying data, and that custom `width`/`height` are respected. Used `sparklineProvider('BTCUSDT').overrideWith(...)` — clean because it's a plain `FutureProvider.family`, no notifier class to work around.
Deliberately **not** attempted this round: `price_ticker.dart` (`PriceTickerBar`). Its `pricesProvider` is a `StateNotifierProvider<PricesNotifier, PriceMap>` whose network+WebSocket initialization (`_init`/`_fetchRest`/`_listenWs`) is entirely in **private** methods — Dart's library-scoped privacy means a test file can't subclass and override them the way `signals_provider_test.dart`'s round-4 `_FixedSignalsNotifier` trick worked for `SignalsNotifier.fetch()` (which is public). Testing `PriceTickerBar` properly needs one of: (a) a small production-code change making those methods overridable/injectable (real refactor, not just adding tests — flagged here rather than done silently), (b) mocking at the `ApiService`/`WebSocketService` level instead of the notifier level, or (c) an equivalent no-op-constructor pattern reworked to not rely on overriding private methods. Not a defect in the app — just a testability gap, and the kind of thing worth a deliberate small refactor rather than a workaround. `flutter test` — **132/132 passing** (131 real + 1 pre-existing placeholder).
DATE: 2026-08-20 (round 6)
COMMIT: `[pending]`
REMAINING RISK: medium-low. 2 of 3 reusable widgets covered; `PriceTickerBar` blocked on a testability decision (see above), not attempted. 0 full-screen widget tests remain — the largest still-open slice of T-012.

### PRIORITY 1 (continuous) — ai-service CORS misconfiguration (T-022)
STATUS: DONE
EVIDENCE: Found during a PM-directed continuous-improvement pass (security is priority #1 per owner's standing instruction). `ai-service/app/main.py`'s `CORSMiddleware` was configured with `allow_origins=["*"]` and `allow_credentials=True` simultaneously — the same anti-pattern the backend's `ALLOWED_ORIGINS` hardening addressed earlier (2026-08-18): wildcard + credentials either gets rejected outright by browsers or gets silently made spec-compliant by echoing back the request `Origin` instead of a literal `*`, which is more permissive than the config reads at a glance. Confirmed via `grep` that this service has zero cookie-based auth anywhere — it's called exclusively server-to-server by the backend (`aiService.js`, `aiWorkerService.js`, `socialService.js`) — so `allow_credentials=True` was dead, incorrect config, not a deliberate choice (unlike the backend's original wildcard, which had a documented rationale). Fixed to `allow_credentials=False`. Added `ai-service/tests/test_cors_config.py` (3 tests: middleware is registered, the dangerous combination is never present, and the fixed state is pinned against regression). Verified in a clean cloud venv (torch 2.13.0, fastapi 0.141.1): full ai-service suite **108/108 passing** (105 prior + 3 new).
Also attempted a live reproduction of the T-021 `jsonable_encoder` crash via `social_analyzer.refresh()` directly in the same venv — did not reproduce, though inconclusively (no real collector credentials available, FinBERT unreachable so VADER fallback engaged). Added as supplementary evidence to T-021's TASKS.md entry; T-021 itself stays TODO, not resolved by this note.
DATE: 2026-08-18
COMMIT: `25d25e3`
REMAINING RISK: none identified for this fix. General note: ai-service's CORS posture (origin still `*`, just no longer credentialed) has not been hardened to an explicit allowlist the way the backend's was — deliberately left as-is since ai-service has no browser clients at all (internal service only) and no owner decision has been requested for this scope; revisit only if that assumption changes (e.g. a future direct-browser integration).

### PRIORITY 5 (continuous) — backend<->ai-service contract tests (T-013)
STATUS: DONE
EVIDENCE: `backend/src/services/aiService.js` (the primary HTTP client for every AI call the backend makes: predictions, news/social sentiment, funding rates, prices, training triggers) and `socialService.js`'s two ai-service-calling functions had zero test coverage -- despite every function on this path silently swallowing errors and returning a fallback (null / {} / 'unreachable') instead of throwing. Added 22 tests across `backend/__tests__/aiService.test.js` (17) and `backend/__tests__/socialService.aiClient.test.js` (5): each function verified for the exact HTTP method/path/body sent (cross-checked against ai-service's live route table in `ai-service/app/api/routes.py`, mounted under `/api` -- /api/predict, /api/news/analyze, /api/social/analyze, /api/status, /api/train, /api/macro/funding-rates, /api/prices/{asset}, /api/global/scan, /api/social/analysis, /api/social/alerts all confirmed to exist server-side, no drift found) and that a network failure degrades to the documented fallback rather than throwing. Includes a regression guard confirming `getPrice()` uses `??` not `||` so a legitimate price of exactly 0 isn't coerced to null.
DATE: 2026-08-18
COMMIT: `102c5ef`
REMAINING RISK: low. This is contract-level (mocked), not a live end-to-end call between the two real services -- if ai-service ever changes a response *shape* (not just the route existing) without the backend adapting, these tests wouldn't catch that. A genuine live-process integration test (both services actually running, real HTTP between them) would close that gap but needs network (unavailable in this session's device sandbox) and is a heavier lift; not attempted this pass. `aiWorkerService.test.js`'s existing coverage of the `/api/global/scan` call was left as-is (already covered, different file).

### PRIORITY 3 (continuous) — data-integrity race in the paper-trading balance (T-023)
STATUS: DONE
EVIDENCE: `virtualTrackingJob.js` registers two independent cron schedules in the same process -- `runVirtualTrackingCycle` every 5 minutes and `runFundingCycle` three times a day at 00:00/08:00/16:00 UTC. Those two schedules land on the exact same tick at 00:00, 08:00, and 16:00 UTC, so both fire concurrently. Both eventually call `virtualTrackingService.js` functions (`checkOpenTrades`, `applyFundingPayments`, and separately `closePositionNow` for the manual "Sell Now" HTTP route) that do a plain fetch -> in-memory mutate -> `portfolio.save()` on the single shared `VirtualPortfolio` document (`currentBalance`, `totalProfit`, `totalLoss`, `balanceHistory`), with zero coordination between them -- a classic lost-update race: whichever `save()` lands second silently overwrites the first's changes. Not hypothetical -- demonstrable given the confirmed-overlapping cron schedules (found by reading the actual cron registrations, not assumed).
Fixed with a minimal in-process async mutex (`backend/src/utils/asyncMutex.js`) and a single shared portfolio lock built on it (`backend/src/utils/portfolioLock.js`), wrapping the three balance-mutating critical sections in `virtualTrackingService.js`. An in-process mutex (not a DB-level lock) is the correct fix for this app's actual deployment model -- single Node process / single Railway service instance per `CLAUDE.md`, not horizontally scaled; documented in the code so this is revisited if that ever changes. 3 new tests (`backend/__tests__/asyncMutex.test.js`) prove real serialization (not just a final-count check -- asserts the actual execution order) and that a rejected critical section doesn't wedge the queue for work behind it. All 32 pre-existing `virtualTrackingService.test.js` tests plus the rest of the backend suite re-verified green after wiring the lock in.
DATE: 2026-08-18
COMMIT: `99c16e3`
REMAINING RISK: low for the current deployment model. If this app is ever scaled to multiple Node instances (not the case today), an in-process mutex stops being sufficient and balance mutations would need to move to atomic MongoDB `$inc` operations or a real distributed lock -- flagged in the code comment so it isn't silently forgotten.
FOLLOW-UP (2026-08-18, same pass): added `_trackingRunning`/`_fundingRunning` self-overlap guards to `virtualTrackingJob.js` (mirroring `aiWorkerJob.js`'s existing `_running` pattern), closing the pileup concern noted above -- a slow cycle now skips its next tick instead of queuing indefinitely behind the lock. No behavior change under normal conditions. Commit `b18fe4f`.

### PRIORITY 7 (continuous) — silent job failure in notificationRetryJob (T-024)
STATUS: DONE
EVIDENCE: Auditing error-handling across all 17 registered cron jobs (prompted by the T-023 race-condition work) found `notificationRetryJob.js` was the only one with zero try/catch around its cron callback -- every other job wraps its body and logs failures via `logger.error`. Investigated the actual failure mode by reading node-cron 3.0.3's own source (`node_modules/node-cron/src/task.js`): async task rejections are caught internally by node-cron itself and turned into a `'task-failed'` event, so this was never a process-crashing risk -- but nothing in this codebase listens for that event, so any error here (a bad `Notification` document, a Firebase/Telegram send throwing, a query failure) was silently swallowed with zero trace in the logs, unlike every other job. That makes a real notification-delivery outage undiagnosable from logs alone.
Fixed by wrapping the existing function body in try/catch with the same `logger.error` pattern every other job already uses -- no logic change, confirmed via `git diff` that only the wrapper was added. Confirmed via grep that nothing else calls the function directly. No existing job-level test coverage exists for any of the 17 jobs (this matches that pre-existing pattern, not a new gap); `virtualTrackingService.test.js` (32/32, exercises adjacent notification-sending code paths) re-verified green.
DATE: 2026-08-18
COMMIT: `f36e45f`
REMAINING RISK: low. This closes the visibility gap for this one job. A broader follow-up (not done this pass, lower priority) would be registering a listener for node-cron's `'task-failed'` event globally across all jobs as a defense-in-depth backstop, in case a future job is added without its own try/catch -- noted here as a possible small hardening item, not filed as a separate ticket since every current job already self-protects.

### PRIORITY 4 (continuous) — AI worker could exceed its own risk cap (T-025)
STATUS: DONE
EVIDENCE: Reading `aiWorkerService.js`'s core cycle in full (prompted by the T-023/T-024 reliability audits) found `runAIWorkerCycle()`'s per-opportunity trade-opening loop was only bounded by `MAX_NEW_PER_CYCLE` (default 3) -- it never checked that new count against `MAX_OPEN_TRADES` (default 5) as it went. The top-of-function check (`openCount >= MAX_OPEN_TRADES -> skip the whole cycle`) only gated whether the cycle ran at all, not how many trades it could add once running. Concretely: a cycle starting at 4 open trades (under the cap, so allowed to run) with 3 qualifying opportunities in one scan could open all 3, reaching 7 open trades -- 2 over the portfolio's own declared risk cap. This was never caught because every existing test's `VirtualTrade.countDocuments` mock defaulted to 0, so the boundary case (already-near-the-cap + multiple qualifying opportunities) was never exercised.
Fixed with a single added guard inside the loop: `if (openCount + tradesCreated >= MAX_OPEN_TRADES) break;`. 2 new tests added to `aiWorkerService.test.js`: one reproduces the exact scenario above and confirms only 1 trade opens (not 3), the other confirms `MAX_NEW_PER_CYCLE` still independently caps a cycle when there's plenty of room under `MAX_OPEN_TRADES`, so the two limits compose correctly rather than one silently disabling the other. Full `aiWorkerService.test.js` (8/8) and `virtualTrackingService.test.js` (32/32) re-verified green.
DATE: 2026-08-18
COMMIT: `051be27`
REMAINING RISK: none identified for this specific cap. This was found by reading the core AI-decision cycle end to end looking for correctness issues, the same way T-023 was found -- worth continuing that kind of close read of the remaining trading-logic files (`pickupNewSignals`, `computeSpotSizeUsd`, `getEdgeMultiplier`) as a standing practice rather than a one-time pass, since this is exactly the class of bug that's easy to miss in review but has real capital-at-risk consequences (even in paper trading, it defeats the purpose of the risk cap the product advertises).

### PRIORITY 4 (found, not actioned) — pickupNewSignals() is dead code with a latent risk-cap gap (T-026)
STATUS: TODO — documented, not fixed, pending an owner call on intent
EVIDENCE: While reading the trading-logic files end to end after T-025, found `pickupNewSignals()` (`virtualTrackingService.js`) is fully implemented, exported, and unit-tested, but has **zero live call sites** -- confirmed via grep across `backend/src/routes`, `backend/src/controllers`, `backend/src/jobs`, and `backend/server.js`. No cron job schedules it, no route triggers it. It appears to be superseded by `aiWorkerService.js`'s `runAIWorkerCycle` (which has its own signal-scanning + trade-opening logic, now risk-capped per T-025) but was never removed.
Separately: if this function were ever wired up as-is, it would have an even larger version of the T-025 gap -- it opens one `VirtualTrade` per new signal with **no check against `MAX_OPEN_TRADES` (or any cap) at all**, processing up to 50 signals in a single call via `insertMany`. Not fixing this now: it doesn't run today, so there's no live risk, and deciding whether to remove it, wire it back up (with a T-025-style cap added first), or leave it as intentionally-reserved code is a product/architecture call, not a pure bug fix -- flagging for the owner rather than guessing.
DATE: 2026-08-18
COMMIT: n/a (documentation only, no code changed)
REMAINING RISK: none today (dead code). Becomes a real risk only if someone wires this back into a job/route without also adding the missing cap -- this entry exists so that doesn't happen silently.

### PRIORITY 6 (continuous) — stale live-price data could silently drive trade closures (T-027)
STATUS: DONE
EVIDENCE: While reviewing market-data reliability, found `binanceService.js`'s REST price-poll fallback is only ever started once -- 30 seconds after server boot, and only if the WebSocket price cache was still empty at that point (`server.js`). If the WebSocket connects successfully at startup and then drops for an extended period later at runtime (network blip, a Binance-side issue, a geo-block that starts mid-session), nothing restarts REST polling: `getAllCachedPrices()` keeps returning whatever price last arrived, indefinitely, with no way for callers to tell fresh from stale. `checkOpenTrades()` runs every 5 minutes and uses exactly this cache to decide TP/SL/liquidation for every open trade -- a stale price could silently fail to trigger a real close, or trigger one against a price that no longer reflects the market.
Fixed by adding a `PRICE_STALENESS_MS` threshold (10 minutes) and a check in `checkOpenTrades`'s per-trade loop: a cached price older than that is now treated exactly like "no price at all," which the function already handled safely (skip that asset for the cycle, no crash, no bad action). Only applies to the `{price, ts}` object shape production code actually sends; the bare-number shorthand every pre-existing test in the file uses has no timestamp to check and is treated as fresh, so no existing test needed to change. 2 new tests: a 20-minute-old cached price is confirmed to never close a trade even though the price itself would clearly trigger the take-profit; a fresh-timestamp price is confirmed to still close trades normally. Full `virtualTrackingService.test.js` (34/34) and `aiWorkerService.test.js` (8/8) green.
DATE: 2026-08-18
COMMIT: `5931553`
REMAINING RISK: low for the fix itself. The underlying gap it works around -- REST polling never auto-restarting after a runtime WebSocket outage -- is still there; this fix prevents that gap from corrupting trade decisions, but doesn't close the gap itself (the price cache would just stay empty/stale-and-ignored rather than actively refreshing). A fuller fix would make the REST fallback an ongoing periodic health check rather than a one-time startup check -- bigger behavioral change to the live-stream orchestration, not attempted this pass since it needs real network access to test meaningfully (this session's sandbox has none) and Railway is currently down anyway (see Priority 3), so there's no live instance to observe this against right now. Worth a follow-up once Railway is funded and redeployed.

### PRIORITY 5 (continuous) — crypto scoring always used a hardcoded regime, biasing every trade's SL/TP and score (T-028)
STATUS: DONE
EVIDENCE: While auditing the AI-reliability tier (`ai-service/app/services/global_analyzer.py`, the engine behind `/api/global/scan`), found `_score_crypto()` derived its market regime via `result.get("regime", "TRENDING")` -- but `UnifiedAnalyzer.analyze()`'s return dict never contains a "regime" key anywhere (confirmed by reading the full function; grepped the whole file for the string with zero hits besides the `.get()` call site itself). That means every crypto asset, on every single scan, silently fell back to the "TRENDING" default: TRENDING's SL/TP width (1.5x/3.0x ATR, vs. e.g. 2.0x/4.0x for VOLATILE or 1.2x/2.4x for DOWNTREND) and TRENDING's score modifier (a permanent +10% boost to BUY signals, -20% penalty to SELL signals) applied regardless of whether the market was actually trending, ranging sideways, in a downtrend, or highly volatile. Crypto is this app's primary, most heavily-scanned asset class (6 tracked assets scanned every cycle via `aiWorkerJob`). The parallel non-crypto path, `_score_multi_asset()`, was unaffected -- it already called `RegimeDetector.detect(df)` correctly with real OHLCV data.
Fixed by adding `RegimeDetector.detect_from_values(price, ema50, ema200, atr)` -- a scalar-input sibling to the existing DataFrame-based `detect()`, which now simply extracts its row and delegates to it (verified identical behavior via a dedicated test, no regression to the multi-asset path). `_score_crypto` now calls this with the ema50/ema200/atr/current_price it already receives from the order-block engine's response, avoiding any second network fetch. 15 new tests added: 11 for `RegimeDetector` (all four regime classifications, the detect()/detect_from_values() equivalence, fallback-on-bad-input, and `regime_score_modifier`'s per-regime multipliers), 4 for `_score_crypto`'s regime wiring specifically (a downtrend-shaped technical snapshot now correctly yields "DOWNTREND" not "TRENDING"; an uptrend snapshot yields "TRENDING"; the resulting stop_loss/take_profit values are pinned to the DOWNTREND-correct numbers, distinct from what the old always-TRENDING bug would have produced; missing price data still falls back safely to TRENDING without crashing). Verified in a clean venv: full ai-service suite 120/120 passing (105 prior + 15 new), zero regressions.
Also audited during this pass, found NOT to be a live defect: `RiskManager.compute_position_size()`'s Kelly-lite formula (`risk_pct / atr_normalized`) is, for any realistic ATR (well under ~20% of price), always dominated by its own `MAX_POS_CAP` (10%) ceiling -- so the "ATR-based dynamic position sizing" the module's docstring describes effectively never varies in practice. However, grepped the full backend and confirmed `aiWorkerService.js` never reads this field for real order sizing -- it computes its own `sizeUsd` independently from `portfolio.currentBalance * riskPerTradePct`. So this is a misleading informational number surfaced on the Global Scan display (relevant only if a human manually sizes a trade off it via `approveSuggestion`), not an automated-trading risk defect. Not actioned this pass -- doesn't meet the same demonstrable-live-consequence bar as T-023/T-025/T-027/this task, and the "correct" fix would mean changing financial risk-policy constants (RISK_PCT, MAX_POS_CAP) that are the owner's call, not a clear code defect. Documented here for visibility; no TASKS.md row opened for it pending owner interest.
DATE: 2026-08-18
COMMIT: `f37ea87`
REMAINING RISK: low. The fix only changes which regime is *detected* for crypto -- it doesn't change the regime-classification logic itself (unchanged, reused as-is from the already-live multi-asset path), and doesn't change compute_sl_tp/regime_score_modifier's math at all, just what regime value they're called with. One caveat carried over unchanged from before this fix: ATR for crypto still comes from `unified_analyzer.py`'s fallback estimate (`current_price * 1.5%`), since the order-block engine's response has no real `atr` field either -- so the VOLATILE classification (ATR/price > 2.5%) can still never trigger for crypto specifically (that constant fallback is always 1.5%, under the threshold). TRENDING vs. DOWNTREND vs. SIDEWAYS now correctly reflect real ema50/ema200 structure, which is the majority of the value here, but true volatility-regime detection for crypto would need a follow-up to plumb real ATR through from the order-block/strategy engines -- not attempted this pass to keep the fix narrowly scoped to the specific, confirmed defect.

### PRIORITY 8 (continuous) — Price Alerts silently never fired for any user (T-029)
STATUS: DONE
EVIDENCE: While auditing backend job reliability (all 18 cron jobs -- confirmed every one now has try/catch after T-024), found `priceAlertJob.js`'s `checkAlerts()` reads prices via `binanceService.getAllCachedPrices()`, which returns `{ asset: { price, ts } }` (confirmed directly from `binanceService.js` -- `priceCache.set()` always stores that object shape, both in the WebSocket handler and the REST-poll fallback). But `checkAlerts()` was comparing that whole object directly against `alert.targetPrice` (`currentPrice >= alert.targetPrice`, where `currentPrice` was the `{price, ts}` object, not a number). Comparing an object to a number coerces the object to `"[object Object]"`, which is never `>=` or `<=` any number -- so `triggered` was always `false`. This job had zero prior test coverage, which is exactly why a complete, silent failure of an entire user-facing feature (Price Alerts -- the job's whole purpose) went unnoticed: no error was ever thrown, nothing was ever logged as wrong, alerts simply never fired for any user, on any asset, ever, since this job existed.
Fixed by extracting the numeric `.price` field from the cached entry (with a defensive fallback that still works if a bare number is ever passed instead). 5 new tests added covering: "above" trigger against the real object shape (the core regression test), "below" trigger, no-trigger-when-price-hasn't-crossed, an asset missing from the cache being skipped safely, and the bare-number fallback shape. Full backend suite re-verified 135/135 green (130 pre-existing + 5 new), zero regressions.
DATE: 2026-08-18
COMMIT: `5f31612`
REMAINING RISK: low. This is a narrowly-scoped extraction fix with no change to trigger logic, DB writes, or notification content. One thing worth flagging for the owner separately (not fixed here, out of scope): `checkAlerts()` does not apply the same price-staleness guard added in T-027 for `checkOpenTrades()` -- a stale cached price could still fire (or fail to fire) an alert based on old data. Lower severity than T-027 since price alerts are informational (no money/position at stake, unlike TP/SL/liquidation), but worth a follow-up pass to reuse the same staleness pattern here for consistency.

### PRIORITY 7 — env template consolidation (T-016)
STATUS: DONE
EVIDENCE: Ground-truthed both `backend/.env.example` and `backend/.env.railway` against `grep -rhoE "process\.env\.[A-Z_]+" backend/src backend/server.js` rather than eyeballing diffs. Kept both files (dev vs Railway-prod are genuinely different environments, not redundant) but fixed real drift: removed 4 vars from `.env.example` that backend never reads (`BINANCE_API_KEY`, `BINANCE_SECRET_KEY`, `TWITTER_BEARER_TOKEN`, `REDDIT_*`) — confirmed via `ai-service/app/config.py` that these belong to ai-service's own `Settings` class, mistakenly copy-pasted into backend's template at some point. Added 5 real, currently-undocumented backend vars to both files (`BINANCE_WS_URL`, `OPENAI_API_KEY`, `AI_MIN_FUSED_SCORE`, `AI_MIN_QUALITY_SCORE`, `AI_MAX_DAILY_LOSS_PCT`) — all optional with safe code-level fallbacks, none newly required. Corrected `.env.railway`'s AI-tuning example values (were 60/10, code's actual fallback defaults are 70/5 as of a "Phase 18" change) — documentation-only fix, zero runtime effect since `dotenv.config()` only ever loads `.env`, never these template files.
DATE: 2026-08-18
COMMIT: `c3fec87`
REMAINING RISK: none — purely documentation, verified `npm test` unaffected (135/135; the 2 failures observed at the same moment were from an unrelated, separate in-progress edit to `priceAlertJob.js`, confirmed via `git diff` before concluding that, not assumed).

### PRIORITY 6 (partial 3) — MongoDB index review (T-014)
STATUS: DONE, as a static review — the task's original verification method ("query plans reviewed against real usage") needs a live database, unavailable while Railway/production is down (see Priority 3). Did the evidence-based version that's actually possible right now: cross-referenced every existing index against real query call sites in source, not guessed.
EVIDENCE: Listed every `.index()`/`index:true` declaration across all 20 models (`grep`), then traced actual `.find()`/`.findOne()`/`.aggregate()`/`.countDocuments()` calls against the three collections the task names as "hot" (signals, market data, virtual trades) across every controller/service/job/route that touches them.
- **MarketData**: single call site (`marketController.getMarketData`) does `find({asset, interval}).sort({timestamp:-1})` — exactly matches the existing `{asset:1, interval:1, timestamp:-1}` compound index. No gap.
- **VirtualTrade**: dominant pattern is `find`/`countDocuments`/`findOne` on bare `{status:'open'}` (11+ call sites — `checkOpenTrades` runs this every 5 min, plus `aiWorkerService`, `aiBrainController`, `budgetController`, `guideController`, `pnlService`). Well-served by the existing `{status:1, openedAt:-1}` index (status as the leading/prefix field). A few call sites add extra filters on top (`source`, `productType`) that aren't in that index, but since `status:'open'` alone already narrows to a tiny result set (hard-capped at `MAX_OPEN_TRADES=5`, see T-025), the extra in-memory filtering on ~5 documents is negligible — not a real gap at this app's actual scale.
- **Signal**: real gap found. `getSignals()` (the main `/api/v1/signals` list endpoint — the mobile dashboard's signals feed) has a default path when no `status` query param is given: `filter.createdAt = {$gte: last24h}`, `.sort({createdAt:-1})`. None of the 3 existing indexes (`{asset,createdAt}`, `{direction,confidence}`, `{status,expiresAt}`) lead with `createdAt`, so this — the most common, filter-less dashboard load — could not use any of them and would fall back to a collection scan as the table grows. `getLatestSignals()` (`status:'active'` + sort `createdAt`) and `getSignalStats()`'s aggregate `$match:{status:'active'}` have the same problem from the other direction. Added two indexes to `backend/src/models/Signal.js`: `{createdAt:-1}` (serves the filter-less default) and `{status:1, createdAt:-1}` (serves every status-filtered + createdAt-sorted query). Mongoose's default `autoIndex:true` (confirmed — no override in `src/config/db.js`) means these apply automatically on next connect, no manual migration needed.
DATE: 2026-08-18
COMMIT: `[pending]`
REMAINING RISK: low. This is a static review, not empirically confirmed with `.explain()` against a live collection with realistic data volume — that verification is blocked until Railway/MongoDB Atlas is reachable again (Priority 3). The two new Signal indexes are purely additive (no existing index removed, no query behavior changed) so there's no regression risk even if the static analysis turns out to be imperfect — worst case they're simply unused, not harmful. Flagged for a real `.explain()` pass once production is back up, to confirm rather than assume.

### PRIORITY 8 (continuous, follow-up) — Price Alerts also called a function that does not exist (T-029 follow-up)
STATUS: DONE
EVIDENCE: While writing the notification-delivery assertion for the T-029 test suite, noticed `checkAlerts()` calls `sendPushNotification(user.fcmToken, {...})`, imported via `const { sendPushNotification } = require('../services/notificationService')`. Grepped the entire backend: `notificationService.js` has never exported a function by that name (its actual "generic push" export is `sendPushToUser(userId, title, body, data)`) -- the only reference to `sendPushNotification` anywhere in the codebase was this one import and its one call site. Destructuring a nonexistent export yields `undefined`; calling `undefined(...)` throws synchronously, before `.catch()` can even attach. That throw was caught by `checkAlerts()`'s outer try/catch (so no crash, matching the "every job has error handling" state from T-024), but it happened *after* the alert had already been marked `active: false` -- so even once the first T-029 fix made `triggered` evaluate correctly, every real alert firing would still throw, log an error, and never actually notify the user, while also consuming the alert so it could never fire again. Net effect: identical to the original symptom (no notification ever delivered), just via a different failure path. Worth noting: my first pass's test suite for T-029 fully mocked `notificationService` with a `sendPushNotification` stub of my own invention, which meant it validated the (now-fixed) price-comparison logic correctly but never would have caught this second bug, because the mock quietly stood in for a function that was never real.
Fixed by switching the call to the real `sendPushToUser(alert.userId, title, body, data)` export, which also does its own internal FCM-token lookup and invalid-token cleanup -- letting the file's separate, now-redundant `User.findById` and the `User` model import be removed entirely. Added a dedicated regression-guard test that calls `jest.requireActual` on the real `notificationService.js` and asserts `sendPushToUser` is genuinely a function, specifically so a mock can never again quietly paper over a call to a function that doesn't exist. Full backend suite re-verified 136/136 green (130 pre-existing + 6 in this file), zero regressions.
DATE: 2026-08-18
COMMIT: `57584e0`
REMAINING RISK: low. This closes both known ways this specific job could fail to deliver a notification. General lesson applied going forward: when mocking a service in a test, prefer asserting the mocked function name is real (via `jest.requireActual` or similar) rather than trusting the mock's shape alone -- this bug specifically slipped past because the mock and the "fix" were written and verified together without cross-checking against the real module's actual exports first.

### PRIORITY 7 (continuous) — one scheduled job function still lacked error handling, plus two verification sweeps (T-030)
STATUS: DONE
EVIDENCE: T-024 (earlier this pass) closed the "zero try/catch" gap in `notificationRetryJob.js`, but that sweep checked try/catch presence per *file* (`grep -c "try {" <file>`), not per scheduled function. Doing a precise pass instead -- listing every `cron.schedule(...)` call site across all 18 job files and checking whether the specific function or inline callback it registers has its own try/catch -- found one gap T-024 missed: `expireOldSignals()` in `signalJob.js`, registered hourly, with no try/catch of its own (the file has one elsewhere, in `processAsset`, which is what let it slip past the coarser file-level check). Same risk profile as T-024: node-cron catches the rejection internally so it's not a crash risk, but a `Signal.updateMany` failure would vanish with zero log trace. Fixed the same way -- wrapped in try/catch, exported the function so it's directly testable, 3 new tests. Full backend suite re-verified 139/139 green.
Also ran two systematic static sweeps this pass, prompted directly by T-029's second bug (a call to `sendPushNotification`, a function that was never actually exported): (1) every `const { a, b } = require('./x')` destructure in `backend/src` cross-checked against the target module's real exports -- 113 files checked, 0 missing; (2) the same idea for `ai-service/app`'s `from app.services.x import a, b` -- 61 files checked, 0 missing. A third sweep cross-checked every `jest.mock('./x', factory)` call in `backend/__tests__` against the real module `./x`'s actual exports, to catch the specific blind spot that let T-029's second bug hide behind a passing test suite (a mock quietly standing in for a function that was never real) -- 0 drift found across all test files. All three sweeps used scratch scripts, run once, and deleted afterward (not committed) -- they're one-time verification, not a new CI gate (a real static-analysis tool like ESLint's `no-undef`-style import checking would be the right long-term answer if this class of bug recurs, but wasn't set up this pass to keep the change narrowly scoped).
DATE: 2026-08-18
COMMIT: `9821709`
REMAINING RISK: low. The two "does this import actually exist" sweeps and the mock-drift sweep together give strong (though not 100% -- regex/AST-based, not a full type-checker) confidence this specific bug class (T-029's second bug) has no other live instances right now. They're not wired into CI, so a *future* regression of the same kind wouldn't be caught automatically -- worth a follow-up to evaluate a real lint rule (e.g. `import/no-unresolved`-style) if the owner wants that guarantee going forward.

### PRIORITY 7 (continuous) — Hourly AI Report always showed a fake, static portfolio (T-031)
STATUS: DONE
EVIDENCE: While reading `hourlyReportJob.js` in full for the same "does this query use the right field name" class of check that found T-029's price-cache bug, found `generateHourlyReport()` queries `VirtualPortfolio.findOne({ key: 'global' })` -- but the model's real singleton field is `portfolioKey` (`VirtualPortfolio.js`: `portfolioKey: { type: String, default: 'global', unique: true }`). Grepped every `VirtualPortfolio.findOne` call site in the backend: `budgetController.js`, `aiWorkerService.js`, and `virtualTrackingService.js` all correctly use `portfolioKey`; `hourlyReportJob.js` was the one outlier. A query on a field that exists on no document always returns null, so the real portfolio lookup has silently failed every single hour since this job was written -- the report has always fallen back to its hardcoded placeholder (`$500 balance, $0 change, 0 open trades`), and that fake data flows into four places: the stored `AIReport` document, the Telegram message broadcast to every subscribed user and the admin channel, the FCM push notification, and -- when `OPENAI_KEY` is configured -- the prompt fed to GPT to write the report's narrative summary (meaning GPT would confidently narrate a portfolio that was never real).
Fixed the field name. 3 new tests: the query is built with the correct field name; a real portfolio document's actual balance/openTrades flow through the report instead of the $500 placeholder; a genuinely-missing portfolio document (e.g. a fresh, unseeded DB) still falls back to the placeholder safely rather than crashing. Full backend suite re-verified 142/142 green, zero regressions.
DATE: 2026-08-18
COMMIT: `8658040`
REMAINING RISK: low. This is a single-field fix with no change to any other logic in the file. One adjacent thing worth a future look, not fixed here to stay narrowly scoped: `dailyReportJob.js` and `weeklyReportJob.js` both call the shared `getSummary()` helper (not a raw `VirtualPortfolio.findOne`) for their portfolio figures, so they were not exposed to this specific bug -- confirmed by reading both files -- but it's worth double-checking `getSummary()`'s own query at some point using the same "does this field name actually exist on the schema" scrutiny, purely as due diligence given how this one hid in plain sight.

### PRIORITY 2 (continuous) — a single unhandled rejection anywhere could crash the entire backend server (T-032)
STATUS: DONE
EVIDENCE: While auditing every backend controller end-to-end for the "does this async route handler contain its own errors" property (the discipline that had already closed T-024/T-030 for scheduled cron functions), found `telegramController.js`'s `handleWebhook` was the one route handler in the entire codebase (26 controllers read in full this pass) with no try/catch and no `next(err)` delegation -- every other controller wraps its body one way or the other. Separately, `server.js` registers `process.on('unhandledRejection', (err) => { logger.error(...); server.close(() => process.exit(1)); })` at bootstrap. Express 4 does not await async route handler return values, so an error thrown inside `handleWebhook` after its initial `res.sendStatus(200)` (e.g. `User.findOne` or `user.save()` throwing on a DB blip or transient validation error, triggerable by any incoming Telegram update once the webhook is live) becomes a genuine process-level `'unhandledRejection'` event -- which is exactly what `server.js`'s handler is listening for. The combination means a single failed database call inside one Telegram webhook interaction could have taken the entire backend process down: every open API request, every WebSocket connection, every scheduled job (signals, price alerts, AI worker, reports) -- for every user, not just failed the one Telegram message. This is a more severe consequence than any of the missing-try/catch gaps found earlier this pass (T-024, T-030), which only risked a silently-swallowed single job cycle, never a full-process crash.
Fixed by wrapping the handler's body in try/catch, logging the error instead of letting it escape. 4 new tests: a `User.findOne` failure during `/start` resolves cleanly instead of rejecting; a `user.save()` failure during `/stop` resolves cleanly; the 200 acknowledgment to Telegram still fires before any of the risky awaits; a malformed update body with no `message` key is a safe no-op. All four assert on the handler's own returned promise settling rather than rejecting -- which is the precise condition that would have produced an unhandled rejection (and a process crash) before the fix. Full backend suite re-verified 146/146 green (142 previous + 4 new), zero regressions.
DATE: 2026-08-18
COMMIT: `e92ef73`
REMAINING RISK: low for now, was high-but-dormant before the fix. Currently unreachable in production because `verifyTelegramWebhook` (T-020's fix) fails closed without `TELEGRAM_WEBHOOK_SECRET` configured, and the owner's standing instruction is not to activate Telegram production config without credentials -- so this bug could not have fired in the app's current deployed state. It was nonetheless a real, demonstrated crash path that would have activated the moment Telegram is ever turned on, independent of that current dormancy, which is why it was fixed now rather than deferred. Worth noting for the owner: the global `unhandledRejection` handler's fail-fast-and-exit design is otherwise a defensible safety pattern (relying on a process manager to restart), but it does mean this class of bug -- one missing try/catch anywhere in the whole request-handling surface -- has an unusually severe blast radius in this codebase specifically. This pass read all 26 controllers, the middleware layer, the WebSocket server, and the core paper-trading services in full and found no other instance of a route handler or fire-and-forget async call lacking error containment.

### PRIORITY 5 (continuous) — social sentiment could silently become 100% fabricated data with zero log trace (T-033)
STATUS: DONE
EVIDENCE: While auditing the ai-service `collectors/` and `intel/` submodules (after finishing `order_block_engine.py`), found `reddit_collector.py`'s `collect_reddit_posts()` (`return posts if posts else _get_mock_posts()`) and `twitter_collector.py`'s `collect_tweets()` (`return tweets if tweets else _get_mock_tweets()`) both silently substitute hardcoded, fixed, mostly-bullish-skewed placeholder content whenever the live fetch fails or returns nothing -- indistinguishable from real posts once returned. `telegram_collector.py` already had this exact bug class fixed in an earlier pass (commit `a1864b5`, by the terminal session) specifically because the old version meant "the social sentiment score has never actually reflected real Telegram content" -- but the identical pattern was still live in the other two collectors, both with zero prior test coverage (as was `social_analyzer.py`, which orchestrates all three). `SocialAnalyzer.refresh()` folds whatever these functions return straight into sentiment scoring with no way to tell fabricated content apart from genuine chatter, and that scoring feeds `order_block_engine._get_sentiment()` (confirmed) and likely other signal paths -- so a Reddit/Twitter outage, rate-limit, or IP block (realistic on a hosted deployment) could silently replace live sentiment with fake content driving real trade confidence scores, with nothing in the logs to say so.
Fixed by adding `is_mock: bool = False` to `RedditPost` and `Tweet`, tagging only the hardcoded fallback content `True`; adding a `logger.warning(...)` at each fallback site (previously silent, or info-level only for Twitter's no-token case) naming that live collection failed and sentiment isn't live; and propagating `is_mock` through `social_analyzer.py`'s `_to_post_dict()` plus a new aggregate warning in `refresh()` naming which platforms and how many posts were mock. This does not change trading behavior (mock content still flows through, same as before -- removing it outright could reduce sentiment coverage during a brief transient outage, and that's a bigger behavioral change than this pass's evidence bar supports) -- it makes a previously invisible data-integrity gap observable for the first time. 15 new tests across 3 new test files (reddit/twitter/social_analyzer -- none of the three had any prior coverage): mock content correctly tagged, real content defaults `is_mock=False`, the fallback path fires and warns only when live collection genuinely returns nothing (both the no-token and token-configured-but-empty cases for Twitter), the non-fallback path never warns, and `SocialAnalyzer.refresh()` emits its aggregate mock-visibility warning only when at least one collected post is mock.
DATE: 2026-08-18
COMMIT: `b43f219`
REMAINING RISK: low. This is an observability fix, not a behavior change -- mock content can still enter sentiment scoring during a live-data outage exactly as before, just now flagged in logs (`is_mock` on each post, an aggregate WARNING in `social_analyzer.refresh()`) instead of silent. A follow-up worth an owner decision later: whether trade signal fusion should actively discount or exclude mock-tagged sentiment rather than just logging it -- not done this pass since it's a behavior change beyond a bug fix and the mock fallback exists partly to keep the pipeline from breaking during transient real-world outages. Verified in a clean venv built specifically for this pass (fastapi/pydantic/aiohttp/vaderSentiment/bs4/lxml/pytest/pytest-asyncio, plus numpy/pandas/motor/yfinance/starlette to unblock unrelated pre-existing suites): full ai-service suite 135/135 green (120 prior + 15 new), zero regressions. `test_cors_config.py` excluded from that run only because it imports the full FastAPI app graph (`sklearn`/`transformers`/`torch`-backed model files) unrelated to this change -- not worth installing to re-verify a file this pass never touched.

### PRIORITY 2 (continuous) — GlobalAnalyzer's macro-contradiction block and macro-weighted scoring silently never worked (T-034)
STATUS: DONE
EVIDENCE: While auditing `global_analyzer.py`'s macro-sentiment sourcing right after closing T-033, found `_get_macro_sentiment()` called `self._news.refresh()` (NewsAnalyzer -- per-asset headline sentiment) and read a key `"overall_sentiment"`. Three bugs stacked: (1) NewsAnalyzer's real response never contains that key inside `"global"` -- it's named `"sentiment"` (confirmed in `news_analyzer.py`), so the lookup silently fell back to `"neutral"` on every single call, the same bug class as T-031's `key`/`portfolioKey` mismatch; (2) even with the key fixed, NewsAnalyzer's `"sentiment"` field only ever takes `"positive"`/`"negative"`/`"neutral"` (confirmed in `news_sentiment.py`), never the `"bullish"`/`"strong_bull"`/`"strong_bear"` vocabulary this module's own macro-block sets and `macro_sc` checks require; (3) it was the wrong service entirely -- `macro_data_service.py`'s `MacroDataService.get_macro_snapshot()` is what actually derives that 5-state vocabulary from Fear & Greed + 24h market-cap change + BTC funding rate, and is the vocabulary `trade_quality_scorer.py` documents expecting. Net effect: the Phase 18 "macro contradiction block" (blocking BUY during strong-bear macro and vice versa) this module's own docstring advertises never fired on any scan, and `macro_sc` was always exactly 50 in both `_score_crypto` and `_score_multi_asset`, silently flattening a whole scoring dimension on every `/global/scan` call regardless of real market backdrop.
Fixed by wiring a `MacroDataService` into `GlobalAnalyzer` for the first time: added a `macro_service=None` constructor param defaulting to a real `MacroDataService()` (keeps existing 3-positional-arg call sites, including `test_global_analyzer_regime.py`'s 4 tests, working unchanged), rewrote `_get_macro_sentiment()` to call `self._macro.get_macro_snapshot()` and read `"macro_bias"`, and fixed `_macro_news_score()` (legitimately stays on NewsAnalyzer -- real per-asset headline sentiment, not macro backdrop) to use the correct `"sentiment"` key and `positive`/`negative` vocabulary. `routes.py` updated to construct `macro_service = MacroDataService()` before `global_analyzer` and pass it in explicitly. 11 new tests: `_get_macro_sentiment()` reads `macro_bias` from a fake `MacroDataService` (proven by giving the fake NewsAnalyzer a contradicting answer it must not use), falls back to `"neutral"` safely on exception, all 5 real `macro_bias` states pass through unmodified, `GlobalAnalyzer()` with no `macro_service` arg constructs a real `MacroDataService` (backward-compat proof), an explicit arg overrides the default, `_macro_news_score()` uses the fixed key/vocabulary (with a regression guard proving a stale `"overall_sentiment"` key is ignored), headline keyword hits still boost score, exceptions still return the safe 50 default, and an integration-level test proving a `"strong_bear"` macro snapshot is what a live `scan_all()` would now see and use to block BUY signals -- previously untestable since macro state could never be anything but neutral.
DATE: 2026-08-20
COMMIT: `8a244ae`
REMAINING RISK: low-medium. This restores a scoring dimension that was always inert (always neutral/50) to actually varying with real market conditions -- meaning `scan_all()` output (action/confidence/blocked counts) can now genuinely differ from before whenever `MacroDataService` reports a non-neutral bias, which is expected and correct given the module's own design intent, but is a real behavior change worth the owner's awareness (still paper-trading/signals-only, no live capital affected). `MacroDataService.get_macro_snapshot()` itself was read in full and confirmed already has its own error handling and a module-level cache; not modified this pass. Verified in a fresh venv against a clean pull of the device's current `ai-service/app` + `ai-service/tests` (i.e. including T-033's already-committed fix, not a stale scratch copy): full suite 146/146 green (135 prior + 11 new), zero regressions.

### PRIORITY 5 (continuous) — news trust scoring was computed but never actually used to weight sentiment (T-035)
STATUS: DONE
EVIDENCE: Found while auditing `news_analyzer.py`/`news_quality_layer.py` right after closing T-034 (which reads `global.sentiment` for per-asset headline scoring in `GlobalAnalyzer._macro_news_score()`). `NewsQualityLayer`'s own class docstring documents its quality formula as Source trust (40%) + Content quality (30%) + Recency (20%) minus a spam/clickbait penalty, and it builds a real per-source trust registry (Reuters/Bloomberg Markets at 1.00 down to Google News/Unknown at 0.40-0.55) plus a dedicated `weighted_sentiment_score()` method meant to apply that trust weighting -- but a grep across the entire `app/` tree confirmed that method was never called anywhere. Past `filter_and_score()`'s binary pass/fail quality cutoff, `NewsAnalyzer.refresh()` fed every passed headline into an unweighted average, so a low-trust source (CryptoPanic 0.60, Google News 0.55) moved the fused global sentiment exactly as much as Reuters or Bloomberg (1.00) -- silently defeating the entire purpose of the trust registry beyond the initial gate.
Fixed by trust-weighting the `global` section of `refresh()`'s result using the already-existing (previously dead) `weighted_sentiment_score()`: `headlines` is built directly from `filtered`, so each per-headline analysis result lines up positionally with its source article's `trust_score`, letting the two be paired and weighted correctly. Falls back to the prior unweighted behavior unchanged whenever quality filtering rejects every article (no trust data available on that fallback path). Per-asset scores and the quality pass/fail gate itself are unchanged -- this only fixes weighting among already-passed articles. 3 new tests (zero prior coverage on either file): high-trust source correctly outweighs an opposite-sentiment low-trust source, the result matches the manual trust-weighted formula, and the no-articles-pass-filter fallback is proven unchanged.
DATE: 2026-08-20
COMMIT: `21a6190`
REMAINING RISK: low. This changes `global.sentiment`/`global.market_score` output values whenever multiple sources of differing trust pass the quality filter together (previously always a plain average, now trust-weighted) -- a real, intended behavior change per the module's own documented design, not a bug in the fix itself. Blast radius is contained: `by_asset` (used by `order_block_engine.py` and `unified_analyzer.py` for actual per-asset trade scoring) is untouched; only `global` (consumed by `GlobalAnalyzer._macro_news_score()` from T-034, plus a few read-only `/news/*` API endpoints) changes. Still paper-trading/signals-only, no live capital affected. Verified in a fresh venv against a clean pull of the device's current `ai-service/app` + `ai-service/tests` (including T-033 and T-034's already-committed fixes): full suite 149/149 green (146 prior + 3 new), zero regressions.

### PRIORITY 6 (continuous) — non-crypto `position_size` silently ignored the real portfolio capital (T-036)
STATUS: DONE
EVIDENCE: Continuing the core-trading-module audit after T-035, found `GlobalAnalyzer._score_multi_asset()` (the gold/oil/forex scoring path) called `RiskManager.compute_position_size(500, atr_val, current_price)` with a hardcoded `500`, while its crypto sibling `_score_crypto()` (same file, same scan) correctly receives and uses the real `capital` argument `scan_all()` was actually called with. `scan_all()` itself never passed `capital` into `_score_multi_asset()` at all -- the call site simply omitted it. Real-world effect: `aiWorkerService.js` calls `POST /api/global/scan` with `capital: portfolio.currentBalance` (the real paper-trading balance), so any non-crypto asset's `position_size` field in that response was silently computed as if the account only ever held $500, regardless of the account's real size.
Confirmed (same check as T-028's note on this same field) that neither the backend nor mobile reads `position_size` anywhere for real order sizing or display -- backend computes its own `sizeUsd` independently -- so this had zero live trading or user-facing impact. Fixed anyway: it's a genuine correctness bug in a field the API response advertises, and the fix is a trivial, zero-risk one-line-per-call-site change (thread `capital` through `_score_multi_asset`'s signature, matching the existing crypto-path pattern exactly, no behavior change to any other field). 2 new tests: `position_size` scales correctly with the real capital argument (isolated via the function's own `entry_price<=0` early-return branch, which pins down exactly which capital value reached `RiskManager` without needing to mock real market data), and `scan_all()` is proven to pass its real `capital` through to every `_score_multi_asset()` call it makes (previously always silently 500 internally, independent of what the caller passed).
DATE: 2026-08-21
COMMIT: `2b413be` (autonomous commit/push this pass, per standing permission granted 2026-08-21)
REMAINING RISK: none identified. Single-field, single-code-path fix; the crypto scoring path (`_score_crypto`) was already correct and is untouched. Verified: full ai-service suite 154/154 green, zero regressions.

### PRIORITY 5 (continuous) — Strategy Engine's "expected move" scaling was silently constant across timeframes (T-037)
STATUS: DONE
EVIDENCE: Continuing the core-trading-module audit after T-036, audited `signal_engine.py` in full first (clean pass — every news/social/regime/multi-timeframe/funding-rate key it reads was cross-checked against the real source module's actual output keys, all correct, no bugs found) then `strategy_engine.py` (zero prior test coverage). Its `analyze()` computes `expected_move_percent` as `returns.std() * 100 * (len(df) ** 0.5)`, documented in a comment as volatility "annualised to timeframe" -- but `fetch_market_data` is always called with `limit=max(limit + 50, 100)`, which floors to exactly 100 for all three `_TIMEFRAME_MAP` entries (`"1d"`: limit=24, `"7d"`: limit=42, `"30d"`: limit=30 -- `max(x+50,100)` is 100 in every case), so `len(df)` was ~100 no matter which timeframe was requested, silently defeating the sqrt-of-time scaling the comment claims. The correct value was sitting unused in the same function the whole time: `limit` (from `_TIMEFRAME_MAP`) is exactly the number of `interval`-candles that make up one period of the requested timeframe (e.g. 24 hourly candles = 1 day), which is precisely the standard sqrt-of-time formula's period count (`sigma(T) = sigma(1) * sqrt(T)`) -- the padded fetch size (`len(df)`) exists only to give `returns.std()` a larger, statistically stable sample, and was never meant to double as the timeframe scaling factor.
Unlike T-036's inert field, this one is genuinely user-facing: `expected_move_percent` is parsed directly on the mobile Strategy screen (`strategy_model.dart`) and feeds `expected_profit`/`expected_loss` in every `/unified/analyze` response, which flows into `unified_provider.dart`, `dashboard_screen.dart`, `ai_brain_screen.dart`, and the backend's `decisionTrackingJob.js`/`AIDecision.js`. Fixed by switching the scaling factor from `len(df)` to `limit`. 2 new tests: requesting "1d" and "7d" against the *identical* underlying synthetic price series (the fake fetch ignores the requested limit entirely, so `len(df)` is identical for both calls -- exactly the condition that would make the old bug produce equal output for both timeframes) now correctly produces different `expected_move_percent` values whose ratio matches sqrt(42/24) exactly; a second test pins the output to the documented sqrt-of-time formula exactly for a single timeframe.
DATE: 2026-08-21
COMMIT: `a284452` (autonomous commit/push, standing permission granted 2026-08-21)
REMAINING RISK: low. This changes `expected_move_percent`/`expected_profit`/`expected_loss` output values for every timeframe other than whichever one happened to produce a value close to the old (effectively-fixed) scaling factor -- a real, intended behavior change per the code's own documented design (correct sqrt-of-time scaling now actually varies by timeframe as advertised), not a bug in the fix. No change to direction/confidence/recommendation logic, entry/SL/TP, or any other field. Still paper-trading/signals-only, no live capital affected. Verified: full ai-service suite 156/156 green, zero regressions.

### PRIORITY 6 (continuous) — position-size formula silently degenerates to a flat cap across the entire realistic ATR range (T-038)
STATUS: DONE (documented + tested; risk-parameter recalibration left as an owner decision)
EVIDENCE: Continuing the core-trading-module audit (post-T-037, coordinating with the terminal session's parallel T-036/T-037 work on the same file list -- confirmed via `git log` that neither touched `risk_manager.py`'s internals, only `global_analyzer.py`/`strategy_engine.py`). Zero prior test coverage existed on `risk_manager.py`. Its `compute_position_size()` docstring describes `size = balance x (risk_pct / atr_normalized)`, capped at `max_cap` -- implying the ATR-normalized formula is the primary driver and the cap is a rare safety ceiling. Computing the formula across the entire realistic ATR/price range (0.1%-19%, well beyond anything a tracked crypto/forex/commodity asset sustains even in sharp moves) showed every single value hits exactly the flat cap; the raw formula only produces a lower value once atr_normalized exceeds risk_pct/max_cap = 20%, a ratio effectively never seen in practice. Both call sites in `global_analyzer.py` use the function's defaults, so every `position_size` field in every `/global/scan` result is currently `capital x 10%` regardless of real volatility, not risk-normalized as the docstring describes.
Unlike T-034/T-035/T-036/T-037 -- each had one unambiguous correct fix confirmed against another source of truth already in the codebase -- there is no such reference here: RISK_PCT/MAX_POS_CAP are risk-policy constants, and picking new values is a real risk-calibration decision, not a bug fix with one objectively correct answer. Rather than silently changing live position-sizing math, this pass: (1) rewrote the docstring to accurately describe actual behavior instead of implying dynamic ATR-scaling that doesn't occur in practice, and (2) added 15 new tests covering `compute_sl_tp()` across all 4 regimes plus edge cases, and `compute_position_size()`'s actual contract -- including a parametrized regression guard proving the flat-cap behavior across 7 realistic ATR/price values, plus a confirming test that only an extreme (25%) ratio produces a below-cap value -- so any future change to RISK_PCT/MAX_POS_CAP forces a conscious look rather than a silent behavior shift.
DATE: 2026-08-22
COMMIT: `a19581b`
REMAINING RISK: none from this pass itself (no behavior changed -- docs + tests only). The underlying finding is a real OWNER DECISION, not resolved by this pass: whether to recalibrate RISK_PCT/MAX_POS_CAP so the ATR-based formula actually varies position size with volatility as originally documented, or to keep/relabel the current flat-cap behavior as intentional. No live trading impact either way -- paper-trading/signals only, and `position_size` is not read by backend order execution or mobile display (confirmed in T-036). Verified: full ai-service suite 174/174 green (153 prior + 21 new), zero regressions.

### PRIORITY 6 (continuous) — MACD "crossover" reason text was misleading on nearly every scored asset (T-039)
STATUS: DONE
EVIDENCE: Continuing the core-trading-module audit (post-T-038). Zero prior test coverage existed on `multi_timeframe_analyzer.py`. `_score_timeframe()` computed `prev = df.dropna().iloc[-2] if len(df.dropna()) > 1 else row` but never referenced `prev` anywhere else -- the smoking gun that the MACD reason logic (`if abs(macd_h) > 0: ... "crossover"`) was checking the wrong thing: a continuous MACD histogram value is essentially never exactly 0.0, so this fired on nearly every scored asset/timeframe regardless of whether an actual sign-change crossover from the previous candle had just happened -- it was really just restating the current MACD sign, not reporting an event. Confirmed via `git log --follow` this has been present since the file's original creation, not a regression. This `reason` field is returned directly by the live `/advisor/analyze` endpoint, read by the mobile Advisor screen. Score/action/confidence were unaffected (they read `macd_h`'s sign directly) -- only the human-readable explanation was misleading.
Fixed by using the previously-unused `prev` row's `macd_hist` to detect an actual sign change, labeling it "crossover" only then and "momentum" for ongoing same-sign direction. 9 new tests covering both crossover directions, both same-sign (non-crossover) cases, the single-row edge case, and the zero-histogram no-reason case, plus 3 `_risk_level()` boundary tests.
DATE: 2026-08-22
COMMIT: `b89f787`
REMAINING RISK: none. Text-only change to an explanation field -- no effect on action/confidence/score/SL/TP. Verified: full ai-service suite 183/183 green (174 prior + 9 new), zero regressions.

### PRIORITY 5 (continuous) — TradeQualityScorer's own 0-100 input contract was violated by an un-clamped fused_score (T-040)
STATUS: DONE
EVIDENCE: Continuing the core-trading-module audit (post-T-039). Zero prior test coverage existed on `trade_quality_scorer.py`. `build_quality_inputs()`'s docstring/module header promise "All inputs are 0-100 scores", and `compute_quality_score()`'s final output IS clamped via `min(100.0, max(0.0, score))` — but the individual `technical_strength` input (from `opp["fused_score"]`) was not clamped before entering the weighted sum. Traced the real call chain in `GlobalAnalyzer._score_crypto()`/`._score_multi_asset()`: the `fused_score` passed in is `adj_score = round(fs * modifier, 1)`, where `fs = _action_to_score(action, confidence)` and `modifier` comes from `RegimeDetector.regime_score_modifier()`. `unified_analyzer._score_to_action()` caps `confidence` at `min(95, int(score))`, so for a BUY `fs` tops out at 95 — but `regime_score_modifier()` returns 1.10 for a TRENDING-regime BUY, so any BUY with confidence in (~91, 95] produces `adj_score` up to 95 * 1.10 = 104.5, above the documented 0-100 bound, on a routine combination of inputs (not an exotic edge case). Because this happens before `compute_quality_score()`'s own final clamp, it is not reliably masked: the weighted sum can land well under 100 even while `technical_strength` itself is over 100 (e.g. technical=104.5, sentiment=macro=volume=50 → weighted score 71.8 instead of the correct 70.0) — a real, silent quality_score inflation large enough to flip a trade across `QUALITY_THRESHOLD=75` right at the boundary.
Fixed by clamping `technical_strength` (the proven overflow source) and `sentiment_alignment` (built from the same kind of externally-sourced, not-independently-bounded `news_score` input) to [0, 100] in `build_quality_inputs()`. `macro_alignment` is already inherently bounded by the fixed `macro_map` table and `volume_confirmation` was already clamped, so no change was needed there. 16 new tests covering weighting/output-clamp regression guards, the exact overflow scenario from the bug analysis, clamping at both bounds for both fields, in-range pass-through, HOLD-action neutral defaults, and macro/volume behavior confirmed unaffected.
DATE: 2026-08-22
COMMIT: 48018ef
REMAINING RISK: low. This tightens (never loosens) the inputs to a weighted average — no input that was already within [0,100] changes. The only behavior change is that a `quality_score` that was previously silently inflated by an out-of-contract `fused_score` will now be computed correctly, which could cause a small number of borderline trades right at `QUALITY_THRESHOLD=75` to now correctly fail the quality gate that previously passed it in error. Verified: full ai-service suite 199/199 green (183 prior + 16 new), zero regressions.


### PRIORITY 6 (continuous) — RLWeightEngine's floor/ceiling only bound the pre-normalisation raw value, not the effective weight actually used (T-041)
STATUS: DONE
EVIDENCE: Completing the core-trading-module audit (post-T-040 — this was the last item on the user's list: `rl_weight_engine.py`). Zero prior test coverage existed on this file. `record_outcome()` clamps each weight to `[MIN_WEIGHT=0.05, MAX_WEIGHT=0.70]` on every single call, but that clamp is applied to the *raw* value before `_normalise()` rescales the whole dict to sum to 1.0 — and that rescale step is not itself bounded. Confirmed by direct simulation (not just analysis): starting from `DEFAULT_WEIGHTS` and applying 500 rounds of realistic, sustained one-sided feedback (`technical` always WIN-attributed, the other three always LOSS-attributed — exactly the steady state this RL loop is designed to reach when one signal source persistently outperforms), `technical`'s *effective* weight (the one `get_weights()` returns and every live `rl_score` fusion formula in `GlobalAnalyzer` actually multiplies by) converges to ~0.7277 — above the documented 0.70 "ceiling" — while the dict still correctly sums to 1.0 throughout. This is a real, non-exotic, reachable-in-production gap, not a mathematical curiosity.
Unlike T-039/T-040 (each had one unambiguous correct fix), correctly enforcing both constraints simultaneously (sum=1.0 AND every individual weight within [MIN_WEIGHT, MAX_WEIGHT]) requires an actual algorithm choice — e.g. iterative water-filling redistribution of the clamped excess/deficit across the still-unclamped weights — not a single-line change, and it would alter live signal-fusion weights used in real (paper-trading) scoring. Per the standing instruction not to invent fixes without one evidence-backed correct answer, this pass: (1) corrected the MIN_WEIGHT/MAX_WEIGHT code comments, which previously said "floor"/"ceiling" without qualification, to accurately state they bound the raw pre-normalisation per-update value, not the final effective weight, and added a detailed explanatory note; (2) added 13 new tests — including two that explicitly lock in the current actual steady-state overflow behavior as a regression guard (so a future intentional fix has a clear, tested "before" to diff against) — plus coverage of basic update/win/loss/reset/stats/persistence/corrupt-file-fallback behavior that had zero prior tests.
Also investigated and ruled out as not-a-bug: `MIN_UPDATES_LOG = 5` is defined but never referenced anywhere in the module (confirmed via grep). Wiring it into the only existing logging gate (`self._updates % 10 == 0`) would be a no-op, since 10 > 5 means that condition can never fire before update 10 regardless — the same "unused but behaviorally redundant even if wired in" conclusion reached for `ATR_SIDEWAYS_THRESHOLD` in `regime_detector.py` earlier in this pass. Left unchanged.
DATE: 2026-08-22
COMMIT: aa818d7
REMAINING RISK: none from this pass — no behavior change, docs + tests only (identical pattern to T-038). REMAINING RISK in the underlying system: low-to-moderate over long RL training horizons — the effective weight ceiling/floor can drift a few percentage points past the documented bound under sustained one-sided feedback, gradually over-weighting one signal source in live rl_score fusion. Flagged as an OWNER DECISION below. Verified: full ai-service suite 212/212 green (199 prior + 13 new), zero regressions.


### PRIORITY 6 (continuous) — OrderBlockEngine leaked an internal `_dist` field into the live order-block API response (T-042)
STATUS: DONE
EVIDENCE: Continuing the autonomous audit into the next highest-priority unreviewed area per the owner's list (item 1: `order_block_engine.py`). Zero prior test coverage existed on this file. Confirmed by full read of the 405-line module plus cross-check against `ai-service/app/api/routes.py`: `OrderBlockRequest`/`/order-blocks/analyze` declares no `response_model`, so FastAPI serializes the raw dict returned by `analyze()` as-is, and that dict's `order_blocks` list contains the SAME dict objects `_generate_signal()` mutated in place with `ob["_dist"] = dist` — an undocumented, purely-internal value (never read anywhere else in the module, confirmed via search) that was leaking into every real `/order-blocks/analyze` API response and reaching consumers, including the mobile app, on every call.
This is an API-contract leak, not a scoring or trading-logic bug — fixed by computing `dist` as a local-only value; the mutation line was removed and replaced with an explanatory comment. Added 35 new tests, including 5 direct T-042 regression guards asserting `"_dist" not in ob` after `_generate_signal()` runs (across BUY/SELL/HOLD paths and multi-ob lists) plus one confirming the fix left the actual signal output unaffected, and broader coverage of `_generate_signal()`'s gating logic, `_hold_signal()`/`_fallback()`, `_strength_score()`, `_deduplicate()`, `_fuse()`, and the `_ema()`/`_rsi()` helpers — none of which had any prior tests.
While pulling a fresh copy of the ai-service tree into the local verification venv, found the venv was missing `scikit-learn` (required by `app/models/market_model.py`, which blocked full-suite collection) and that a prior fresh-pull had omitted `pytest.ini`/`conftest.py` (pytest-asyncio needs `asyncio_mode = auto` from `pytest.ini` to run async tests without per-test markers) — both were gaps in this session's own local verification harness, not product bugs. Fixed by installing scikit-learn into the persistent venv and re-pulling the two missing config files; noting this so the harness stays correct for future passes.
Also investigated and ruled out as not-a-bug: `OrderBlockRequest.timeframe` accepts any string, silently falling back to `("1h", 300)` via `_TIMEFRAME_MAP.get()` for unrecognized values — no evidence found of real-world reachability of an invalid timeframe value (unlike the confirmed, evidenced `_dist` leak), so this was not written up as a finding, per the standing instruction not to invent problems.
Also audited `data_processor.py` and `indicators.py` (both on the owner's suggested list): no bug found in either. `fetch_market_data()` performs a direct synchronous Binance fetch on every call with no caching layer, so there is no staleness-through-caching risk at this layer (distinct from the already-fixed T-027 backend WebSocket price-cache issue). `compute_indicators()`'s `dropna()` correctly drops the roughly 20 leading NaN rows produced by the RSI/ATR/Bollinger warmup windows (confirmed via `indicators.py`'s `min_periods` settings), which stays well within `order_block_engine.py`'s own `len(df) < 50` safety threshold even at the 1d timeframe's `limit=100`.
DATE: 2026-08-24
COMMIT: 29b83cf
REMAINING RISK: none from this pass for the fix itself — the `_dist` field was write-only internal state with no downstream reader inside the module, so removing it changes no scoring or signal behavior, only the shape of the public API response (which now correctly omits an undocumented field it should never have exposed). Mobile or any other consumer that happened to read `_dist` off the response (no evidence any does — it was never documented or referenced in this repo's mobile code) would simply stop seeing that key. Verified: full ai-service suite 250/250 green (215 prior + 35 new), zero regressions.

### PRIORITY 4 — UnifiedAnalyzer's fallback stop-loss/take-profit ignored the fused signal's direction (T-043)
STATUS: DONE
EVIDENCE: Continuing the autonomous audit into `/unified/analyze` — the endpoint backing the mobile dashboard, AI Brain, and virtual (paper) trades screens. Confirmed reachability before treating this as a finding: `backend/src/controllers/unifiedController.js` passes the AI-service response straight through with `res.json(aiResp.data)` (no filtering), and `mobile/lib/features/dashboard/widgets/signal_card.dart` renders "Stop Loss"/"Take Profit" tiles whenever they're non-null, with no gate on the signal's `action`. Zero prior test coverage existed on `unified_analyzer.py`.
The bug: whenever `OrderBlockEngine`'s own signal held (no `stop_loss`/`take_profit` of its own) but strategy+news+social sentiment still pushed the *fused* score to a directional BUY or SELL call, `analyze()`'s fallback synthesis unconditionally computed `stop_loss = current_price * 0.98` and `take_profit = current_price * 1.04` — a long-only shape (stop below entry, target above) — regardless of whether the fused action was actually BUY or SELL. For a fused SELL this handed out a stop_loss BELOW entry and a take_profit ABOVE entry: backwards for a short, offering no real downside protection and a profit target on the wrong side of price. The same unconditional block also fired for a fused HOLD whenever OB still returned a current price, inventing live-looking trade levels for a signal the system itself was calling non-actionable — inconsistent with every other HOLD signal in this codebase (`OrderBlockEngine._hold_signal` sets all trade fields to `None`).
Fixed to mirror `OrderBlockEngine`'s own SELL convention (stop above entry, target below for SELL — the same 2%/4% distances, just flipped; BUY unchanged) and to only synthesize levels when the fused action is actually BUY or SELL, never HOLD.
Added 18 new tests, including 5 direct regression guards: fused SELL with no OB-provided levels now gets a short-shaped stop above / target below; fused BUY still gets the original long-shaped stop below / target above (proving the fix didn't disturb the already-correct BUY case); fused HOLD with a valid current price gets no synthesized levels at all; OB-provided levels still pass straight through untouched regardless of direction; and an OB engine failure/timeout still safely leaves levels `None` with no crash. Also added coverage of the pure `_action_to_score`/`_score_to_action` helpers (58/42 thresholds, 95 confidence clamp), the documented 40/35/15/10 fusion-weight split, the neutral-HOLD no-engine-data case, and basic allocation math (60% capital allocation, 5% risk amount, win-rate source preference) — none of which had any prior tests.
DATE: 2026-08-24
COMMIT: 4b54e64
REMAINING RISK: none from this pass for the fix itself — the change only alters the *direction* of synthesized SL/TP to match the signal that's actually being returned, and removes synthesized levels for HOLD (bringing this path in line with every other HOLD signal in the codebase); no scoring, fusion-weight, or action-selection logic was touched. This bug was live in production-reachable code (paper trading only — no real-money trading is enabled) since `UnifiedAnalyzer` was introduced; any historical SELL signal that hit this exact fallback path (OB holds, fused signal still SELL) would have shown an inverted stop/target on the mobile dashboard and virtual-trades screens. No user-facing announcement is being made per standing instructions (paper trading only, no real capital was at risk), but this is worth being aware of when reviewing historical paper-trade records that may have used those inverted levels. Verified: full ai-service suite 268/268 green (250 prior + 18 new), zero regressions.

### PRIORITY 2 — ModelRegistry's checkpoint pruning silently deleted the live, just-trained model file (T-044)
STATUS: DONE
EVIDENCE: Continuing the autonomous audit into model loading/inference reliability, explicitly on the owner's suggested list. Zero prior test coverage existed on `model_registry.py`. Traced every real caller of `register()` (only 3 in the whole codebase: `routes.py`'s Transformer training endpoint, `main.py`'s auto-train-on-startup pipeline, `feedback_loop.py`'s auto-retrain-fusion-from-feedback) — all three pass a file path that is CONSTANT across repeated trainings for a given model: `TransformerModel._ckpt_path()` always returns `transformer.pt` (or `transformer_{asset}.pt`), `FusionModel._model_file()` always returns `fusion_model.joblib`. Retraining is designed to overwrite the model file in place, not write a new per-version file each time.
`register()`'s checkpoint-pruning step (once more than `MAX_VERSIONS=3` tracked versions accumulate) popped the oldest tracked entry and did `shutil.move(old_file, old_file + ".bak")` using only that entry's filename — with no check for whether any other still-tracked version, including the brand-new active one just registered, pointed at the same file. Because every real model in this codebase shares one fixed filename across all its versions, this meant the 4th (and every subsequent) `register()` call for a given model_name renamed away the SAME file the just-trained active model had been saved to moments earlier by the caller's own `model.train()` call. Any process restart afterward — both `TransformerModel.__init__` and `FusionModel`'s constructor load from that fixed path on init — would then find the model file missing and silently come back untrained. This is a real, recurring reliability bug: it fires on ordinary usage (any model retrained more than 3 times), and `feedback_loop.py`'s auto-retrain-on-accumulated-feedback makes hitting that threshold routine rather than rare.
Fixed by only archiving a pruned entry's file when no other still-tracked version references that same filename — a safe no-op for this codebase's actual fixed-filename models (archiving simply never fires, correctly, since the current codebase never has a truly orphaned file), while still correctly archiving a genuinely orphaned file if a future model is ever built to write distinct per-version filenames (covered by a dedicated test proving the safety net still works).
Added 21 new tests: the core T-044 regression guard registers a model 6 times against a single fixed filename (matching every real caller's actual pattern) and asserts the live file still exists on disk afterward with no `.bak` created, and that its contents are the most recently trained version's — plus a companion orphaned-distinct-filename test proving legitimate archiving still works when filenames genuinely differ. Also added first-ever coverage of version bumping/retirement, `get_active()`, `rollback()` (including no-previous-version and unknown-model cases), performance-history recording/rounding/200-record cap/trend direction, `summary()` shape, disk-persistence round-tripping across fresh instances, and corrupt-registry-file fallback.
Also confirmed a separate, pre-existing design gap while reading this module (not fixed here — flagged as an OWNER DECISION, same category as T-041's RLWeightEngine floor/ceiling gap): `rollback()` only flips a status label in the JSON registry file. `/models/rollback/{model_name}` never actually reloads different model weights into the running process — so calling "rollback" today updates bookkeeping only; it does not restore the previous model's actual behavior. Building real rollback (reloading a previous checkpoint's weights into the live model object) is a genuine architecture decision — it would require either keeping real per-version files on disk (a change from every model's current fixed-filename save/load convention) or a different persistence scheme entirely — not a bug with one obvious fix, so it wasn't changed.
DATE: 2026-08-24
COMMIT: 9d94683
REMAINING RISK: none from this pass for the fix itself — it only adds a check before an existing archive step and changes no version-tracking, rollback-labeling, or performance-tracking logic. Checked `ai-service/saved_models/` and `registry.json` directly as part of this pass: no stray `.bak` files exist, and every tracked model currently has only 1-2 registered versions (none has yet crossed the 4-registration prune threshold in this deployment's actual history) — so this bug was live and would have fired on the next couple of retrainings for several models, but has not caused any damage yet. The separate rollback-doesn't-reload-weights gap remains open, flagged above as an OWNER DECISION. Verified: full ai-service suite 289/289 green (268 prior + 21 new), zero regressions.

### OWNER DECISION — DriftDetector's entropy-drift baseline can be silently contaminated by "no model available" placeholder readings, making that signal mathematically unreachable (T-045)
STATUS: DOCUMENTED + TESTED (no unilateral code change — requires an owner decision, see options below)
EVIDENCE: Continuing the autonomous audit into AI reliability/monitoring. Zero prior test coverage existed on `drift_detector.py` before this pass. Traced `DriftDetector.record()`'s only real production call site — `feedback_loop.py`'s `evaluate_pending()` — which feeds `record()` whatever `sig.get("_transformer_proba")` was attached to the signal. `signal_engine.py` sets that field to the literal uniform placeholder `{"BUY": 33.3, "HOLD": 33.3, "SELL": 33.3}` whenever no trained transformer/LSTM model was available for that prediction (cold start for a newly-tracked asset, or a transient `predict()` exception) — never a special "no data" marker, so `DriftDetector` cannot distinguish a real uncertain prediction from a "no model" placeholder.
A uniform 3-way distribution has Shannon entropy `ln(3) ≈ 1.0986`, the mathematical MAXIMUM possible entropy for any 3-category distribution — confirmed by direct calculation. `_assess()`'s entropy-drift signal fires when `recent_entropy > baseline_entropy * 1.25`, where `baseline_entropy` is fixed forever as the mean of the first `ENTROPY_BASELINE_WINDOW` (100) entropy readings ever recorded by that `DriftDetector` instance (a long-running singleton in `routes.py`). If a meaningful share of those first 100 readings come from the "no model available" placeholder, `baseline_entropy` is pulled toward `ln(3)`. Once `baseline_entropy * 1.25` exceeds `ln(3)` (i.e. baseline > ~0.879), NO real prediction, however uncertain, can ever produce a `recent_entropy` that clears the threshold — entropy-drift detection becomes permanently, silently unreachable for that instance's lifetime, quietly losing one of the module's three documented drift signals (win-rate, entropy, calibration) with no error or log line calling it out. Proven directly with a regression test: a `DriftDetector` whose first 100 readings are all the uniform placeholder ends up with `baseline_entropy` numerically indistinguishable from `ln(3)`, and remains at `drift_level == "none"` even when fed nothing but further maximum-entropy readings afterward.
Practical severity, checked directly rather than assumed: as of this pass, every one of the 10 assets in `binance_collector.py`'s `TRACKED_ASSETS` already has a trained transformer checkpoint on disk in `saved_models/`, so the "no model available" fallback is NOT the common case in the CURRENT deployment — this is a real, reachable structural gap (cold start, any newly-tracked asset added in the future, transient `predict()` failures), not an actively firing bug on every request today.
Per the standing instruction not to change algorithm/policy blindly without an evidence-backed single correct answer: unlike T-042/T-043/T-044/T-046, there is no single unambiguous fix here. It requires a design choice with real tradeoffs:
  OPTION A — Exclude "no model available" readings from entropy-baseline/drift tracking entirely. Requires `signal_engine.py` to pass an explicit availability flag alongside `_transformer_proba` (e.g. `_transformer_available: bool`) so `feedback_loop.py` can skip calling `drift_detector.record()`'s entropy tracking (or skip `record()` entirely) for placeholder readings. Cleanest long-term signal; slows how quickly a fresh deployment or newly-tracked asset accumulates the 100 readings needed to establish any baseline at all.
  OPTION B — Bound the entropy-drift comparison relative to the theoretical ceiling instead of the raw baseline, e.g. `min(baseline_entropy * 1.25, MAX_ENTROPY - epsilon)` or a hard `baseline_entropy < 0.95` sanity check before trusting it. Smaller, self-contained change to `drift_detector.py` alone; a magic epsilon/ceiling still needs to be chosen, and doesn't address that the baseline itself is still contaminated (win-rate/calibration signals still evaluated at that contaminated baseline).
  OPTION C — Accept the current gap as low-severity for now, revisit if/when a newly-tracked asset or a period of transformer instability is anticipated. Zero code change; the existing win-rate and calibration-gap signals remain fully functional and unaffected — entropy is one of three signals, not the only one — and the practical reachability today is low (all 10 tracked assets have trained models).
This pass adds test coverage documenting and locking in the CURRENT (gap-containing) behavior as a regression guard — including a companion test proving entropy-drift detection DOES work correctly against a genuinely healthy (non-contaminated) baseline, so the gap is isolated and provable rather than a blanket "entropy detection is broken" claim — without picking one of the above options.
Added 16 new tests (of the 19 new in this pass; the other 3 are T-046's, listed separately): fresh-detector defaults; win-rate-only threshold boundaries (none/warn/critical) and the <15-outcomes no-assessment guard; calibration-gap triggering; rolling-window capping and roll-off behavior; consume-retrain-flag read-and-reset semantics; entropy-baseline establishment timing (not set before 100, set exactly at 100, immutable after); a healthy-baseline contrast test proving entropy-drift detection works correctly when NOT contaminated; and the two T-045 regression guards described above.
DATE: 2026-08-24
COMMIT: 638c567
REMAINING RISK: none from this pass itself — no source logic was changed for this finding, only test coverage was added. The underlying gap remains open pending an owner decision among the three options above. Verified: full ai-service suite 308/308 green (289 prior + 19 new across T-045+T-046), zero regressions.


### PRIORITY 2 — DriftDetector.status() silently reported a genuine win_rate=0.0 (total losing streak) as null instead of 0.0 (T-046)
STATUS: DONE
EVIDENCE: Found while writing first-ever test coverage for `drift_detector.py` (T-045 audit pass). `status()` built its response dict with plain truthy checks: `round(win_rate, 4) if win_rate else None`, and the same pattern for `avg_confidence`, `avg_entropy`, `baseline_entropy`. In Python, `0.0` is falsy, so a completely healthy, real reading of `win_rate=0.0` — every recent signal wrong, the single worst-case and most important state for a drift monitor to surface accurately — was collapsed into the exact same `null` as "not enough data recorded yet," making the two states indistinguishable to any caller. Confirmed this is a real, reachable, currently-live gap, not theoretical: `status()` is returned directly by the public `GET /models/drift` endpoint and is also embedded verbatim into two other route payloads (`app/api/routes.py` lines 181 and 200) — an operator or dashboard checking model health during an actual total losing streak would see `rolling_win_rate: null` and reasonably read that as "insufficient data," not "0% win rate, critical." The same falsy-zero flaw applied to `avg_confidence` (reachable if confidence=0 readings dominate the window) and, in principle, `avg_entropy`/`baseline_entropy` (not practically reachable given the module's 1e-9 floor clip on probabilities, but fixed for consistency and correctness).
Unlike T-045 below (found in the same file, same pass), this had one unambiguous correct fix with no design tradeoff: replace each truthy check with `is not None`. Fixed all four fields in `status()`. Added 3 new regression tests: a full-window total-losing-streak (`outcome=0` × WINDOW) asserting `rolling_win_rate == 0.0` and is not `None`; a zero-confidence-reading case asserting `avg_confidence == 0.0` and is not `None`; and a sanity check that the ordinary already-working case (a genuinely non-zero baseline_entropy) still reports correctly after the rewrite.
DATE: 2026-08-24
COMMIT: 638c567
REMAINING RISK: none — this only changes which values status() is willing to report as non-null; it changes no thresholds, no drift_level logic, and no recorded data. Verified: full ai-service suite 308/308 green (289 prior + 19 new across T-045+T-046), zero regressions.


### OWNER DECISION — feedback_loop.py always reinforces the RL weight engine with an identical, hardcoded signal_contributions split (T-047)
STATUS: DOCUMENTED + TESTED (no unilateral code change — requires an owner decision, see options below)
EVIDENCE: Continuing the overnight autonomous audit into AI reliability (feedback_loop.py had zero prior test coverage — added general coverage for SignalOutcomeEvaluator plus this specific finding). `evaluate_pending()`'s RL feedback call is:
```
from app.services.global_analyzer import rl_record_outcome
rl_record_outcome("WIN" if correct else "LOSS", {
    "technical": 0.70, "news": 0.20, "social": 0.10, "macro": 0.0,
})
```
This exact same dict is passed for EVERY evaluated signal outcome, regardless of what actually drove that specific signal. Traced why: `record_signal()` only ever persists `asset/direction/entry_price/confidence/generated_at/_transformer_proba/feature_vector` to Mongo — `signal_engine.py` (the actual generator of every signal that reaches this feedback loop) never computes or attaches a per-source technical/news/social/macro contribution breakdown at generation time, so by the time `evaluate_pending()` runs (up to `EVALUATION_DELAY_HOURS=4` later) there is no way to recover what really drove that particular signal. The hardcoded dict is a stand-in, not a placeholder pending real data.
Two concrete, evidence-backed consequences, proven directly against the real `RLWeightEngine` in the new test file: (1) every WIN reinforces, and every LOSS punishes, the identical 70/20/10/0 split — two `RLWeightEngine` instances fed the same sequence of WIN outcomes via this exact call converge to byte-identical weights, proving the update carries no information about how the two (in reality different) underlying signals were actually composed; (2) `macro`'s contribution is hardcoded to exactly `0.0`, so `delta = direction * LEARNING_RATE * contrib` is exactly `0` for macro on every single call — macro's raw (pre-normalisation) weight never receives a direct reinforcement signal from this call site, only passive redistribution via `_normalise()`'s sum-to-1 rescale whenever the other three weights move from their own direct updates. This is a distinct root cause from T-041 (which is about the post-rescale ceiling not holding) — this is one weight component receiving zero direct learning signal at all, by construction of the caller, for as long as the process runs.
Separately notable, checked directly: this cross-feeds outcomes of `signal_engine.py`-generated signals into the SAME `RLWeightEngine` singleton that `global_analyzer.py`'s `_score_crypto()`/`_score_multi_asset()` reads via `rl_get_weights()` for an entirely different, independent scoring feature — `signal_engine.py`'s own predictions do not themselves use RL-weighted fusion at all. Whether that cross-pollination between two otherwise-separate signal pipelines is intentional is itself an open question feeding into this finding.
Per the standing instruction not to invent a fix without one evidence-backed correct answer: unlike T-042/T-043/T-044/T-046, there is no single unambiguous fix. It requires design decisions with real tradeoffs:
  OPTION A — Have `signal_engine.py` compute and attach a real per-signal contribution snapshot (e.g. derived from which model components actually fired: transformer confidence, news/social scores if consulted, etc.) to the signal payload at generation time, thread it through `record_signal()`'s persisted fields (a new `_rl_contributions` field) and have `evaluate_pending()` read it back instead of hardcoding. Correct in principle, but `signal_engine.py`'s pipeline doesn't naturally produce a technical/news/social/macro split the way `global_analyzer.py`'s fusion score does — the mapping itself needs to be designed, not just plumbed.
  OPTION B — Stop cross-feeding `signal_engine.py` outcomes into `global_analyzer.py`'s RL engine entirely (they are separate pipelines); give `global_analyzer.py` its own outcome-evaluation loop feeding `rl_record_outcome()` with contributions it actually knows (it computes `fs`/`news_sc`/`social_sc`/`macro_sc` itself in `_score_crypto()`), decoupling the two features cleanly. Larger structural change — a new evaluation loop needs building for `global_analyzer.py`'s own signals.
  OPTION C — Accept the current behavior for now: the RL weight engine still moves (mostly via `_normalise()`'s rescale) and win-rate/calibration-based retraining triggers are unaffected: this only degrades the internal reward-attribution semantics documented as "reward winning signal combinations, punish losing ones", not simple win/loss detection. Zero code change.
Added 19 new tests: 15 covering `SignalOutcomeEvaluator`'s general mechanics for the first time (record_signal field storage, the 500-entry sequence-cache eviction, BUY/SELL/HOLD correctness thresholds, per-signal exception isolation within a batch, retrain-flag combination from win-rate and drift-detector, get_stats() shape) plus 2 direct T-047 regression guards proving the two consequences above against the real `RLWeightEngine`, and 2 more general-coverage tests folded into the retrain-flag class.
DATE: 2026-08-25
COMMIT: fd2cb03
REMAINING RISK: none from this pass itself — no source logic was changed, only test coverage was added. The underlying gap remains open pending an owner decision among the three options above. Verified: full ai-service suite 327/327 green (308 prior + 19 new), zero regressions.


### PRIORITY 2 — StrategySimulator crashed the whole multi-asset paper-trading simulation on one thin-history asset, or on capital=0 (T-048)
STATUS: DONE
EVIDENCE: Continuing the overnight autonomous audit into core paper-trading correctness (`strategy_simulator.py` had zero prior test coverage). Found two distinct, evidence-backed crash bugs in the same file while adding first-ever coverage:
1) `_simulate_asset()`'s insufficient-data early return (`len(df) < signal_window + 10`) omitted the `"final_balance"` key that its only caller, `StrategySimulator.simulate()`, unconditionally reads twice (`final_balance += sim["final_balance"]` and again in the per-asset result dict). Reproduced directly: any single requested asset whose fetched candle history is shorter than `signal_window+10` (realistic — a newly-listed asset, a partial/rate-limited Binance response, or simply a longer timeframe than the asset has history for) raised an unhandled `KeyError` from inside the per-asset loop, uncaught anywhere inside `simulate()`. `routes.py`'s `/strategy/simulate` handler does catch generic `Exception` and turns it into a 500, so the service itself doesn't crash, but ONE thin-history asset took down the entire batch response for every other requested asset in the same call, even ones with plenty of data — unlike a failed fetch (returned as an `Exception`/`None` by `asyncio.gather`), which was already handled gracefully with a flat "capital preserved, no trades" fallback. Fixed by adding `"final_balance": round(capital, 2)` to the early-return dict, matching that same fallback semantics.
2) Found while writing regression coverage for (1): the per-asset `return_pct` calculation divides by `per_asset_capital` with no zero-guard, while the AGGREGATE `return_pct` two lines below it already has one (`... if capital > 0 else 0.0`). `SimulateRequest`'s `capital` field has no validation constraint (no `gt=0`), so a caller passing `capital=0` via `/strategy/simulate` hit an unhandled `ZeroDivisionError` — reproduced directly. Fixed by adding the same guard already used for the aggregate figure one line below.
Verified both fixes with direct before/after reproductions (a short-history asset no longer raises `KeyError`; `capital=0` no longer raises `ZeroDivisionError`) before writing the permanent regression tests. Added 14 new tests: the two direct T-048 regression guards (KeyError fix, ZeroDivisionError fix) plus first-ever general coverage of `_simulate_asset()`'s P&L math (win/loss reconciliation with trade count, final_balance never negative, boundary-length behavior), the `_ema()` helper, and `StrategySimulator.simulate()`'s aggregation (failed-fetch fallback, capital split evenly across assets, unknown-timeframe fallback to 7d config, win_rate matching aggregated wins/trades).
DATE: 2026-08-25
COMMIT: e120898
REMAINING RISK: none — both fixes only add a missing dict key / a zero-guard matching an existing pattern one line away; no simulation logic, P&L math, or EMA/RSI signal logic was touched. Verified: full ai-service suite 341/341 green (327 prior + 14 new), zero regressions.


#### PRIORITY 1 — SmartSimulator inverted P&L sign for every SELL/short trade (T-049)
STATUS: DONE
EVIDENCE: Continuing the overnight autonomous audit into core paper-trading correctness (`smart_simulator.py` had zero prior test coverage). Found via careful manual symbolic tracing of `_simulate_asset()`'s TP/SL-hit pnl calculation — not a test failure. TP/SL are placed on the direction-correct sides of entry at signal-generation time (for a SELL/short entry, sl is placed above entry and tp below), so `hit_tp`/`hit_sl` are already direction-aware by construction: `pnl_pct = abs(tp - entry) / entry` on a TP hit is already positive for either direction (a TP hit is a win by definition), and `pnl_pct = -abs(sl - entry) / entry` on an SL hit is already negative for either direction (an SL hit is a loss by definition). The code then applied `if direction == "SELL": pnl_pct = -pnl_pct`, which inverted this already-correct sign specifically for SELL trades. Proven directly before touching source: an isolated replica of the exact formula showed a SELL trade hitting TP (win=True) producing pnl=-1.25, and a SELL trade hitting SL (win=False) producing pnl=+0.75 — both backwards. BUY trades were unaffected (the flip only fired for SELL). Practical impact: any simulation containing short trades — which the bearish EMA-crossover signal generates routinely, this being a two-directional strategy simulator — had a systematically wrong `final_balance`/`profit`/`return_pct`: a genuinely profitable bearish period would report as a loss and vice versa. `win_rate` (computed from the independent `"win"` field, itself correctly derived from hit_tp/hit_sl) stayed accurate throughout, which is precisely what made the bug easy to miss on a quick read — trade counts and win rate look sane; only the dollar P&L was wrong, and only for shorts. Fixed by removing the erroneous sign-flip block entirely — the pre-flip sign was already correct for both directions — and left an in-code comment documenting the reasoning and this finding for future readers. Re-verified with a second before/after reproduction script covering all four cases (SELL-hits-TP, SELL-hits-SL, BUY-hits-TP, BUY-hits-SL): all four now correctly signed post-fix. Added 14 new tests to a brand-new `test_smart_simulator.py` (zero prior coverage): direct T-049 regression guards using hand-crafted deterministic OHLCV DataFrames to force exact bearish/bullish entries and exact TP/SL exits (winning short → positive pnl, losing short → negative pnl, the win-field/pnl-sign invariant for SELL never disagreeing), a BUY-side unaffected guard, plus first-ever general coverage of the crossover entry conditions, `_compute_indicators()`'s output columns, `_summary()`'s message thresholds, and `SmartSimulator.run()`'s capital floor (min 10), per-asset insufficient-data handling without crashing the batch, and the 10-asset cap.
DATE: 2026-08-26
COMMIT: 96ef315
REMAINING RISK: none — the fix only removes a redundant/incorrect sign flip; no other simulation logic, EMA/RSI signal generation, or risk sizing was touched. Verified: full ai-service suite 355/355 green (341 prior + 14 new), zero regressions.

#### PRIORITY 3 — MacroDataService silently defeated its own 10-minute failure cache, re-hammering rate-limited free APIs after any single failure (T-050)
STATUS: DONE
EVIDENCE: Continuing the overnight autonomous audit into market-data reliability (`macro_data_service.py` had zero prior direct test coverage). Found via careful reading of `_cached()`'s contract (returns `None` for "no entry", the cached value itself — including a falsy one — for a hit) against its five call sites, all of which used `if hit := _cached(key): return hit`. Three of the five methods' documented failure fallback is the falsy value `{}`: `get_global_crypto()` and `get_funding_rates()` on any fetch exception, and `get_fred_series()` both when a series legitimately has no observations and on fetch exception. A cached `{}` is indistinguishable from "nothing cached" under a plain truthy check, so any call that failed once silently bypassed the cache on every subsequent call within the same 10-minute window and re-hit the upstream API again — directly contradicting the module's own stated purpose ("All calls are cached for 10 minutes to avoid hammering free endpoints."). CoinGecko's free tier and Binance's futures API are both rate-limited; re-hammering them specifically during the failure scenario (upstream trouble or rate-limiting) is the one case where the cache mattered most, and risks escalating a transient failure into an IP-level block that would degrade macro-context data (fear/greed index, market dominance/cap change, funding rates, Fed series) feeding the `macro_bias`/`macro_sentiment` AI signal inputs. Reproduced directly at the `_cached`/`_store` primitive level before touching source — confirmed a stored `{}` reports as a miss under the buggy pattern and correctly reports as a hit under `is not None`. Then confirmed end-to-end for `get_global_crypto()` and `get_funding_rates()` using a fake client that raises `AssertionError` if invoked a second time: under the pre-fix code the second call reached the network (assertion fired); post-fix it correctly served the cached `{}` without a network call. Fixed by changing all 5 occurrences from `if hit := _cached(key):` to `if (hit := _cached(key)) is not None:`. Added 20 new tests covering: the cache-helper falsy-value regression at the primitive level (empty dict, empty list, zero, genuine miss, expired entry); end-to-end no-refetch-after-failure guards for `get_global_crypto`, `get_funding_rates`, and `get_fred_series` (including the "API key set but series has no observations" case and the "no API key, short-circuits without caching" case); a genuine-truthy-cache-hit-still-avoids-network confirmation (to prove the fix didn't just turn every call into a miss); `get_fear_greed()`'s failure fallback; `_macro_bias()`'s full scoring logic (strong bull/bear thresholds, neutral midpoint, funding-rate adjustment, missing-BTCUSDT-key default); and `get_macro_snapshot()`'s exception-to-safe-default aggregation plus bullish/bearish `macro_sentiment` derivation.
DATE: 2026-08-26
COMMIT: ae2b34c
REMAINING RISK: none — the fix only tightens a cache-hit check from a truthy test to an explicit `is not None` test, matching `_cached()`'s actual documented contract; no fetch logic, parsing, scoring formulas, or cache TTL/eviction behavior were touched. Verified: full ai-service suite 375/375 green (355 prior + 20 new), zero regressions.

#### PRIORITY 4 — NewsQualityLayer's clickbait penalty was diluted 10x, letting some articles that should be rejected reach the AI sentiment pipeline (T-051)
STATUS: DONE
EVIDENCE: Continuing the overnight autonomous audit into AI reliability (`news_quality_layer.py` had zero prior direct test coverage — the existing `test_news_analyzer_trust_weighting.py` covers a different bug, T-035, in the caller `NewsAnalyzer`, not this class). Found via careful reading of `evaluate()`'s quality formula against its own class docstring, which documents the score as a weighted combination: "Source trust (40%), Content quality (30%), Recency (20%), Spam/clickbait penalty (10%)". The trust, content-quality, and recency components are each used directly at their stated weight (`trust * 0.40`, `cq * 0.30`, `rec * 0.20`), but `clickbait_penalty` — already set to exactly `0.10` (the full 10%-weight value) when a clickbait pattern is detected in the title — was then multiplied by ANOTHER `0.10` in the final subtraction (`- clickbait_penalty * 0.10`), reducing its actual effect on the score to just `0.01`: ten times weaker than every other component's per-unit weight, and ten times weaker than the module's own stated design. Proven directly, before touching source: a clean title and a clickbait title from the same trusted source (Reuters) produced quality scores differing by only 0.01 (expected ~0.10). Then confirmed a concrete, reachable pass/fail flip with a borderline article — a clickbait-titled, low-trust ("Unknown Source"), 30-hour-old article with mediocre content — which scored 0.375 (wrongly PASSING the 0.30 `MIN_QUALITY_SCORE` cutoff) under the buggy formula, versus the correct 0.285 (correctly REJECTED) once the full intended penalty is applied. Since `NewsQualityLayer.filter_and_score()` is called directly by `NewsAnalyzer.refresh()` and its passed articles feed the trust-weighted sentiment fusion that `GlobalAnalyzer` reads as macro/global signal input (T-034/T-035), this bug meant some clickbait-flagged, low-trust, stale articles the quality filter was explicitly designed to screen out were instead reaching the AI sentiment pipeline with nearly full effective weight. Fixed by removing the redundant `* 0.10` — `clickbait_penalty` is now subtracted directly, exactly like the other three weighted components. Re-verified post-fix: the clean/clickbait quality gap is now ~0.10 as intended, and the borderline article now correctly scores 0.285 and is rejected. Added 26 new tests to a brand-new `test_news_quality_layer.py` (zero prior direct coverage): the T-051 regression guard (penalty magnitude assertion, the exact pass/fail-flip case reproduced as a permanent test, confirmation that non-clickbait titles get no deduction at all), plus first-ever general coverage of `get_trust_score()`'s substring matching and default fallback, `is_spam()`/`is_clickbait()` pattern detection, `recency_score()`'s time-bucket thresholds and naive-datetime handling, `content_quality_score()`'s individual title/summary penalties and floor-at-zero behavior, `evaluate()`'s spam-rejection and age-rejection short-circuit paths, `filter_and_score()`'s batch pass/fail splitting and safe-default handling for missing article fields, and `weighted_sentiment_score()`'s trust-weighted averaging plus its empty-list and missing-trust-score defaults.
DATE: 2026-08-26
COMMIT: 12c690d
REMAINING RISK: none — the fix only removes a redundant multiplication that diluted one already-correctly-scaled penalty term; no other scoring weights, thresholds, spam/clickbait pattern lists, or trust registry values were touched. Verified: full ai-service suite 401/401 green (375 prior + 26 new), zero regressions.

#### PRIORITY 2 — trackerController's evaluate() let a zero-price recommendation permanently corrupt the shared accuracy stats (T-052)
STATUS: DONE
EVIDENCE: Continuing the overnight autonomous audit — moved to backend reliability/data integrity after three ai-service modules (signal_engine.py, data_processor.py, trainer.py) were read and traced in full with no unambiguous bugs found. `trackerController.js` had zero prior test coverage. Found by tracing `evaluate()`'s P&L computation the same way T-049's sign bug was found — by hand, not from a failing test. `evaluate()` computes `actualReturn = priceDiff / rec.priceAtRecommendation * 100` with no guard against a zero (or negative) `priceAtRecommendation`. `store()`'s own validator, `body('priceAtRecommendation').isFloat({ min: 0 })`, explicitly allows exactly 0 through (`min` is inclusive of the boundary) — and critically, the `/tracker/store` route requires only `protect` (any authenticated user), not `authorize('admin')`, so this was reachable by any logged-in user of the app, not just an admin. Reproduced directly with the exact formula before touching source: a record with `priceAtRecommendation=0` evaluates to `actualReturnPct: Infinity`. Once such a record is marked `evaluated`, `accuracy()`'s aggregate `avgProfitPerTrade = sum(profitIfFollowed) / total` — a stat returned to every user who calls `GET /api/tracker/accuracy` — permanently becomes `Infinity`, since nothing else in the sum could ever be large enough to dilute an `Infinity` term back to a finite number; the only way to recover is to manually delete the bad record from the database. Fixed two ways: (1) `store()`'s validator tightened from `isFloat({ min: 0 })` to `isFloat({ gt: 0 })` — verified directly against the actual installed `validator@13.15.35` package that `gt: 0` correctly rejects `'0'` and negative strings while still accepting any positive float; (2) added a defense-in-depth guard directly inside `evaluate()`'s per-record loop that skips (with a warning log identifying the asset and record id) any pending record whose `priceAtRecommendation` is not strictly positive, so a bad record that predates the validation fix — or reaches the collection any other way in the future — still can't corrupt the shared stats. Verified post-fix with both a supertest-driven check that `POST /tracker/store` with `priceAtRecommendation: 0` now returns 400 before ever reaching `AIRecommendation.create`, and a direct unit test confirming `evaluate()` skips (never calls `updateOne` for) a pre-existing bad record instead of computing `Infinity`. Added 14 new tests (zero prior coverage on this file), following the existing `priceAlertJob.test.js`/`inputValidation.test.js` conventions (supertest against the real router with `protect`/`authorize` and Mongoose model methods mocked, no real DB or network): the T-052 regression guard (rejects exactly-0, rejects negative, accepts a valid positive price, `evaluate()` skips a bad pre-existing record), plus first-ever general coverage of `evaluate()`'s BUY/SELL `wasCorrect` logic and its profit-sign invariant tested across all four BUY/SELL × win/loss combinations, the no-pending-records short-circuit (confirms it never calls axios when there's nothing to evaluate), per-record failure isolation (one asset's price-fetch failure doesn't abort evaluation of the rest of the batch), and `accuracy()`'s aggregation (per-asset breakdown, and the empty-evaluated-set case returning clean zeroed-out stats rather than NaN or a division error).
DATE: 2026-08-26
COMMIT: 458ce71
REMAINING RISK: none — the fix only tightens one input-validation boundary and adds one defensive skip-check at the exact division site; no P&L sign logic, expiry-window logic, pagination, or accuracy aggregation math was altered. Verified: full backend jest suite 160/160 green across all 14 test suites (146 prior + 14 new), zero regressions, run directly on the device against the real installed node_modules.

### OWNER DECISION — budget/start and budget/stop let any non-admin user reset the single shared AI paper-trading portfolio and force-close every open trade (T-053)
STATUS: DOCUMENTED + TESTED (no unilateral code change — requires an owner decision, see options below)
EVIDENCE: Continuing the overnight autonomous audit into backend/src/ controllers with zero test coverage after T-052. Read `budgetController.js` and `routes/budget.js` in full. `BudgetSession` (`sessionKey: 'global'`) and `VirtualPortfolio` (`portfolioKey: 'global'`) are both app-wide singletons — one shared AI paper-trading "budget manager" for the whole app, not one per user — yet `POST /budget/start` and `POST /budget/stop` are gated by `protect` only, with no `authorize('admin')`, unlike this codebase's own established pattern for other global-state-mutating endpoints (`routes/signals.js`'s `/scan` and `routes/tracker.js`'s `/evaluate` are both `authorize('admin')`-gated). Confirmed self-registration (`POST /api/v1/auth/register`, `routes/auth.js`) is open to anyone with no invite/approval step, and `models/User.js` always defaults new accounts to `role: 'user'`, never `'admin'`. So today, any registered non-admin user can:
  - `POST /budget/start` with any budget from $1 to $1,000,000 — this wipes the shared `VirtualPortfolio`'s entire performance history (`totalProfit`, `totalLoss`, `winCount`, `lossCount`, `peakBalance`, `maxDrawdown`, `bestTrade`, `worstTrade`, `balanceHistory` — the numbers every user sees on their dashboard) and resets it to the caller's chosen starting budget, while simultaneously force-closing every currently open virtual trade system-wide via an unscoped `VirtualTrade.updateMany({status:'open'}, {$set:{status:'expired', exitReason:'session_reset'}})` — not limited to the caller's own trades.
  - `POST /budget/stop` — pauses the shared AI budget manager for every user.
Unlike T-052 (an unambiguous validator bug with exactly one correct fix), this has no single correct fix. This project's own sibling endpoints show BOTH access levels already in active use for this class of global-state mutation, so there's no one obviously-intended convention to restore: `signals.js`/`tracker.js` admin-gate their equivalents, but the Flutter mobile app's Start/Stop budget-manager controls (`mobile/lib/core/providers/budget_provider.dart`) are wired for any signed-in user with no role check on the client side either, and this repo has no separate web/admin dashboard — the feature was built end-to-end (backend route + mobile UI) as available to any account, not just admins. Naively adding `authorize('admin')` to match the `signals.js`/`tracker.js` pattern risks locking the owner's own mobile app out of its core interactive feature if the owner's own account isn't itself flagged `admin` in the database.
Per the standing instruction not to change access-control policy blindly without an evidence-backed single correct answer, this requires an owner decision among:
  OPTION A — Add `authorize('admin')` to `POST /budget/start` and `POST /budget/stop`, matching `signals.js`'s `/scan` and `tracker.js`'s `/evaluate`. Correct if the shared budget manager is meant to be an admin-controlled demo/showcase that regular users only ever view. Risk: will return 403 to any non-admin account currently using the mobile app's Start/Stop controls, including the owner's own account if it isn't already flagged `admin` — worth confirming the owner's account role before applying this.
  OPTION B — Keep current behavior as-is. Correct if this app is intentionally used only by a small set of trusted accounts (e.g. the owner and a few invited people) where every registered user is implicitly trusted with the shared budget manager, or if open self-registration is not actually exposed/promoted anywhere in practice. Zero code change.
  OPTION C — Scope `BudgetSession`/`VirtualPortfolio` per-user instead of one global `'global'`-keyed singleton, so each user manages an independent paper-trading session without being able to affect anyone else's. This is the architecturally "correct" fix for genuine multi-tenancy, but is a real schema/migration change (new per-user documents, updated queries in every caller — `budgetController.js`, `aiWorkerService.js`, `virtualTrackingService.js`, `hourlyReportJob.js`, `guideController.js`, `pnlService.js` per PROJECT_STATUS.md's own `VirtualTrade` call-site notes above) — out of scope for a quick pass and needs explicit sign-off given its size.
This pass adds first-ever test coverage for `budgetController.js` (10 new tests) without changing any source file: two explicit T-053 regression guards proving a non-admin (`role: 'user'`) caller can currently reach `/budget/start` and `/budget/stop` and mutate the global singleton exactly as described (including asserting the unscoped `VirtualTrade.updateMany` call), plus general coverage of `start()`'s validation bounds (budget min/max, riskLevel enum), `status()`'s sessionPnL/winRate/totalTrades aggregation and its zero-division/zero-trades guards, and `report()`'s daily/weekly range selection, session-null handling, and balanceHistory 50-entry truncation.
DATE: 2026-08-26
COMMIT: 238e514
REMAINING RISK: none from this pass itself — no source logic was changed for this finding, only test coverage was added. The underlying access-control gap remains open pending an owner decision among the three options above. Verified: full backend jest suite 170/170 green across all 15 test suites (160 prior + 10 new), zero regressions, run directly on the device against the real installed node_modules.


#### PRIORITY 2 — advisorController's auto-tracking bypassed T-052's fix entirely, and the actually-scheduled tracker-eval cron never got that fix either (T-054)
STATUS: DONE
EVIDENCE: Continuing the overnight autonomous audit into backend/src/ modules with zero test coverage after T-053. While reading `advisorController.js`, found `_autoTrack()` (fire-and-forget, called after every successful `/advisor/analyze` ai-service call) writes `AIRecommendation` docs via `AIRecommendation.insertMany(docs, {ordered:false})` — a raw Mongoose model write, not a call to `trackerController.js`'s `store()` HTTP endpoint, so it never runs through that endpoint's express-validator at all and is completely unprotected by T-052's `isFloat({gt:0})` fix. The doc-building line `priceAtRecommendation: tf.current_price || 0` silently substitutes `0` whenever ai-service's response for a timeframe has no `current_price` (missing field, or itself `0`) — the `|| 0` fallback is itself evidence this was a recognized-but-mishandled edge case. Traced what happens to a `priceAtRecommendation: 0` record once created: `AIRecommendation`'s schema only has a two-value `status` enum (`pending`/`evaluated`), and every evaluate() code path just skips a bad-price record without ever updating its status — so such a record stays `status: 'pending'` forever, permanently re-matching the `{status:'pending', expiresAt:{$lte:now}}` query on every future evaluation cycle, indefinitely occupying a slot of that query's result limit with no way to ever clear it (confirmed with a standalone Node repro tracing `_autoTrack`'s doc-building and each evaluate path's skip condition against a degraded/zero-price input).
While tracing "does anything ever evaluate this," discovered `trackerEvalJob.js` — a SEPARATE, independently-written implementation of the exact same "evaluate pending recommendations" logic already fixed in `trackerController.js` for T-052, registered on its own `node-cron` schedule (`30 */2 * * *`, every 2 hours) and therefore the implementation that actually runs automatically in production — unlike `trackerController.js`'s `evaluate()`, which is only reachable via an admin-only manual HTTP call. T-052's fix never touched this file. Its own zero-price guard, `if (!currentPrice || !rec.priceAtRecommendation) continue;`, does correctly catch exactly `0` — but not a negative price, since `!(-10)` is `false` in JS. Checked reachability directly: grepped every `AIRecommendation.create`/`insertMany`/`new AIRecommendation` call site in the codebase (exactly two — `trackerController.js`'s now-validated `store()`, and `advisorController.js`'s `_autoTrack()`) — a negative `priceAtRecommendation` is not reachable through either write path today (store()'s validator rejects it post-T-052; `_autoTrack()` only ever wrote `0` or a real positive market price even before this pass), so the negative-price half of this fix is defense-in-depth, not a live corruption today, exactly matching T-052's own defense-in-depth rationale for the equivalent guard in `trackerController.js`.
Fixed two ways: (1) `_autoTrack()` now filters out any timeframe rec with a non-positive/missing `current_price` before building docs at all — root-cause fix, no zero-price zombie record is created going forward, with a warning log naming how many were skipped and why; (2) `trackerEvalJob.js`'s guard strengthened from `!rec.priceAtRecommendation` to `!(rec.priceAtRecommendation > 0)`, matching T-052's exact defense-in-depth pattern and protecting the actually-scheduled production cron path that T-052 never reached. Any zero-price zombie records already sitting in the database from before this fix are unaffected by this pass (no migration/cleanup script was written — the schema has no "invalid" status value to reclassify them into, and this pass's guard already stops both evaluate() paths from ever corrupting stats off an existing bad record); if the deployed database is later found to have such records, they can be identified via `AIRecommendation.find({status:'pending', priceAtRecommendation: {$lte: 0}})` and removed manually, the same recovery path noted for T-052.
Added 13 new tests across two brand-new files (zero prior coverage on either module): `advisorController.test.js` (7 tests — the T-054 regression guard confirming a missing/zero/negative `current_price` timeframe is dropped without being inserted while valid timeframes are still auto-tracked normally, plus general coverage of `/advisor/supported` and `analyze()`'s timeframe filtering and ai-service-unavailable handling) and `trackerEvalJob.test.js` (6 tests — the T-054 regression guards for both the already-working zero-price skip and the newly-fixed negative-price skip, plus first-ever general coverage of `evaluatePending()`'s correct BUY evaluation, its per-unique-asset price-batching, its per-asset failure isolation, and its no-pending-records short-circuit).
DATE: 2026-08-26
COMMIT: f1c71b2
REMAINING RISK: none — the fixes only prevent a new class of untrackable record from being created and add one defensive skip-check at a division site already proven unreachable today; no P&L sign logic, evaluation thresholds, batching, or accuracy aggregation math was altered in either file. Verified: full backend jest suite 183/183 green across 17 test suites (170 prior + 13 new), zero regressions, run directly on the device against the real installed node_modules.


#### PRIORITY 3 — sendTradeClosedNotification() ignored the fcmEnabled push preference every other broadcast path honors (T-055)
STATUS: DONE
EVIDENCE: Continuing the overnight autonomous audit, pivoted from backend/src/controllers/ and backend/src/jobs/ (now mostly clean, per the sweep recorded above) to backend/src/services/, since the real business logic mostly lives there. Read `binanceService.js`, `newsService.js`, `socialService.js`, `notificationService.js`, `firebaseService.js` in full — the first four are all clean, no bugs found. In `notificationService.js`, `sendTradeClosedNotification()` builds its FCM fan-out list via `User.find({isActive:true, fcmToken:{$exists:true,$ne:''}}).lean()` and pushes to every returned token, with no filter on `user.preferences.fcmEnabled` at all. Confirmed `fcmEnabled` is a real, schema-documented per-user preference (`models/User.js`, default `true`) that FOUR other broadcast-push paths in this exact codebase already respect before pushing: `sendSignalNotification` in this same file (line ~220, `.filter(u => u.preferences?.fcmEnabled !== false && u.fcmToken)`), `jobs/dailyReportJob.js`, `jobs/weeklyReportJob.js`, and `jobs/globalScanJob.js`'s `_notifyBrainUpdate`. `sendTradeClosedNotification` was the one exception to an otherwise-consistent codebase convention. Confirmed this is live, frequently-firing production code, not a rare edge case: it's called from `virtualTrackingService.js` on every single virtual trade close — both the automatic TP/SL/liquidation path inside `checkOpenTrades` (line ~531) and the manual "Sell Now" `closePositionNow` path (line ~626) — meaning a user who explicitly disabled push notifications in-app kept receiving one anyway on every trade close, silently overriding their stated preference. Reproduced with a standalone Node script (`/tmp/repro_t055.js`) before touching source: built a 3-user fixture (one with `fcmEnabled:false`) and ran the pre-fix token-selection logic against it — confirmed the opted-out user's token was present in the pushed-to list; running the fixed logic against the same fixture confirmed it's correctly excluded.
Fixed by filtering the `allUsers` list on `preferences?.fcmEnabled !== false` before mapping to FCM tokens — the exact one-line pattern already used at the four sibling call sites, so this restores the codebase's own established convention rather than introducing a new one. No other logic in the function (Telegram admin-channel broadcast, per-trade P&L formatting, delivery persistence) was touched.
Added `backend/__tests__/notificationService.test.js` (4 new tests, first-ever coverage for this file): the T-055 regression guard (an opted-out user's token is excluded from the `sendMulticast` call while an opted-in and a default-preference user's tokens are included), an all-opted-out case (confirms `sendMulticast` isn't called at all when nobody is eligible, avoiding an empty-array FCM call), a default-preferences case (confirms a user with no `fcmEnabled` key set at all — i.e. the schema default — still receives the push, proving the fix didn't accidentally flip the default to opt-out), and a no-fcmToken-field case.
DATE: 2026-08-26
COMMIT: 1acb6f3
REMAINING RISK: none — the fix only narrows the FCM recipient list to match an existing, already-tested codebase convention; no P&L math, trade-closing logic, Telegram delivery, or notification persistence was altered. Verified: full backend jest suite 187/187 green across 18 test suites (183 prior + 4 new), zero regressions, run directly on the device against the real installed node_modules.


### OWNER DECISION — virtual/reset and virtual/set-capital let any non-admin user permanently delete every user's trade history and reset the single shared paper-trading portfolio (T-056)
STATUS: DOCUMENTED + TESTED (no unilateral code change — requires an owner decision, see options below)
EVIDENCE: Continuing the overnight autonomous audit, pivoted to auditing route-level auth-gating for the remaining unchecked route files (11 of ~24 had been checked before this cycle). Read all 16 remaining route files (`aiBrain.js`, `brain.js`, `global.js`, `guide.js`, `health.js`, `macro.js`, `market.js`, `news.js`, `orderBlocks.js`, `pnl.js`, `priceAlerts.js`, `simulator.js`, `strategy.js`, `telegram.js`, `unified.js`, `virtual.js`) in full. `market.js`'s `/train` and `news.js`'s `/collect` are both correctly `authorize('admin')`-gated already. `virtual.js` is the one file in this batch with the same class of gap T-053 already found in `budget.js` — and a more severe instance of it. `router.use(protect)` gates the whole file with no `authorize('admin')` anywhere, and two of its endpoints mutate the single shared `VirtualPortfolio` (`portfolioKey: 'global'`) / `VirtualTrade` collection (schema confirmed to carry no per-user ownership field at all — every registered account shares the exact same paper-trading portfolio and trade history, per this session's earlier full read of both schemas):
  - `POST /virtual/reset` calls `virtualTrackingService.resetPortfolio()`, which runs `VirtualTrade.deleteMany({})` — permanently deletes every trade record for every user of the app, not just the caller's own — followed by `VirtualPortfolio.deleteMany({})` and a fresh `VirtualPortfolio.create()` at the caller's chosen starting balance/risk%. This is outright data destruction, a strictly more severe action than T-053's budget/start (which only overwrote fields and force-closed open trades; it never deleted trade history documents).
  - `POST /virtual/set-capital` calls `setCapital()`, overwriting the shared portfolio's `startingBalance`/`riskPerTradePct` for every user.
Confirmed via `mobile/lib/core/providers/virtual_portfolio_provider.dart` that both are wired into the Flutter app for any signed-in user with no client-side role check (`ApiService.dio.post('virtual/reset', ...)` and `('virtual/set-capital', ...)`, called directly from a portfolio-settings screen's reset/set-capital actions) — the identical "built end-to-end backend+mobile as available to any account, no separate admin surface exists anywhere in this repo" situation T-053 already documented for `budget/start`/`budget/stop`. Exactly like T-053, this has no single unambiguous fix: naively adding `authorize('admin')` risks locking the owner's own mobile app out of a core interactive settings feature if the owner's own account isn't itself flagged `admin` in the database, and this project's sibling endpoints for this class of global-state mutation already show both access levels in active use (`signals.js`'s `/scan` and `tracker.js`'s `/evaluate` are admin-gated; `budget.js`'s `/start`/`/stop` and now this file are not).
Per the standing instruction not to change access-control policy blindly without an evidence-backed single correct answer, this requires an owner decision among:
  OPTION A — Add `authorize('admin')` to `POST /virtual/reset` and `POST /virtual/set-capital` (and, for consistency, `budget/start`/`budget/stop` from T-053 at the same time, since they're the same underlying gap). Correct if the shared paper-trading portfolio is meant to be an admin-controlled demo/showcase that regular users only ever view, never reset. Risk: returns 403 to any non-admin account currently using these mobile settings controls, including the owner's own account if it isn't already flagged `admin` — worth confirming the owner's account role before applying this.
  OPTION B — Keep current behavior as-is. Correct if this app is intentionally used only by a small set of trusted accounts where every registered user is implicitly trusted with the shared portfolio (matching T-053's OPTION B reasoning), or if open self-registration isn't actually exposed/promoted in practice. Zero code change, but the data-destruction risk (any user can wipe everyone's trade history with one tap) is strictly worse than T-053's and worth the owner's explicit awareness even if the final answer is "leave it."
  OPTION C — Scope `VirtualPortfolio`/`VirtualTrade` per-user instead of one global singleton, so each user resets only their own paper-trading session without affecting anyone else's (same architecturally-correct fix T-053's OPTION C already identifies as out-of-scope for a quick pass — this finding is additional evidence for why that migration would be worth doing, since it would resolve both T-053 and T-056 at the root simultaneously).
  OPTION D (specific to this finding, not available for T-053) — Leave `set-capital` as any-user-reachable (it only overwrites two config fields, no deletion) but require `authorize('admin')` specifically on `/reset` given it is the one endpoint that permanently destroys data. A narrower middle ground than Option A if the owner is comfortable with users adjusting the shared risk settings but not wiping trade history.
This pass adds first-ever route-level test coverage for `virtual.js`'s `/reset` and `/set-capital` endpoints (5 new tests, `backend/__tests__/virtualResetRoute.test.js`) without changing any source file: three explicit T-056 regression guards proving a non-admin (`role: 'user'`) caller can currently reach both endpoints and that they reach the underlying service calls exactly as described (including the no-body-supplied default-to-$500/5% case), plus two tests confirming each endpoint's own field-level validation (`isFloat({min,max})`) still correctly rejects out-of-range values regardless of the access-control gap.
DATE: 2026-08-26
COMMIT: 196a3c1
REMAINING RISK: none from this pass itself — no source logic was changed for this finding, only test coverage was added. The underlying access-control gap remains open pending an owner decision among the options above, and is more urgent than T-053's given it involves permanent data deletion rather than a reversible overwrite. Verified: full backend jest suite 192/192 green across 19 test suites (187 prior + 5 new), zero regressions, run directly on the device against the real installed node_modules.


#### PRIORITY 3 — Ask-the-Brain asset news lookup used a field name that doesn't exist on the schema (T-057)
STATUS: DONE
EVIDENCE: Continuing the overnight autonomous audit into `brainController.js` after the route-level auth-gating sweep (T-053/T-056). `_buildAnswer()`'s asset-question handler queried `NewsData.find({ relevantAssets: symbol })` — but `NewsData.js`'s actual schema field is `relatedAssets`, confirmed by reading the schema directly and cross-checking every other query site in the codebase (`newsService.js`, `newsController.js`), all of which correctly use `relatedAssets`. A query on a field that exists on no document matches nothing (no error, no warning) — so every "Ask the AI Brain" question about a specific asset (e.g. "what's happening with BTC") silently returned zero matching news articles, regardless of how much real news had actually been collected for that asset, since the launch of this feature.
Fixed the field name (one-line change, no other logic touched). 4 new tests, first-ever coverage for this controller: the T-057 regression guard proving the query now filters on `relatedAssets` not `relevantAssets`; a real matching-articles fixture now correctly flows through into the built answer (previously always empty under the bug); confirms non-asset questions (e.g. performance/portfolio questions) never touch `NewsData` at all, so the fix doesn't introduce an unnecessary query on unrelated intents; confirms a missing `question` request body still returns 400 without touching any model.
DATE: 2026-08-26
COMMIT: `eb4415a`
REMAINING RISK: none identified. Single-field, single-query-site fix; no other behavior in the controller changed. Verified: full backend suite 196/196 green across 20 test suites (192 prior + 4 new), zero regressions.


#### PRIORITY 3 — AIDecision dedup window barely outlasted its own scan cadence, writing a near-duplicate decision almost every cycle (T-058)
STATUS: DONE
EVIDENCE: Owner sent a full product specification ("AI Trading Intelligence System") and required a complete product-to-code audit before any further code changes, explicitly suspecting repetitive BUY/SELL/WAIT decisions with no genuinely new information. Delivered a 16-item audit report (`PRODUCT_TO_CODE_AUDIT_2026-08-26.md`, sent to the owner) built from seven independent read-only traces of the real runtime call chains. Root cause #1: `decisionTrackingJob.storeGlobalDecision()` only skipped writing a new `AIDecision` if the *identical* asset+action pair had already been written in the last 15 minutes. `globalScanJob` (every 30 min) plus `aiDecisionJob` (which just re-triggers the same scan, offset at :15/:45) together produce an effective scan roughly every 15 minutes — so the window barely outlasted the job's own cadence, and a near-duplicate row got written on almost every cycle regardless of whether the market actually moved.
Fixed by replacing the fixed time window with real state-change detection against the most recent decision for that asset: skip only when the action is unchanged, confidence has moved by less than 5 percentage points, price has moved by less than 1.5%, and the last decision is under 6 hours old (a staleness ceiling ensures a fresh read still happens periodically even if nothing meaningfully changed). 7 new tests (first-ever coverage for this job): no-prior-decision creates; unchanged+recent skips; action flip creates; confidence jump (>=5pp) creates; small confidence wobble (<5pp) still skips; price jump (>=1.5%) creates; stale (>6h) prior decision creates even when otherwise unchanged.
DATE: 2026-08-26
COMMIT: e71b20f
REMAINING RISK: none identified for this fix in isolation — it only tightens when a new AIDecision row gets written, no other logic changed. It does not by itself resolve the audit's separate finding that three independent pipelines can still generate different decisions for the same asset (see T-060); that required its own fix. Verified: targeted test file green (7/7), full backend suite re-run in batches on-device, all 21 suites green.


#### PRIORITY 3 — hourlyReportJob pushed a notification every hour with no comparison to the previous report (T-059)
STATUS: DONE
EVIDENCE: Second, independent root cause identified in the same product-to-code audit for the reported repetitive-notification behavior. `generateHourlyReport()` sent a "BRAIN_REPORT" push (and Telegram message) to every eligible user every hour whenever any `Signal` existed in the trailing hour, with no check against what the previous hour's report had already said — so a user received a fresh push every hour even when the AI's actual recommendation was unchanged. This is a different code path from T-058 (report/notification job vs. decision-persistence job) and needed its own fix.
Added a change-gate that mirrors `globalScanJob.js`'s existing `_lastBest` pattern: the `AIReport` document is still created and stored every hour (it's a real historical record, and the audit's "market education" and "what changed" requirements benefit from that record existing), but the push/Telegram notification now only fires when the reported asset, action, market mood, or confidence (>=5pp) actually changed since the last time a notification was sent. 6 new tests added to the existing 3-test file (9 total): first-report-ever notifies; identical next report skips the push; the report document is still created even when the push is skipped; action flip re-notifies; confidence jump (>=5pp) re-notifies; small confidence wobble (<5pp) does not re-notify.
DATE: 2026-08-26
COMMIT: e71b20f
REMAINING RISK: none identified. Daily/weekly report jobs have the same "unconditional send" pattern noted in the audit report as a lower-priority follow-up (more defensible at daily/weekly cadence) — not changed in this pass. Verified: targeted test file green (9/9), full backend suite re-run in batches on-device, all 21 suites green.


#### PRIORITY 3 — aiWorkerService independently re-scanned every 5 min instead of reusing globalScanJob's cache, producing disagreeing decisions for the same asset (T-060)
STATUS: DONE
EVIDENCE: Third finding from the same audit, and the direct code-level explanation for "are multiple jobs making duplicate/conflicting decisions" — confirmed yes. Three independently-scheduled pipelines write AIDecision/Signal records: (1) globalScanJob (30 min) + aiDecisionJob (offset re-trigger) → AIDecision via decisionTrackingJob; (2) aiWorkerService (5 min) → its own independent `/api/global/scan` call → AIDecision + VirtualTrade; (3) signalJob (15 min) → `/api/predict` (SignalEngine, a fully separate model stack) → Signal. Pipelines (1) and (2) both hit the same non-deterministic `/api/global/scan` endpoint independently, so they could return different results for the same asset within minutes of each other, each persisted as its own AIDecision — surfaced to clients via two different controllers (`brainController.js` reading globalScanJob's cache, `aiBrainController.js` reading aiWorkerService's AIDecision rows) that could disagree.
Fixed the (1)/(2) overlap per the audit's recommended architecture (§15): `aiWorkerService.runAIWorkerCycle()` now reuses `globalScanJob`'s cached scan result when it is 35 minutes old or less, and falls back to its own direct `/api/global/scan` call only when the cache is empty or stale — keeping the worker functional standalone (at boot, or if globalScanJob's own cycle has failed) without routinely duplicating the call. 3 new tests added to the existing 8-test file (11 total): fresh cache is used and axios.post is not called; stale (>35 min) cache is ignored and a direct call is made instead; no cache at all falls back to a direct call.
The third pipeline (signalJob/SignalEngine) was deliberately left unchanged in this pass — it's architecturally a separate model stack (RandomForest/Transformer/fusion ensemble) rather than a scheduling duplicate of the same computation, and reconciling it into one canonical "AI Brain" (fold it in as a fusion input, or keep it clearly labeled as a secondary/experimental signal source) is a larger design decision the audit report flags for the owner rather than a bug fix.
DATE: 2026-08-26
COMMIT: e71b20f
REMAINING RISK: low. globalScanJob calls the scan with a fixed `capital: 500` while aiWorkerService previously passed the live portfolio balance; the cached response's action/confidence/quality fields the worker actually uses are not capital-dependent (position sizing is computed separately from the portfolio balance via `capToMaxRisk`, not read from the scan response), so this is not expected to change trade sizing — flagged here for visibility rather than treated as a hidden risk. The two controllers (`brainController.js`/`aiBrainController.js`) still read from two different underlying sources; picking one as sole authoritative and retiring/proxying the other (per audit §15) is a further follow-up, not done in this pass. Verified: targeted test file green (11/11), full backend suite re-run in batches on-device, all 21 suites green.


#### PRIORITY 3 — Guide-approved VirtualTrades had no persisted link back to the AI signal/decision that justified them (T-061)
STATUS: DONE
EVIDENCE: Final item from the 2026-08-26 product-to-code audit's §13 (T-058/T-059/T-060 already closed this same pass). `guideController.resolveSuggestion()` always sources the "just tell me what to do" suggestion from an AI-driven `Signal` document or the global-scan cache — never from client input, confirmed by re-reading the full request path. But `approveSuggestion()` (`virtualTrackingService.js`) only ever set `source: 'guide'` on the resulting `VirtualTrade`, never `signalId` or `aiDecisionId` — the two fields every other trade-opening path in the app (spot pickup, futures, the AI worker) already populates for exactly this purpose. A Guide-approved trade was therefore the one path in the app where a real, AI-sourced trade left no queryable trail back to what justified it.
Fixed by threading the source id through `resolveSuggestion() → approve() → approveSuggestion()`. For a Signal-sourced suggestion this is exact (`signalId: sig._id`). For a global-scan-sourced suggestion there's no synchronously-available persisted id at resolve time (`decisionTrackingJob` writes the matching `AIDecision` fire-and-forget, and T-058's new state-change gate means it may not write a fresh one at all on a given cycle) — so `approve()` does a best-effort lookup of the most recent `AIDecision` matching that asset+action and links it on a hit, leaving `aiDecisionId` null on a miss rather than ever blocking or delaying approval. 5 new tests: 3 for `guideController.approve()` (Signal-sourced trade gets the exact signalId with no AIDecision lookup performed; global-scan-sourced trade with a matching AIDecision links it; global-scan-sourced trade with no match still approves cleanly with a null id) and 2 for `virtualTrackingService.approveSuggestion()` (caller-supplied ids are persisted onto the created trade; both default to null and the function stays backward compatible when a caller passes neither).
DATE: 2026-08-26
COMMIT: fde2a36
REMAINING RISK: none identified. Purely additive — no existing field, validation, or sizing/risk logic touched; a miss on the best-effort AIDecision lookup degrades to exactly the prior (untraceable) behavior rather than failing the approval. This closes every remaining §13 item from the audit that didn't require an owner decision (Railway/infra) or the standing ML-model-delivery report-first process. Verified: 3 targeted test files green (64/64), 4 further test files that import guideController/virtualTrackingService re-run and green (no regressions).


#### PRIORITY 4 — app.js's CORS comment contradicted the actual T-010 deny-by-default behavior (T-062)
STATUS: DONE
EVIDENCE: Continuous-improvement pass (autonomous overnight loop, self-scheduled per the owner's explicit "don't stop" instruction) after the audit's four action items (T-058–T-061) closed. Read the backend entry-point files not yet explicitly audited this engagement: `server.js`, `src/app.js`, `src/config/db.js`, `src/config/logger.js`. `server.js` and `db.js` traced clean (DB-connect retry-without-crashing is a deliberate, correctly-implemented design choice per its own comment, not a gap). Found one real, provable discrepancy: the comment directly above `app.use(cors(buildCorsOptions(...)))` in `app.js` read "allow all origins in production" — but `corsConfig.js`'s actual logic (and its own docstring, added for T-010) does the opposite: unset/empty `ALLOWED_ORIGINS` denies all browser-based cross-origin requests by default, only a `*` explicitly present in the list opens it up (intended for local dev only). The comment described pre-T-010 behavior and never got updated when the security fix landed — low severity on its own (comment-only, the actual enforced behavior was never wrong), but a real maintenance hazard: a future change "fixing CORS to match the comment" would silently reintroduce the wildcard vulnerability T-010 closed.
Fixed the comment to match `corsConfig.js`'s actual, documented policy. No behavior change, so no new tests needed; re-ran `cors.test.js` (14 tests, still exercises `corsConfig.js` directly) plus a smoke-check that `app.js` still requires cleanly, both green.
Also investigated and ruled out a second, plausible-sounding suspect while in this area: winston's `File` transport writing to `logs/error.log`/`logs/combined.log`, and `logs/` being gitignored (confirmed via `git ls-files logs/` returning nothing) — a fresh clone/deploy has no `logs/` directory on disk. Rather than assume this breaks logging on a fresh Railway deploy, tested it empirically with a live Node script pointing winston at a genuinely nonexistent directory: this project's winston version (^3.13.0) auto-creates the missing directory on first write, no error event fired. Confirmed not a bug — noted here so it isn't re-investigated from scratch later.
DATE: 2026-08-26
COMMIT: ff994ab
REMAINING RISK: none. Comment-only change to a security-relevant but already-correct control; verified no behavior changed (cors.test.js green, app.js still boots).
#### OWNER DECISION APPLIED — budget/start and budget/stop are now admin-gated (T-063, resolves T-053)
STATUS: DONE
EVIDENCE: Owner reviewed T-053's three options (admin-gate / leave as-is / scope per-user) and chose admin-gate, matching this project's existing pattern on `signals.js`'s `/scan` and `tracker.js`'s `/evaluate`. `backend/src/routes/budget.js` now reads `router.post('/start', protect, authorize('admin'), ...ctrl.start)` and `router.post('/stop', protect, authorize('admin'), ctrl.stop)`. `budgetController.test.js` was rewritten in place: the old "regression: non-admin succeeds" tests (which had mocked `authorize` as a permissive no-op, so they only ever exercised the controller, never the actual gate) are replaced with tests against the REAL `authorize` middleware — a role-controlled `protect` mock reads an `x-test-role` header so both the 403-for-non-admin and 200-for-admin paths are genuinely exercised, plus the existing validation-bounds/status/report coverage (now run with an admin header, since authorize runs before validation in the chain).
IMPORTANT OPERATIONAL CAVEAT: this only actually protects the app if the owner's own account has `role: 'admin'` in MongoDB — self-registration always defaults to `role: 'user'` (see `models/User.js`), and this repo has no seed/bootstrap script that ever promotes an account to admin. This session could not verify the owner's account role directly: the sandboxed device-bridge VM used for this work cannot reach MongoDB Atlas (connection attempt failed with an IP-whitelist/network error, unlike the host machine's own network). **The owner must confirm their own account's role before relying on this fix** — if it is still `'user'`, they will be locked out of Start/Stop in the mobile app until it's promoted to `'admin'` (e.g. via `db.users.updateOne({email: <owner's email>}, {$set: {role: 'admin'}})` in mongosh/Compass, run from a machine with real Atlas network access).
DATE: 2026-08-28
COMMIT: `b29099f`
REMAINING RISK: low — resolved 2026-08-29: confirmed the owner's real account (`renastalabani@gmail.com`) was `role: 'user'`, exactly the lockout risk flagged above, and promoted it to `role: 'admin'` in the database with the owner's explicit go-ahead. No longer a caveat. Verified: full backend suite green (see T-064 entry below for the combined run — both fixes landed and verified together).

#### OWNER DECISION APPLIED — virtual/reset and virtual/set-capital are now admin-gated (T-064, resolves T-056)
STATUS: DONE
EVIDENCE: Owner reviewed T-056's options and chose admin-gate, same decision as T-053/T-063 (T-056 was the higher-urgency instance since `/reset` is data deletion, not a reversible overwrite). `backend/src/routes/virtual.js` now reads `router.post('/reset', authorize('admin'), [...validators], ...)` and `router.post('/set-capital', authorize('admin'), [...validators], ...)` (the file's own `router.use(protect)` already covers authentication; `authorize('admin')` is added per-route so the file's many other read/DCA endpoints stay open to any signed-in user as before). `virtualResetRoute.test.js` was rewritten the same way as `budgetController.test.js`: real `authorize` exercised via a role-controlled `protect` mock, non-admin now asserted 403 with the underlying service call never made, admin retains the exact prior behavior (including the field-validation-bounds tests, now run with an admin header since authorize runs before express-validator in the chain).
Same operational caveat as T-063 applied here — same shared account, resolved the same way (see T-063's entry: the owner's real account was confirmed `role: 'user'` and promoted to `role: 'admin'` on 2026-08-29, with explicit go-ahead).
DATE: 2026-08-28
COMMIT: `b29099f`
REMAINING RISK: medium until the owner confirms their own account is role:'admin' (see T-063). Verified: full backend suite 221/221 green across 22 test suites (196 prior + T-058..T-062's growth to 212, +9 net for this pass: two new-behavior test files replacing the old permissive-mock versions with real-gate coverage), zero regressions elsewhere. Full run: `npx jest --silent` on-device, 123.75s, all suites passed.

#### OWNER DECISION APPLIED — real WAIT/AVOID decision states added to SignalEngine (T-065)
STATUS: DONE (signal_engine.py path only — see SCOPE NOTE below)
EVIDENCE: The 2026-08-28 real-product-validation pass confirmed a genuine gap against the original product vision: `DECISION_TYPES` always specified BUY/SELL/WAIT/AVOID, but a live grep of the entire backend + ai-service found WAIT/AVOID nowhere — the running system only ever produces BUY/SELL/HOLD, with "avoid" existing only as an *implicit* absence of any signal, never a labeled state.
Investigated whether WAIT/AVOID could be added as real *trained* model output classes: no — `market_model`/`transformer_model`/`fusion_model`'s probability dicts are hardcoded 3-class (BUY/SELL/HOLD), so that would require retraining all three models, squarely inside the standing "no ML model changes without explicit analysis and approval" rule and a materially different, much larger project than a labeling fix.
OWNER-APPROVED DESIGN instead: a derived, display-only label computed on top of the trained models' unchanged real output, using risk signals the pipeline already computes but previously only used to nudge `raw_confidence` up/down (never surfaced as a distinct state): social manipulation/pump detection, the higher timeframe actively fighting the fused lean (multi-timeframe "fights" flag), and extreme funding-rate conflict (crowded-position contrarian check). Owner explicitly approved using exactly these three as AVOID triggers.
Implemented in `ai-service/app/services/signal_engine.py` (the module `/predict` actually calls — the same one this session's validation pass exercised live): a new pure function `_decision_label(final_dir, manip_detected, mtf_fights, funding_against)` — HOLD + no risk flag → WAIT; HOLD + manipulation detected → AVOID; BUY/SELL + no risk flag → unchanged (still BUY/SELL); BUY/SELL + any risk flag → AVOID. `mtf_fights` and `funding_against` are newly captured as named booleans from logic that already existed (previously computed inline and only used to adjust `raw_conf`, never retained). The result is written to a new `decision` field on the signal payload — `direction`/`confidence`/`raw_confidence` are all left completely unchanged, and every existing consumer of `direction` (backend paper-trading eligibility, quality filters, notification triggers) is unaffected; this is purely additive.
9 new unit tests added (first-ever coverage for `signal_engine.py`, `tests/test_signal_engine_decision_label.py`) covering every HOLD/BUY/SELL × flag-combination branch. TEST EXECUTION CAVEAT (2026-08-28, at authoring time): the device-bridge sandbox VM that wrote this had no network egress to PyPI and was missing `pydantic-settings`/most of ai-service's dependency tree, so the real test file could not be executed there — the `_decision_label` logic was independently re-verified by copying the function in isolation and running all 12 branch combinations in a standalone script. **RESOLVED 2026-08-29**: run from the real local `.venv` on the owner's machine — all 12 tests in `test_signal_engine_decision_label.py` passed for real, plus the full ai-service suite (413/413) and full backend suite (221/221), both green.
SCOPE NOTE (2026-08-28, at authoring time): the Guide screen's PRIMARY suggestion source is `globalScanJob` → ai-service `/global/scan` → `global_analyzer.py` → `unified_analyzer.py` — a materially different pipeline from `signal_engine.py`. Deliberately not extended this pass, flagged as a follow-up rather than guessed at. **RESOLVED 2026-08-29, see T-066**: traced the pipeline as instructed and found `manipulation_detected` genuinely was real, already-fetched, sufficient data (`UnifiedAnalyzer.analyze()` already calls `SocialAnalyzer.refresh()`, just wasn't reading that field) — extended WAIT/AVOID there too, reusing this same `_decision_label()` function. Multi-timeframe-fights/funding-rate-conflict remain a genuine follow-up (would need new I/O, not just wiring) — still open, now tracked under T-066 instead of here.
DATE: 2026-08-28
COMMIT: `501aa8a`
REMAINING RISK: low. Purely additive field, zero change to existing `direction`-keyed logic. Test-execution caveat resolved 2026-08-29 (see above) — real suite run, all green. Guide-pipeline scope note resolved the same day via T-066.

#### PRIORITY 4 — Guide home-screen pipeline extended with the same WAIT/AVOID label as SignalEngine (T-066)
STATUS: DONE
EVIDENCE: Directly resolves T-065's SCOPE NOTE above, per explicit owner instruction to trace the Guide pipeline first and only implement if the existing data was genuinely sufficient (not guess/invent). Traced the full pipeline before writing any code:
1. `globalScanJob.js` → `POST /api/global/scan` → `GlobalAnalyzer.scan_all()` → `_score_crypto()` (crypto) calls `UnifiedAnalyzer.analyze()`; `_score_multi_asset()` (gold/oil/forex) is a separate, independent method with no social component at all.
2. `UnifiedAnalyzer.analyze()` already calls `self._social.refresh()` (SocialAnalyzer) unconditionally on every call — the same real data source `signal_engine.py` uses for its own `manipulation_detected` check — but only ever read `sd.get('market_score', 50)` from the result, discarding `manipulation_detected`/`pump_detected` even though they were already sitting in the already-fetched response. This is genuinely free, real, already-fetched data — not something that needs a new API call, matching T-065's own bar for what's safe to surface.
3. Multi-timeframe-fights (`MultiTimeframeAnalyzer`) and funding-rate-conflict (`MacroDataService.get_funding_rates()`) are NOT called anywhere in `_score_crypto`/`_score_multi_asset` today. Adding either would mean new async I/O per asset per scan cycle (13 assets, every 30 minutes) — a real architectural/latency/cost addition, not wiring existing data through. Per the owner's explicit instruction ("if it does NOT have enough information, do NOT guess... document exactly what is missing... stop for owner approval"), this was deliberately NOT added — both `mtf_fights` and `funding_against` are passed as hardcoded `False` into the shared `_decision_label()` function, honestly reflecting what this pipeline actually knows today. Flagged as a follow-up requiring an explicit owner decision (new I/O cost/latency trade-off), not guessed at.
4. `_score_multi_asset()` (gold/oil/forex) has no social/manipulation source of any kind — confirmed via a full read of the method, no `SocialAnalyzer` call anywhere. So for that asset class, `decision` can only ever equal `action` (or WAIT for HOLD) — never AVOID. This is an honest reflection of what this pipeline actually knows for those assets, not a gap papered over.
5. Structural finding worth recording: **WAIT can never actually reach the Guide's "I have an active suggestion" state in either pipeline.** `scan_all()`'s own filter (`confidence >= MIN_CONFIDENCE=70`) always rejects a HOLD-scored candidate before it can become `best` (`_score_to_action()` scores HOLD at confidence 50, well under 70) — so `best` is always either an already-actionable BUY/SELL or `None`. Separately, `signalJob.js`'s `processAsset()` already discards HOLD signals before `Signal.create()` (`if (direction === 'HOLD') return null;`). So WAIT is already fully represented by the pre-existing "The AI is still studying the markets" empty state on the Guide screen — the only genuinely new, reachable state this pass needed to add was AVOID.

IMPLEMENTED (data was sufficient for AVOID via manipulation-detection — the only risk flag this pipeline can honestly compute today):
- `ai-service/app/services/unified_analyzer.py`: surfaces `sentiment.manipulation_detected` (from the already-fetched social result); computes `signal.decision` by importing and reusing T-065's `_decision_label()` (not reimplementing the labeling rule — both AI pipelines now share one definition of what AVOID means) with `manip_detected` from the real flag and `mtf_fights`/`funding_against` hardcoded `False` per point 3 above. Purely additive — `signal.action`/`signal.confidence` are untouched, verified by the existing `test_unified_analyzer.py` suite passing unchanged.
- `ai-service/app/services/global_analyzer.py`: `_score_crypto()` passes `sig.get("decision", sig["action"])` straight through from `UnifiedAnalyzer`'s own computation (no recomputation, no duplicated logic) — falls back to `action` if an older `UnifiedAnalyzer` response is ever missing the key. `_score_multi_asset()` computes its own `decision` the same way, with no manipulation source (so it can never produce AVOID, per point 4).
- `backend/src/models/Signal.js`: added a non-required `decision` field (enum `BUY/SELL/WAIT/AVOID`, no default) — pre-existing Signal documents are simply undefined on it, no migration needed.
- `backend/src/jobs/signalJob.js`: `processAsset()` now persists `decision: prediction.decision || direction` — ai-service's `/predict` has returned this since T-065, it was just being silently discarded on the way into `Signal.create()`.
- `backend/src/controllers/guideController.js`: `resolveSuggestion()` now surfaces `decision` from both the global-scan-cache branch (`best.decision || best.action`) and the Signal-fallback branch (`sig.decision || sig.direction`) — the exact same fallback-to-action pattern used throughout, so an older ai-service/backend build or a pre-existing Signal document never produces `undefined`. `getSuggestion()`'s JSON response includes the new field.
- Mobile (`mobile/lib/core/providers/guide_provider.dart`, `mobile/lib/features/guide/guide_screen.dart`): `GuideSuggestion` carries `decision` (falls back to `action` client-side too, belt-and-suspenders). `_SuggestionCard` shows a distinct amber "· FLAGGED" badge, a specific warning banner ("The AI found a lean here, but flagged a real risk... read carefully before acting"), and relabels the approve button "Do it anyway" when `decision == 'AVOID'`. The underlying `approve()` mechanism and trade direction are completely unchanged — this app is paper-trading only and had already settled on an "AI suggests, user decides" autonomy level earlier in the project, so the risk is made visible rather than the action being silently blocked.

`action`/`confidence`/trading eligibility/notification triggers are unchanged everywhere in both pipelines — purely additive, exactly matching T-065's own philosophy, so this does not reintroduce or interact with the repetitive-decision/notification-suppression fixes from T-058/T-059/T-060.

TESTING: 16 new tests. ai-service (`test_guide_pipeline_decision_label.py`, 8 tests): `UnifiedAnalyzer` correctly surfaces `manipulation_detected` and computes `decision` for BUY/SELL/HOLD crossed with manipulation present/absent (including `pump_detected` alone also triggering AVOID); `GlobalAnalyzer._score_crypto()` passes the decision through unchanged and falls back to `action` when the key is missing; `_score_multi_asset()` proven to never produce AVOID (no social source). Backend (`guideDecisionLabel.test.js`, 5 tests; `signalJobDecisionPersist.test.js`, 3 tests): `resolveSuggestion()` surfaces `decision` correctly from both branches including the pre-existing-document fallback case; `processAsset()` actually persists the field onto the real `Signal.create()` call, including the older-ai-service-response fallback case. Verified: full ai-service suite 421/421 green (413 prior + 8 new), full backend suite 229/229 green (226 prior + 3 new), `flutter analyze` clean on both changed mobile files.

LIVE VERIFICATION (against the real running local stack, not just unit tests, per explicit owner instruction): restarted backend + ai-service + web app with the new code. `POST /unified/analyze` for BTCUSDT (real live market data) returned `signal.decision: "WAIT"` and `sentiment.manipulation_detected: false` for a real HOLD case — confirmed correct end to end. Re-ran the real `runSignalGeneration()` cycle live against the real DB: confirmed dedup still correctly suppressed a re-evaluated ADAUSDT signal as a duplicate — not reintroducing the repetitive-decision problem T-058/T-059/T-060 fixed. Screenshotted the live web app (localhost:5173) confirming it still renders the Guide screen correctly with the new required mobile model field, no crash, existing empty-state behavior unaffected. Could NOT observe a live AVOID case today specifically — 0 of the currently-scanned assets have `manipulation_detected: true` this cycle, an honest reflection of real current market/social conditions, not a bug — that code path's correctness rests on the 16 new automated tests (which directly exercise it with realistic fixtures) plus the live-confirmed WAIT case sharing the exact same `_decision_label()` code path and wiring.
DATE: 2026-08-29
COMMIT: `b0881cc`
REMAINING RISK: low. Purely additive field, zero change to any existing `action`/`direction`-keyed logic anywhere. Two things intentionally left open, both flagged above rather than guessed at: (1) whether to add multi-timeframe-fights/funding-rate-conflict as AVOID triggers for the Guide pipeline too, which needs new I/O per asset per scan cycle — an owner decision on latency/cost trade-off, not a bug; (2) a live AVOID case specifically wasn't observed today (honest absence of current risk-flagged candidates, not a gap in coverage).

### Not yet started (blocked or queued)
- PRIORITY 3 continuation — awaiting owner decision on (a) whether/when to trigger a redeploy of backend + ai-service, and (b) how trained models reach the ai-service container (see above). Once redeployed: repeat health/DB/model/cron verification against a live instance, since none of that could be observed this pass.
- PRIORITY 4 (model artifacts) — partially answered above (delivery path gap found); full resolution still open.
- PRIORITY 5 (mobile inspection) — started (T-012, see above), partial coverage.
- PRIORITY 6 (API docs, `ai-service-err.log` triage) — done (T-006, T-007, see above).
- PRIORITY 7/8 (monitoring, rollback) — not yet started.
- T-020 (Telegram webhook authenticity check) — code complete and tested; confirmed **not configured at all** in Railway (no `TELEGRAM_WEBHOOK_SECRET` var exists yet), on top of not being live. Needs the owner to set it in Railway and call Telegram's `setWebhook` with a matching `secret_token` (see the dedicated entry above for exact commands) — and this should happen in the same window as a redeploy, not before or after, per the fail-closed design.

## CURRENT PHASE
Phase 1 (CI/CD) is live and green on GitHub Actions. Phase 4 (Security / Priority 2) is complete and pushed. **Phase 5 (Priority 3 / Railway) is audited but blocked**: production is down on both services (pre-dates this engagement — last live deploy attempts were 2026-04-26/05-04), and bringing it back up is a deploy decision that needs explicit owner sign-off, not an audit action.

## OVERALL PRODUCTION READINESS
**Still NOT production-verified — and now confirmed NOT production-running.** GitHub-side work (CI, security fixes, tests) is solid and evidenced. But "the code is good" and "the system is online" are separate facts: right now there is no live backend or ai-service behind either Railway domain, both health endpoints 404 at the edge, and production is 48 commits stale even before accounting for the fact it never successfully ran the current codebase at all. Next real milestone is not "more code" — it's an owner decision to redeploy, plus a decision on how trained ML models actually get into the ai-service container, followed by genuine (not assumed) health/DB/model/cron verification against a live instance.
